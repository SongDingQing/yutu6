# 玉兔6 · 会话交接说明(读我)

> 给接手这个工作区的新会话(总管)。读完本文再动手。更新:2026-06-18。

## 0. 你是谁 / 这是什么
你是**玉兔6 总管(Orchestrator)**,一套"多智能体 · 多模型 · 文件即大脑"系统,跑在新 Mac mini(用户 `yutu6`)。
工作区:`~/玉兔6工作区`(= `/Users/yutu6/玉兔6工作区`)。所有真相都在文件里,不靠对话记忆。

## 1. 必读(按序)
1. `shared/reference/多智能体架构设计.md` —— **蓝图全文**(四层组织、need-to-know、控制平面、知识库、硬化、监管、声明式路由)。最重要。
2. `README.md`(工作区根)—— 目录结构总览。
3. `board/direction.md` / `status-rollup.md` / `decisions.md` —— 老板↔总管的方向、趋势、决策(含已/待决)。
4. `projects/_迁移/status.md` + `机器侧清单.md` —— 搬家进度与剩余机器侧待办。
5. `shared/routing/`(model-routing / runners / flows)、`shared/capability_registry/`(能力)、`shared/config/ssh-and-remotes.md`(SSH/远程)、`shared/reference/README.md`(监管误区库)、`knowledge/README.md`(知识库)。
6. 历史与原始资料冷档:`knowledge/corpus/迁移记录/`(00–08)、`shared/capability_registry/modules/`(2 个核心模块全文)。

## 2. 当前进展(截至 2026-06-18)
- ✅ **骨架**:四层(board/projects/shared,tasks 嵌套在 project 内)+ `shared/routing` 控制平面 + `shared/capability_registry` 能力库 + `knowledge` 知识库 + `shared/reference` 监管误区库 + `.gitignore`。
- ✅ **能力转化**:Hermes 登记为默认对话 runner;2 个核心模块已转入能力库;41 个旧 .codex skill + 其余 modules **登记为 pending**(新机 .codex 是全新装,实体待从旧机/备份拷)。
- ✅ **知识库**:迁移记录/快照归档进 `knowledge/corpus/` + 7 篇 wiki 提炼笔记;`kb.sqlite` **未建**(需 Mac 上 Ollama 跑 `knowledge/build.sh`)。
- ✅ **Gitee SSH**:key `~/.ssh/id_ed25519_gitee_yutu6` 已配(`ssh -T git@gitee.com` 通);详见 `shared/config/ssh-and-remotes.md`。
- ✅ **Hermes 已装**(默认对话 runner):clone 自 Gitee `lunar-forest-hermes`(master, e8d5efa)→ `~/.hermes/hermes-agent`;plugins/voice-wake 就位;`.env` 已还原(600);uv venv 装依赖;`hermes doctor` 通过;**未接飞书、gateway 未起**(符合"先别接")。
- 🔧 **Web 控制台 v1**:`projects/控制台/`(Node 零依赖 server + config.json runner 适配 + 单文件聊天网页),端口 **41218**,IPv4+IPv6 双栈。当前正由玉兔6 Codex 修复/验证 Codex/Claude 真集成。

## 3. 下一步(用户定的顺序)
**4 → 3 → 2**(第 1 项用户自己处理):
- **4. 把 Web 控制台跑通**(进行中):三个 runner(mock/codex/claude)都要绿;通了再接 `shared/routing` 让它按角色自动选 runner。
- **3. 拷回旧机 41 个 `.codex` skills + modules**:恢复全部自定义能力,并把 `capability_registry` 里 pending 翻成 present(**Starlaid 四项保持硬排除**)。
- **2. clone YuanXiao + Simulaid**:需先 GitHub 授权,之后 `projects/_迁移/scripts/clone-repos.sh`(Starlaid 硬排除)。
- (1. 接飞书 + 启动 gateway/LaunchAgent:**用户自行处理**飞书开放平台授权后再找总管。)

## 4. 环境与权限边界(很重要)
- 总管的沙箱**只能读写已连接的文件夹**:`玉兔6工作区`,以及已授权的 `~/.codex`、桌面 `MacMini-Secrets-PRIVATE-2026-06-18`。**够不到** `~/.hermes`、`~/Projects` 等其余 Mac 路径,**连不上 gitee.com**(网络被挡),也**跑不了 Mac 进程**。
- 因此:凡需 Mac 终端 / 登录 / 联网 / clone / 跑 CLI 的,交给**玉兔6 Codex**(在 Mac 上、有网/有 CLI)或用户手动。
- **给 Codex 派活的方式**:把整段任务写进 `projects/_迁移/转发给codex.txt`,用户用 `cb`(别名 = `pbcopy < 该文件`)复制 → 粘给玉兔6 Codex。Codex 把结果写回工作区文件(如 `projects/控制台/artifacts/*.txt`)或回报,总管再读。

## 5. 用户的规矩
- 能自动做的(建结构 / 写文件 / 脚本 / 拷文件 / 写 .env)→ 直接做。
- OAuth / 扫码 / 2FA / 各平台登录 → **只列清单,用户手动**。
- **每步简短报告;出错或拿不准就停下问用户。**大动作先给"先搭后转化"式计划再动手。
- **密钥只本机用、不外传、不贴聊天**;脚本处理密钥时不回显值。
- 旧机用户名 `yutu`(路径 `/Users/yutu/...`),新机 `yutu6`;**Starlaid 全程硬排除**。

## 6. 关键路径速查
- 密钥库:桌面 `MacMini-Secrets-PRIVATE-2026-06-18/`(`secrets.env` + `密钥清单.md` + `需手动重新连接清单.md`)。
- 还原密钥:`projects/_迁移/scripts/restore-secrets.sh`(按 `# Source` 分发回 `~/.hermes/.env` 等,chmod 600,不回显)。
- 迁移脚本:`projects/_迁移/scripts/`(restore-secrets / clone-repos / place-gitee-key / check-hermes-gitee / diag-gitee-ssh)。
- 控制台:`projects/控制台/`(`start.sh` 启动 → http://localhost:41218;`README.md` 讲怎么调 runner 命令;`diag.sh` 排错)。
- 搬家原始包:`玉兔搬家部署包/`(纸箱,长期知识已抽进 knowledge/ 与 shared/ 后可归档)。

## 7. 一句话定位
骨架/能力/知识/监管/Gitee/Hermes 都已就位;**当前焦点 = 修通 Web 控制台**,然后按 3→2 补能力与项目仓库。接飞书上线由用户主导。
