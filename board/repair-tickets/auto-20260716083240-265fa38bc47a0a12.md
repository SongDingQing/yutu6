# 维修工单 auto-20260716083240-265fa38bc47a0a12 · 自动维修 supervisor-控制台/b595375a

- status: done
- created_at: 2026-07-16T08:32:40.337Z
- source: 自动故障触发
- priority: high


## 问题
队列任务失败,系统自动开维修工单。失败原因:done_gate.review_verification: review.verification.acceptance_table 第3行 使用不可核声明作证据: 任务验收: 混合标签按既定优先级得到可预测结果，并覆盖每一级优先级翻转测试；

## 事件证据 / 路径
- queueAgent=supervisor-控制台
- queueId=b595375a
- task=cr-1784189981696-b595375a
- role=supervisor
- flow=review-loop
- project=控制台
- reason=done_gate.review_verification: review.verification.acceptance_table 第3行 使用不可核声明作证据: 任务验收: 混合标签按既定优先级得到可预测结果，并覆盖每一级优先级翻转测试；
- engine_code=3
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1784189981696-b595375a

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Codex 特权),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260716083240-265fa38bc47a0a12.md)"
```

## 处理结果
- status: done

### 维修主管初判(2026-07-16)
- 链路证据: `ceo/4a14d243` 于 `engine-events.jsonl:seq=275209-275210` 把完整目标路由给 `supervisor-控制台/b595375a`;本轮 `implement-1` 声明完成,系统随后于 `seq=285834` 启动独立 review,review 以 `pass=false` 确定性复现两种视觉分类降级;最终 `seq=286181` 失败收口并自动建本单。
- 需求传递判断: CEO 原始目标、董事优先级修订、九条任务验收与非视觉 N/A 行均出现在 `engine-runs/cr-1784189981696-b595375a/task.md:6-45` 和 implement/review 任务中,无交接文字遗漏。丢失发生在代码数据契约:producer 未持久化首次分类输入,runner 二次分类覆盖首次审计。
- 严重度: 严重。涉及公共 `engine/runner/done gate` 契约,可影响所有视觉任务并绕过真实 Peekaboo/Codex 证据硬门。
- 根因: `projects/控制台/engine-runner.js:611-616` 对已结构化 `acceptance` 无条件重新分类;`projects/控制台/ceo-worker.js:1851-1884` 的 spec 未完整保留 `taskType/task_type/changeType/category` 等首次输入。review 已证明“显式 Peekaboo 要求仅在 acceptance”和“非视觉 goal + taskType=ui”均会从 `pending_visual_evidence` 降为 `not_applicable`。
- done gate 判断: 本单标题所述“第3行使用不可核声明作证据”是 review 表证据格式的正常拒绝,不应放宽 done gate;真实需修的是其下层 producer→runner 分类契约。
- 派工范围: 仅修复分类审计在 `makeSpec→makeCtx` 的单调传递(允许 human gate/新 changed_files 升级,禁止无依据降级),并补两条生产链集成回归;不放宽真实图片证据门,不碰无关脏工作树。

### 主管接管修复与复核(2026-07-16)
- 派工回执: 首次 `repair/3be9f7a7` 因未传 `--repair-ticket-id` 被作用域门以 `invalid_scope_schema` 回退为 `ceo/069ae44e`;已取消该错路队列,不当作维修成功。重新签发的 `repair/3b0f2b97` 于 `engine-events.jsonl:seq=286420` 通过 `trusted_repair_ticket_scope`,但因 repair-lead/repair 共用 `codex-privileged` 单飞锁,在 `seq=286422` 停于 pre-engine lock wait。
- 自锁处置: 参照同域已审计先例,于 `seq=286559` 向 `repair/3b0f2b97` 写入 mandatory read-only/no-op steer;子单 `enginePid=null`,未执行写码。主管接管两文件最小修复,避免释放锁后重复落码。
- 修复: `projects/控制台/engine-runner.js:611-659` 新增任务信封视觉审计合法性检查与单调合并。合法 producer 审计作为冻结基线;runner 重算只能以更高优先级 human gate/变更路径升级,不能丢失显式要求或 taskType 信号。
- 护栏: `tests/visual-acceptance.test.js:378-414` 新增三条生产契约回归:显式 Peekaboo 要求只在 acceptance、非视觉 goal + `taskType=ui`、冻结 N/A + runner-time `changedFiles` 升级。
- 复核: `node tests/visual-acceptance.test.js` exit 0;`node tests/done-gate.test.js` exit 0;`node tests/cli-runner.test.js` exit 0;`node tests/handoff-shadow.test.js` exit 0;`node tests/project-routing.test.js` exit 0;两种生产降级探针均为 `pending_visual_evidence→pending_visual_evidence`;`node --check` 两文件与限定 `git diff --check` 均 exit 0。
- 广门诊断: `node tests/run.js --profile lean` 中本修复相关项全部通过;唯一失败为既有范围外 `tests/hardening-hooks.test.js:142`,其 fixture 声明 changed file 但脏工作树下无该文件 diff,被 changed-files 真实性门先拦;与 reviewer 原基线一致,不作本修复通过证据。
- 架构判断: 分类审计跨 producer/runner 丢语义是系统性契约问题,已在消费端加单调守卫;另有 `repair-lead/repair` 共用 runner 导致同步派工自锁的独立架构风险,本单仅用 no-op steer 防重复执行,不手工删锁/并发绕锁。
- 知识沉淀候选: 可泛化模式。问题模式=`producer` 已写入版本化分类审计,下游却从派生表格反推原始需求;根因=派生文本不再保留原始结构信号;解法=信任且校验冻结审计,仅允许下游单调升级,并补 producer→consumer 集成回归。
- 回滚点: 只需撤销 `projects/控制台/engine-runner.js:611-659` 和 `tests/visual-acceptance.test.js:378-414` 的本次局部增量,不得覆盖两文件其他已有改动。

| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 完成 | projects/控制台/artifacts/engine-events.jsonl:87894; projects/控制台/artifacts/engine-events.jsonl:87896; projects/控制台/artifacts/engine-events.jsonl:88033; projects/控制台/engine-runner.js:611; tests/visual-acceptance.test.js:378; node tests/visual-acceptance.test.js exit 0; node tests/done-gate.test.js exit 0; node tests/cli-runner.test.js exit 0; node tests/handoff-shadow.test.js exit 0; node tests/project-routing.test.js exit 0 | 已核查、分级、签名派工并处置 runner 自锁,完成最小修复与独立重跑;结案命令负责 status/HTML/飞书/元宵回执。 |
| 视觉/UI证据: not_applicable | not_applicable | task-envelope:visual_acceptance | source=task_type; no positive visual requirement after explicit/human-gate/path/task-type evaluation |

## 结案协议
- 本维修请求单独建单,不同故障不得混入本单。
- 完成记录必须包含:链路证据、需求传递判断、严重度、根因、处理过程、复核验证、架构判断、知识沉淀候选、剩余风险 / 下一步。
- 使用 `repair-ticket-complete` 结案;系统生成固定 HTML,飞书发送摘要卡 + 附件,元宵同步报告文档。
- 收口握手启用后,`repair-ticket-complete` 遇到 running repair 子任务会返回 `closing_pending_child`;子任务进入终态后用 `repair-ticket-child-confirm --mode read-only-no-op-confirmed` 确认,再重试结案。
- TTL 超时只生成签名主人决策卡;`force-read-only` 必须消费持久化拍板回执,并保留 unconfirmed child warning。

### 完成记录 2026-07-16T08:42:14.323Z
- status: done
- report: projects/控制台/artifacts/repair-reports/auto-20260716083240-265fa38bc47a0a12.html
- report_sha256: f6a96251528dad99231bedaab828219949865fb5c9f24398301123ffb66235f4

链路证据：ceo/4a14d243 于 projects/控制台/artifacts/engine-events.jsonl:77212-77213 将原始目标及董事修订路由给 supervisor-控制台/b595375a；handoff 元数据见 projects/控制台/artifacts/engine-runs/cr-1784189981696-b595375a/meta.json:3-9，完整九条任务验收与 N/A 行见同 run/task.md:30-45。implement-1 自报完成后，独立 review-2 于 interaction-trace.json:60-67 返回 pass=false，确定性复现显式 acceptance 与 taskType 两种 pending_visual_evidence→not_applicable 降级；engine-events.jsonl:87829-87835 记录 review 调用、队列失败与通知。需求传递判断：CEO→supervisor→implement→review 的目标、优先级、正反测试要求均完整，无文字交接遗漏；语义丢失发生在代码契约，producer 已生成 visual_acceptance 审计，但 runner 从结构化 acceptance 反推原始输入并覆盖审计。严重度：严重。公共 engine/runner/done-gate 契约可影响所有视觉任务并绕过真实 Peekaboo/Codex 证据硬门，存在假完成风险。根因：projects/控制台/engine-runner.js 原 611-616 无条件调用 classifyVisualAcceptance；结构化 v2 表不再保留原始显式 acceptance 和 taskType 标签，而 projects/控制台/ceo-worker.js 已写入的版本化 visual_acceptance 审计未被消费。工单标题中的 review 第3行不可核声明是 done gate 正常拒绝，不应放宽；真实故障是其下层 producer→runner 分类降级。处理过程：首次派 repair/3be9f7a7 因漏传 repairTicketId 被作用域门 fail-closed 回退 CEO，已取消错误回退 ceo/069ae44e；随后签名派 repair/3b0f2b97，engine-events.jsonl:87894 证明 trusted scope，但因 repair-lead/repair 共用 codex-privileged runner 在:87896 自锁等待。已于:88033 注入 mandatory read-only/no-op steer且子单 enginePid=null，防止主管释放锁后重复落码。主管接管最小修复：projects/控制台/engine-runner.js:611-659 校验冻结审计并只允许更高优先级 runtime 信号升级，不允许无依据降级；tests/visual-acceptance.test.js:378-414 增加显式 acceptance、taskType=ui 和 changedFiles 单调升级三条生产链回归；未放宽真实图片硬门。复核验证：node tests/visual-acceptance.test.js exit 0；node tests/done-gate.test.js exit 0；node tests/cli-runner.test.js exit 0；node tests/handoff-shadow.test.js exit 0；node tests/project-routing.test.js exit 0；两种 reviewer 生产探针均得到 pending_visual_evidence→pending_visual_evidence；node --check projects/控制台/engine-runner.js 与 tests/visual-acceptance.test.js exit 0；限定 git diff --check exit 0。node tests/run.js --profile lean 的相关项通过，唯一非零为既有范围外 tests/hardening-hooks.test.js:142 changed_files fixture 基线问题，与原 review 记录一致，不包装成通过证据。架构判断：系统性 producer/consumer 派生数据契约缺口已用冻结审计+单调升级守卫修复；另有 repair-lead/repair 共用 runner 造成同步派工自锁的独立架构风险，本单按既有 no-op steer 先安全收口，不手工删锁或并发绕锁，后续应评估独立 runner 或异步续核。知识沉淀候选：泛化判断=可泛化模式；问题模式=上游持久化版本化决策审计、下游却从派生文本重算原始需求；根因=派生文本丢失结构信号且 consumer 无单调性约束；解法=验证并冻结上游审计，只允许下游以更强信号升级，同时补 producer→consumer 集成回归。项目技术映射=控制台 → visual-acceptance@1/structured-acceptance@2 producer-consumer 契约 → projects/控制台/engine-runner.js、tests/visual-acceptance.test.js。回滚点：仅撤销 engine-runner.js:611-659 与 visual-acceptance.test.js:378-414 的本次局部增量，不覆盖两文件其他既有改动。剩余风险 / 下一步：repair/3b0f2b97 将在主管释放锁后按 mandatory steer 只读 no-op；lean 的 hardening-hooks 范围外基线失败仍待其所属任务处理；本修复未重启常驻服务，新进程加载后生效。\n\n| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |\n|---|---|---|---|\n| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 完成 | board/repair-tickets/auto-20260716083240-265fa38bc47a0a12.md:3; projects/控制台/artifacts/engine-events.jsonl:87894; projects/控制台/artifacts/engine-events.jsonl:87896; projects/控制台/artifacts/engine-events.jsonl:88033; projects/控制台/engine-runner.js:611; tests/visual-acceptance.test.js:378; node tests/visual-acceptance.test.js exit 0; node tests/done-gate.test.js exit 0; node tests/cli-runner.test.js exit 0; node tests/handoff-shadow.test.js exit 0; node tests/project-routing.test.js exit 0 | 已完成链路核查、严重度分级、签名派工及 runner 自锁处置、最小修复、独立重跑与唯一命令结案；本命令负责 HTML、飞书与元宵投递回执。 |\n| 视觉/UI证据: not_applicable | not_applicable | task-envelope:visual_acceptance | source=task_type; no positive visual requirement after explicit/human-gate/path/task-type evaluation |

### 完成记录 2026-07-16T08:48:05.233Z
- status: done
- report: projects/控制台/artifacts/repair-reports/auto-20260716083240-265fa38bc47a0a12.html
- report_sha256: a7e8239d253350c83a55c734c6a11a886931ba54b3a0681a008a2396ad905610

## 链路证据
CEO 根任务 ceo/4a14d243 于 projects/控制台/artifacts/engine-events.jsonl:76679-76680 路由到 supervisor-控制台/b595375a；projects/控制台/artifacts/engine-runs/cr-1784189981696-b595375a/meta.json:3-9 记录 ceo→supervisor 交接，task.md:6-45 保留原始目标、董事修订、九条验收和非视觉 N/A 行。implement-1 的 interaction-trace.json 记录 ceo→worker_code→supervisor-控制台 且 status=completed；review-2/interaction-trace.json:60-67 记录独立 review 输出 pass=false，review-2/result.redacted.md:2 确定性复现显式 acceptance 与 taskType=ui 两种 pending_visual_evidence→not_applicable 降级。engine-events.jsonl:87655-87657 记录 supervisor 失败、自动建单并入 repair-lead。

## 需求传递判断
CEO→supervisor→implement→review 的目标、优先级、正反测试与硬门边界均完整，无文字交接遗漏或跳过。需求语义丢失发生在代码数据契约：producer 已持久化 visual_acceptance 审计，runner 却从结构化验收表反推原始输入并覆盖首次审计。

## 严重度
严重。涉及公共 engine/runner/done-gate 契约，可影响所有视觉任务并绕过真实 Peekaboo/Codex 证据硬门，存在跨任务假完成风险；repair-policy-check 亦判定 severe/global_system_trace。

## 根因
原 projects/控制台/engine-runner.js:611-616 对已结构化 acceptance 无条件重分类；structured-acceptance@2 表不再保存原始显式 acceptance 和 taskType 标签，而 ceo-worker 生成的版本化 visual_acceptance 审计未被消费。工单标题所述“第3行使用不可核声明作证据”是 done gate 的正确失败关闭，不应放宽；真实故障位于 producer→runner 分类契约。

## 处理过程
首次 repair/3be9f7a7 因未携带 repairTicketId 被作用域门以 invalid_scope_schema 失败关闭并回退 ceo/069ae44e，错误回退已取消；随后重新签发 repair/3b0f2b97，engine-events.jsonl:87894 证明 trusted repair ticket scope。该子单因 repair-lead 与 repair 共用 codex-privileged 单飞锁在 engine-events.jsonl:87896 等待，enginePid 为空；engine-events.jsonl:88033 已持久化 mandatory read-only/no-op steer，禁止释放锁后重复写码。维修主管接管最小修复：projects/控制台/engine-runner.js:611-659 校验冻结审计并仅允许更高优先级 runtime 信号升级；tests/visual-acceptance.test.js:378-414 增加显式 acceptance、taskType=ui、changedFiles 升级三条生产链回归；未放宽真实图片证据门。另发现首份 HTML 因结案参数使用字面量换行而缺 7 个结构段，本次仅经 repair-ticket-complete 入口用真实换行重建，不手改报告。

## 复核验证
独立实跑 node tests/visual-acceptance.test.js、node tests/done-gate.test.js、node tests/cli-runner.test.js、node tests/handoff-shadow.test.js、node tests/project-routing.test.js，均 exit 0；两条原故障生产探针均为 pending_visual_evidence→pending_visual_evidence；node --check projects/控制台/engine-runner.js 与 tests/visual-acceptance.test.js、限定 git diff --check 均 exit 0。node tests/run.js --profile lean exit 1，唯一失败仍为既有范围外 tests/hardening-hooks.test.js:142 fixture 声明 changed_files 但无对应 diff，被真实性门先拦；不作为本修复通过证据。

## 架构判断
分类审计跨 producer/consumer 丢语义是系统性契约缺口，已用冻结审计、合法性校验、单调升级和集成回归修复。另有独立系统风险：projects/控制台/config.json:74-88 将 repair-lead 与 repair 都绑定 codex-privileged，导致主管同步派工自锁；本单用持久化 no-op steer 安全收口，后续应评估独立 runner/slot 或异步续核，不能靠手工删锁。

## 知识沉淀候选
泛化判断=可泛化模式。问题模式=上游持久化版本化决策审计，下游又从派生文本重算原始需求。根因=派生文本丢失结构信号且 consumer 无单调性约束。解法=校验并冻结上游审计，仅允许下游以更强信号升级，并补 producer→consumer 集成回归。项目技术映射=控制台视觉验收分类契约→projects/控制台/engine-runner.js 与 tests/visual-acceptance.test.js。

## 剩余风险 / 下一步
repair/3b0f2b97 当前仍在 pre-engine lock wait，已持久化只读 no-op steer且未启动 engine；主管释放单飞锁后只允许返回只读核验回执。lean 的 hardening-hooks 基线失败归其所属任务处理。常驻服务未重启，新进程加载后生效。

| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 完成 | board/repair-tickets/auto-20260716083240-265fa38bc47a0a12.md:3; projects/控制台/artifacts/engine-events.jsonl:76679; projects/控制台/artifacts/engine-events.jsonl:87655; projects/控制台/artifacts/engine-events.jsonl:87894; projects/控制台/artifacts/engine-events.jsonl:88033; projects/控制台/artifacts/engine-events.jsonl:88303; projects/控制台/engine-runner.js:611; tests/visual-acceptance.test.js:378; node tests/visual-acceptance.test.js exit 0; node tests/done-gate.test.js exit 0; node tests/cli-runner.test.js exit 0; node tests/handoff-shadow.test.js exit 0; node tests/project-routing.test.js exit 0 | 已核查、分级、签名派工并处置 runner 自锁，完成最小修复、独立复核和唯一入口结案；飞书已有成功回执，本次结案继续幂等尝试飞书/元宵。 |
| 视觉/UI证据: not_applicable | not_applicable | task-envelope:visual_acceptance | source=task_type; no positive visual requirement after explicit/human-gate/path/task-type evaluation |
