# 维修工单 auto-20260714085254-fb6ba867125a78f0 · 自动维修 supervisor-控制台/397d53ad
- status: done
- created_at: 2026-07-14T08:52:54.591Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:done_gate.logic_chain: implementation.acceptance_table 第1行 未完成: 任务验收: 在 控制台 项目 scope 内跑 review-loop; 完成前更新 projects/控制台/status.md。

## 事件证据 / 路径
- queueAgent=supervisor-控制台
- queueId=397d53ad
- task=cr-1784018284182-397d53ad
- role=supervisor
- flow=review-loop
- project=控制台
- reason=done_gate.logic_chain: implementation.acceptance_table 第1行 未完成: 任务验收: 在 控制台 项目 scope 内跑 review-loop; 完成前更新 projects/控制台/status.md。
- engine_code=3
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1784018284182-397d53ad

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Codex 特权),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260714085254-fb6ba867125a78f0.md)"
```

## 处理结果
- status: done

## 维修主管初查与派工 brief 2026-07-14

### 链路证据
- 公告板任务进入 CEO 根队列 `ceo/34ff7914`，原始目标、边界和验收为“任务有事件日志可追踪；产物路径清楚；不需要视觉时无需截图”，见 `projects/控制台/artifacts/queues/ceo/failed/34ff7914.json:4-12`。根任务 `cr-1784014332008-34ff7914` 经董事会后由 orchestrator 产出 10 条可执行验收，其中第 9 条明确“视觉/UI证据：NA——纯引擎、hook、兼容设计与测试任务无视觉面”，见 `projects/控制台/artifacts/engine-runs/cr-1784014332008-34ff7914/orchestrator-plan-1/result.md:2`。
- CEO 于 `projects/控制台/artifacts/engine-events.jsonl` seq `246355-246360` 把任务派到 `supervisor-控制台/397d53ad`；完整 revised goal/root-parent id 保存在 `projects/控制台/artifacts/queues/supervisor-控制台/failed/397d53ad.json:4-28`，但子任务结构化验收只剩“跑 review-loop/更新 status”及自动视觉行，见同文件 `:16-21`。
- 同一 queueId 连续运行三次：`cr-1784014494477-397d53ad` 在 seq `249833-249835` 因非 UI 的视觉 NA 行缺真实图失败；`cr-1784017768825-397d53ad` 在 seq `250254-250256` 因同一视觉行失败；`cr-1784018284182-397d53ad` 在 seq `250922-250924` 因执行者如实把“完整 review-loop 尚未 review”标为部分而在 implement 阶段失败。三次都未进入真实 review 节点，最终 seq `250932-250954` 使 supervisor 与 CEO 根任务失败并自动开本工单。
- 最后一轮 `worker_code` 实际完成 AHR-26..30 准备包、专项测试和 status 更新，并明确未切换生产 blocking hook；但其结果正确声明整体 review 尚未通过，见 `projects/控制台/artifacts/engine-runs/cr-1784018284182-397d53ad/implement-1/result.md:2`。

### 需求传递判断
- 原始目标、边界、董事会修订和 root/parent id 从 CEO 传到 supervisor，未发生项目串单或目标丢失。
- 存在两处公共交接遗漏/传错：① `shared/engine/done-gate.js` 的自动视觉判定把“不需要视觉时无需截图”“视觉/UI证据：NA”“本任务无 UI”等否定/元讨论当成正向视觉需求，根任务和 supervisor 子任务都被错误注入视觉行；② orchestrator 已输出的 10 条验收未进入 supervisor `task.acceptance`，`projects/控制台/engine-runner.js:1338-1345` 只保留 projectId 与原始结果摘要，`routeBriefToSupervisor` 在 `:1174-1180` 又用通用 review-loop 文案重建结构化验收。执行者只能从 goal/brief 猜验收，done gate 却只认被缩写后的表。

### 严重度 / 根因 / 架构风险
- 严重。涉及公共 project-route、review-loop、结构化验收生成和 done gate；同一 queue 已连续三轮失败，向上拖垮 CEO 根任务，并触发自动维修。影响所有含“无需截图/非 UI/视觉 NA”说明或依赖 orchestrator 细化验收的任务，存在误阻断、重复重试、假完成压力和需求静默丢失风险。
- 根因一：`shared/engine/done-gate.js:914-946` 的 `buildStructuredAcceptanceTable()` 对 `goal + acceptance` 直接执行宽泛 `VISUAL_UI_RE.test(text)`；正则没有语义极性/否定边界，因此否定性说明也被转成强制视觉行。已有显式视觉行的严格图片/Codex门应保留，问题只在“自动生成”阶段。
- 根因二：`projects/控制台/engine-runner.js:1328-1345` 没有结构化保存 `orchestrator.acceptance`；`routeBriefToSupervisor()` 在 `:1174-1180` 不消费 orchestrator 验收，只生成通用任务行。硬验收从 orchestrator → supervisor 的交接被跨过。
- 根因三：通用行“在项目 scope 内跑 review-loop”在 implement 返回时就被 `shared/engine/engine.js:404-417` 校验为必须完成，文案却容易被理解为未来 review 也已完成，形成阶段自依赖/诚实回报被罚。应改为 implement 阶段可完成的交付语义，并明确 review 由系统随后执行。

### repair 执行范围与验收
- 先读本工单和当前 `git diff`；目标文件已有其他未提交改动，必须保留无关 hunk，不得覆盖、回退或格式化重写。
- 最小修复自动视觉判定：否定/NA/纯引擎元讨论不得自动生成视觉行；真实 UI/页面/布局/样式/图片等任务仍自动生成视觉行；一旦任务验收已显式包含视觉行，现有 Peekaboo + Codex + review-owned trace/SHA/逐图观察门继续严格 fail-closed，禁止通过 NA 绕过。
- 修复 orchestrator → supervisor 验收传递：结构化读取并保存 `orchestrator.acceptance`，让 supervisor 子任务验收包含这些具体条目；不要只靠截断的 raw result/brief 摘要。保留项目 scope/status 更新要求，但把其文案改为 implement 阶段可完成、review 由系统后续执行，避免要求当前节点证明未来节点。
- 补正反回归：①默认“事件日志可追踪；产物路径清楚；不需要视觉时无需截图”不产生视觉行；②含“视觉/UI证据：NA/本任务无 UI”的非 UI 引擎任务不产生视觉行；③真实 UI 任务仍产生视觉行且缺图被拒；④ orchestrator 具体 acceptance 逐条进入 supervisor 子任务结构化表；⑤子任务行不要求 implement 伪称 review 已完成。
- 至少运行 `node --check shared/engine/done-gate.js`、`node --check projects/控制台/engine-runner.js`、`node tests/done-gate.test.js`、`node tests/project-routing.test.js`，并用当前根任务 goal/acceptance 做只读构建重放，证明不再生成视觉行且具体 orchestrator 验收被保留。相关相邻测试能安全运行则补跑；全量既有红灯必须分账。
- 禁止修改 `projects/控制台/artifacts/queues/**` 历史状态、原 engine-run 结果、AHR-26..30 业务产物或生产 hook 配置；不得自行结案、重启服务、杀进程或重新派发原业务任务。高危/不可逆操作停下给主人确认；密钥不回显。完成后在 repair 队列结果或本工单写清 changed files、diff、测试退出码、原任务是否需另行重跑和残余风险，等待维修主管独立复核。

### 结构化验收表（主管初查）

| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 部分 | `projects/控制台/artifacts/queues/ceo/failed/34ff7914.json:4`; `projects/控制台/artifacts/queues/supervisor-控制台/failed/397d53ad.json:4`; `projects/控制台/artifacts/engine-runs/cr-1784018284182-397d53ad/implement-1/result.md:2`; `shared/engine/done-gate.js:914`; `projects/控制台/engine-runner.js:1174` | 已完成全链路核查、需求遗漏判断、严重度和派工范围；待 repair 执行、主管独立复核及唯一入口结案。 |

## 结案协议
- 本维修请求单独建单,不同故障不得混入本单。
- 完成记录必须包含:链路证据、需求传递判断、严重度、根因、处理过程、复核验证、架构判断、知识沉淀候选、剩余风险 / 下一步。
- 使用 `repair-ticket-complete` 结案;系统生成固定 HTML,飞书发送摘要卡 + 附件,元宵同步报告文档。

### 完成记录 2026-07-14T09:53:13.470Z
- status: done
- report: projects/控制台/artifacts/repair-reports/auto-20260714085254-fb6ba867125a78f0.html
- report_sha256: 1b8ca2482b5fc35e7e4aa7ce5a30968929d54581005ca17764a94acae37806d7

链路证据:
- CEO 根队列 `ceo/34ff7914` 保存主人原始目标、边界和默认非视觉验收；orchestrator 将其细化为 10 条验收，含“视觉/UI证据：NA——纯引擎、hook、兼容设计与测试无 UI 面”，证据：projects/控制台/artifacts/queues/ceo/failed/34ff7914.json:4、projects/控制台/artifacts/engine-runs/cr-1784014332008-34ff7914/orchestrator-plan-1/result.md:2。
- CEO 在 projects/控制台/artifacts/engine-events.jsonl 的 seq 246355-246360 将任务派给 `supervisor-控制台/397d53ad`；supervisor 信封保留 root/parent id，但旧 acceptance 仅剩通用 review-loop/status 行并被自动注入视觉行，证据：projects/控制台/artifacts/queues/supervisor-控制台/failed/397d53ad.json:4-28。
- 同一 supervisor queue 三次失败：seq 249833-249835、250254-250256 均被错误视觉门拦截；seq 250922-250924 因 implement 如实把尚未发生的 review 标为部分而被拦；seq 250932-250954 令子任务与 CEO 根任务失败并自动开本单。最后一次 implement 已完成 AHR-26..30 准备包但未伪称 review 完成，证据：projects/控制台/artifacts/engine-runs/cr-1784018284182-397d53ad/implement-1/result.md:2。
- 严重故障先派 `repair/74298708`；首轮只读复核发现“视觉NA/视觉 NA”和编号无损解析残余后，再派 `repair/ae57db3e`。因 repair-lead 与 repair 共用单飞槽，按维修提示词由独立 repair 手动接管第二轮；正式队列项已有防重复 steer，普通 cancel 被控制面以“running 队列项只读”拒绝，未绕过安全约束。

需求传递判断:
原始目标、边界、项目和 root/parent 标识未串单；存在公共交接遗漏。其一，自动视觉判定把“不需要视觉、视觉 NA、本任务无 UI”等否定/元讨论当正向需求。其二，orchestrator 的 10 条结构化验收没有进入 supervisor task.acceptance，routeBriefToSupervisor 以通用行重建信封，导致具体验收静默丢失。其三，通用行要求 implement 节点证明整个 review-loop 已完成，形成阶段自依赖，诚实回报反而失败。

严重度:
严重。故障位于公共 done gate、project-route、runner 与 review-loop 交界，已在同一队列连续复发三次并拖垮 CEO 根任务；可影响所有含非视觉说明或依赖 orchestrator 细化验收的任务，具有误阻断、重复重试、假完成压力和需求静默丢失风险。

根因:
1. shared/engine/done-gate.js 旧逻辑对 goal+acceptance 执行宽泛视觉词面匹配，没有否定极性；首轮正则又要求冒号，遗漏“视觉NA/视觉 NA”。
2. splitAcceptanceItems 旧编号解析可在任意数字点附近切分并对长编号项再按分号拆分，破坏 AHR-26..30、timeoutMs=100、IP、浮点与 key=数字等正文。
3. projects/控制台/engine-runner.js 旧 runOrchestratorPlan 未结构化保存 orchestrator.acceptance，routeBriefToSupervisor 不消费该字段，只生成通用 review-loop 行。
4. 旧通用验收把后续 review 当成 implement 当前阶段的前置完成条件。

处理过程:
- shared/engine/done-gate.js:26、887-905：增加视觉否定/NA 归一化，识别无冒号 NA/N/A/不适用；同句存在真实 UI 修改动作时仍判正向。真实 UI 自动视觉行与已显式视觉行继续严格要求 Peekaboo+Codex 证据。
- shared/engine/done-gate.js:812-841：编号仅在明确条目边界切分；编号项不再按分号二次切碎，保护 AHR、timeout、IP、浮点和 key 值。
- projects/控制台/engine-runner.js:1140-1164、1347-1354、1369-1374：结构化读取和保存 orchestrator.acceptance，并传入 supervisor；追加 implement 可诚实完成的交付/status 行，明确 review 由系统随后单独执行。
- tests/done-gate.test.js:259-340 与 tests/project-routing.test.js:38-54、385-405：补视觉正反例、显式视觉 fail-closed、数字正文保真，以及历史真实 10 条 acceptance 的 10+1 无损重放。
- 未改历史 queue/engine-run 结果、AHR-26..30 业务产物或生产 hook；未重启、杀进程、重派原业务任务。

复核验证:
- repair-lead 独立审阅实际文件与 scoped diff；四目标文件 `git diff --check` exit 0。
- `node --check shared/engine/done-gate.js` exit 0；`node --check projects/控制台/engine-runner.js` exit 0。
- `node tests/done-gate.test.js` exit 0，输出 `{"pass":true,"suite":"done-gate"}`。
- `node tests/project-routing.test.js` exit 0，输出 `{"pass":true,"suite":"project-routing"}`。
- 从真实 ceo/34ff7914、supervisor/397d53ad 和 orchestrator result.md 做只读内存重放 exit 0：根表 1 行且无自动视觉行；子表 11 行；历史 10 条逐条原文相等；implement 行保留；AHR-26..30 与 timeoutMs=100 可检索；无自动视觉行。
- 首次组合语法检查因复核脚本传入字面 `\\n` 导致路径拼接错误，已分成两条独立命令重跑并均 exit 0；该审计脚本错误未归为产品红灯。

架构判断:
系统性、可泛化问题，已修公共机制而非只改当前队列。视觉门应持续保持“先做语义极性归一化，再判定正向工作”；结构化验收应采用明确边界语法，并以真实历史信封做无损契约测试。现有 splitAcceptanceItems 最多 16 项及英文同一行无明确句末分隔的编号写法仍是既有限制，超过范围时应另开评估，不在本单扩面。

知识沉淀候选:
泛化判断：可泛化模式。
问题模式：宽泛关键词或编号正则把自然语言中的否定视觉语义、版本范围和数值配置误当控制语法，同时中间路由用摘要重建验收导致硬约束丢失。
根因：语义极性与词面匹配混用；编号切分缺明确边界；orchestrator 结构化字段未贯穿 supervisor 信封；测试 fixture 过于简单。
解法：否定语义归一化 + 明确编号边界 + 结构化字段端到端传递 + 真实历史信封 10+1 无损重放；保留显式视觉门 fail-closed。
项目技术映射：控制台 → Node.js 公共 done gate/project-route/review-loop → shared/engine/done-gate.js、projects/控制台/engine-runner.js。

剩余风险 / 下一步:
- 原 AHR-26..30 业务任务未在本单重派，符合红线；应由正常控制面复用现有业务产物进入 supervisor review/验收，禁止改写历史 queue 或手工重派旧 queueId。
- `repair/ae57db3e` 仍为健康 pre-engine-lock-wait，enginePid=null；普通取消被 running 只读守卫拒绝，且已有 steer 要求工单 done/补丁存在时只读核对。当前 repair-lead 释放单飞槽后由队列 worker 自行收口，不应强制终态或清进程。
- dirty worktree 含大量他人改动；后续提交必须只挑选四个目标文件的本单 hunk，不得整体格式化或覆盖。

结构化验收表:
| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 完成 | board/repair-tickets/auto-20260714085254-fb6ba867125a78f0.md:2; projects/控制台/artifacts/repair-reports/auto-20260714085254-fb6ba867125a78f0.html; projects/控制台/artifacts/engine-events.jsonl | 链路、派工和独立复核证据已列；本命令原子写入 done、生成固定 HTML，并执行默认飞书/元宵投递尝试。 |
