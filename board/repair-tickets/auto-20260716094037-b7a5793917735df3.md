# 维修工单 auto-20260716094037-b7a5793917735df3 · 自动维修 ceo/1bf82f42

- status: done
- created_at: 2026-07-16T09:40:37.153Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:queue worker crashed: resource lock wait timeout after 1800000ms

## 事件证据 / 路径
- queueAgent=ceo
- queueId=1bf82f42
- task=worker-crash-1784194837149-1bf82f42
- role=orchestrator
- flow=worker-crash
- project=控制台
- reason=queue worker crashed: resource lock wait timeout after 1800000ms
- engine_code=unknown
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/worker-crash-1784194837149-1bf82f42

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Codex 特权),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260716094037-b7a5793917735df3.md)"
```

## 处理结果
- status: done

### 维修主管初判与派工范围(2026-07-16)
- 链路证据: 质量运营公告板任务完整进入 `ceo/1bf82f42`;失败队列项 `projects/控制台/artifacts/queues/ceo/failed/1bf82f42.json` 保存原始 goal、边界、验收和 `taskId=cr-1784193036612-1bf82f42`。该项 09:10:36 被领取后未生成 engine-run,而是以 write[`queue-state`,`brief-status`] 等待资源锁;`projects/控制台/artifacts/resource-locks/arbitration.jsonl:19710` 记录首次冲突,`:21621` 记录 30 分钟超时。`projects/控制台/artifacts/engine-events.jsonl:628-634` 随后记录 `resource.lock.timeout -> worker-crash` 失败包装、自动建单和 repair-lead 入队。
- 实际阻塞: 同期 `supervisor-控制台/8e694fd9` 持有 `queue-state`。仲裁统计显示本等待者写入 1909 条 wait、1 条 timeout;阻塞 token 从 `bafdb68f8b88abbf`(task `cr-1784191963030-8e694fd9`)切换为 `4708cbff5a8d682c`(同 queueId 的下一 task `cr-1784194540567-8e694fd9`),旧等待者在持锁任务重试换 token 时仍未获授。
- 需求传递判断: 无老板需求遗漏、缩写、跳过或传错。CEO brief、边界和验收完整落在失败队列项;故障发生在 engine 启动前,不存在下游返回内容。
- 严重度: 严重/系统性。资源锁、queue worker 和失败分类属于公共控制面;当前阻塞同时影响 CEO、supervisor 和 memory-officer。历史工单 `auto-20260705024709-80f0fc0b11091c09` 已以相同 reason fingerprint 诊断同族 30 分钟饥饿,但当时只给待拍板建议、未修公平授予,本次已证明复发。
- 根因假设: `projects/控制台/resource-locks.js` 的授予判定只检查当前 holders,waiter 仅用于环路检测而不参与先来顺序;后到任务可在锁释放/重试换 token 时抢占旧 waiter。`role=orchestrator/project-route` 又无条件持有 `queue-state`/`brief-status` 全任务写域,放大长任务阻塞面。仲裁 `wait` 每轮都落盘,本次单 waiter 产生 1909 条记录,形成额外日志膨胀。`ceo-worker` 将锁等待超时包装成 `queue worker crashed`,导致故障分类失真。
- 派工范围: 交给 `repair` 在不放宽真实资源冲突、不延长超时掩盖问题的前提下,优先实现可证明的旧 waiter 防饥饿/有界公平并补确定性竞争回归;同时评估并以最小风险处理无条件宽写域、wait 仲裁刷盘和 worker-crash 误分类。不得覆盖无关脏工作树;不得手工删活锁、杀活进程或重启服务;高危/不可逆操作先停下给主人确认;密钥不回显。

## 结案协议
- 本维修请求单独建单,不同故障不得混入本单。
- 完成记录必须包含:链路证据、需求传递判断、严重度、根因、处理过程、复核验证、架构判断、知识沉淀候选、剩余风险 / 下一步。
- 使用 `repair-ticket-complete` 结案;系统生成固定 HTML,飞书发送摘要卡 + 附件,元宵同步报告文档。

### 完成记录 2026-07-16T09:57:01.356Z
- status: done
- report: projects/控制台/artifacts/repair-reports/auto-20260716094037-b7a5793917735df3.html
- report_sha256: 8a04a76262731879d13da275c839b741c4f48cef3bd66e6117564b150d671453

链路证据:
质量运营公告板目标完整进入 ceo/1bf82f42；失败队列项 projects/控制台/artifacts/queues/ceo/failed/1bf82f42.json:4-12,20-52 保存原始 goal、边界、验收、task/root 链和 write[queue-state,brief-status]。projects/控制台/artifacts/resource-locks/arbitration.jsonl:19710 为首次等待，:21621 为 30 分钟 timeout；独立统计为 1909 条 wait、1 条 timeout，holder token 从 bafdb68f8b88abbf 切到 4708cbff5a8d682c。projects/控制台/artifacts/engine-events.jsonl:628-634 记录 resource.lock.timeout→失败终态→自动建单，:642 记录 repair-lead 入队。派工链见 :998-1004（repair/78763150 scope 消费后卡 runner-singleflight）和 :1048-1055（engine 前强制只读/no-op steer）。完整复核见 projects/控制台/artifacts/repair-verification/auto-20260716094037-b7a5793917735df3.md:5-15,31-35。

需求传递判断:
无老板需求遗漏、缩写、跳过或传错。CEO brief、边界和验收均在失败队列项中；故障发生在任何 engine node 启动前，因此没有下游内容返回或执行偏差。

严重度:
严重 / 系统性。故障涉及公共资源锁、queue worker、失败终态分类和多队列调度；历史工单 board/repair-tickets/auto-20260705024709-80f0fc0b11091c09.md:46-73 已诊断同族 30 分钟饥饿，本次为明确复发并同时影响 CEO、supervisor、memory-officer。

根因:
一、旧授予判定只检查当前 holders，waiter 只参与环路检测，后到冲突任务可在 holder 释放/同队列重试换 token 时抢过老 waiter。二、waiter 轮询重写 requested_at，无法形成稳定公平顺序。三、相同 wait 每轮落盘，本次产生 1909 条仲裁记录。四、普通 project-route 的 queue-state/brief-status 宽写域放大阻塞面。五、ceo-worker 把资源锁等待超时统一包装成 worker-crash，造成失败分类失真。

处理过程:
已在 projects/控制台/resource-locks.js:448-490,605-617,747-865 加入稳定 requested_at+token 的冲突 waiter 有界公平、旧 waiter 防抢占与 wait 审计节流，保留真实读写冲突、deadlock、stale sweep 和 privileged bypass；在 projects/控制台/ceo-worker.js:4825-4868 将锁超时单列 resource-lock-timeout/failureKind，真实异常仍为 worker-crash；在 projects/控制台/tools/resource-locks-smoke-test.js:221-307,345-363 增加确定性公平、时间戳、审计节流和失败分类回归；在 tests/resource-lock-inference.test.js:30-43 固化普通 project-route 不占 queue-state、真实队列维护仍取写锁。主管已按协议派 repair/78763150，但 repair-lead 与 repair 共用 codex-privileged 单飞槽，子任务未启动 engine；为避免释放后重复落码，已 steer 为只读/no-op，主管 failover 接管限定补丁。该互锁由 board/repair-tickets/repair-20260713-privileged-runner-singleflight.md:1-20 独立跟踪，本单未扩大修复。未删锁、杀进程、重启、发布或改密钥。

复核验证:
git diff --check 限定本单文件 exit 0；三个变更 JS 的 node --check exit 0；node projects/控制台/tools/resource-locks-smoke-test.js exit 0（pass=true, events=20）；node tests/resource-lock-inference.test.js、stale-running-heartbeat.test.js、notify-severity-tiers.test.js、project-routing.test.js、queue.test.js 全部 exit 0。node tests/run.js --profile lean exit 1，唯一失败为 tests/hardening-hooks.test.js:142 的既有错误优先级断言，已在 board/repair-tickets/auto-20260713093810-8e1f550782c2d691.md:106 记录；node tests/ceo-serial-lock.test.js exit 1，仍为 :513 的既有 1200ms event-wake 时序红灯，至少从 board/repair-tickets/auto-20260629135336-3b2cb807896962c6.md:65 持续记录。相关测试均绿，两个既有基线红灯未冒充全绿。证据汇总见 projects/控制台/artifacts/repair-verification/auto-20260716094037-b7a5793917735df3.md:37-44。

架构判断:
这是可泛化的公平锁缺陷，不是一次性配置。当前修复建立 bounded FIFO、防时间戳漂移、仲裁日志节流、资源域回归和 failureKind 分账；同类后到抢占将由确定性测试阻断。repair-lead→repair 的共享特权单飞互锁是独立 L3 架构风险，继续由既有专单跟踪。

知识沉淀候选:
泛化判断：可泛化模式。
问题模式：高竞争文件资源锁中，合法长持锁与后到抢占使旧 waiter 饥饿到 timeout，并被误报为 worker crash。
根因：授予只看 holders、不看更老冲突 waiter；公平时间戳被轮询刷新；wait 审计无节流；失败类型未分账。
解法：稳定请求时间 + 冲突 waiter 有界公平；状态变化/时间窗审计；资源域最小化回归；锁超时独立 failureKind。
项目技术映射：控制台 → 文件资源锁仲裁/有界公平 → projects/控制台/resource-locks.js；控制台 → queue worker 终态分类 → projects/控制台/ceo-worker.js。

剩余风险 / 下一步:
本轮遵守红线未重启控制台/worker；代码在相关 worker 下次正常重载后生效，如需立即热生效须主人另行批准服务重启并补 live 验证。repair-lead/repair 单飞互锁继续由 repair-20260713-privileged-runner-singleflight 跟踪；hardening-hooks 与 ceo-serial-lock 两个既有基线红灯未在本单越权修复。除此之外无本单阻塞风险。

验收表协议: structured-acceptance@2
| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 完成 | projects/控制台/artifacts/repair-verification/auto-20260716094037-b7a5793917735df3.md:5-58; projects/控制台/artifacts/engine-events.jsonl:998-1004,1048-1055; repair-ticket-complete 输出 | repair 已派出但在 engine 前被既有 singleflight 互锁，强制只读/no-op 后由主管 failover 接管；本命令原子置 done、生成报告并尝试通知。 |
| 视觉/UI证据: not_applicable | not_applicable | task-envelope:visual_acceptance | source=task_type; no positive visual requirement after explicit/human-gate/path/task-type evaluation |
