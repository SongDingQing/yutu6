# 维修工单 auto-20260710142454-69940c668bc65909 · 自动维修 supervisor-控制台/034079bb

- status: done
- created_at: 2026-07-10T14:24:54.734Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:done_gate.review_verification: review.verification.acceptance_table 第2行 未完成: 任务验收: 完成后更新 projects/控制台/status.md,并由系统增量更新 board/status-rollup.md。

## 事件证据 / 路径
- queueAgent=supervisor-控制台
- queueId=034079bb
- task=cr-1783692211429-034079bb
- role=supervisor
- flow=review-loop
- project=控制台
- reason=done_gate.review_verification: review.verification.acceptance_table 第2行 未完成: 任务验收: 完成后更新 projects/控制台/status.md,并由系统增量更新 board/status-rollup.md。
- engine_code=3
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1783692211429-034079bb

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- Starlaid 排除
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Codex 特权),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260710142454-69940c668bc65909.md)"
```

## 处理结果
- status: done

### 完成记录 2026-07-10T14:31:21.347Z
- status: done

维修主管结案(repair-lead)· 目标任务 cr-1783692211429-034079bb(supervisor-控制台/034079bb, review-loop)

【链路证据】谁派给谁/brief/期望/实际:
- ceo(dc89e529) → supervisor-控制台(034079bb),handoff meta: engine-runs/cr-1783692211429-034079bb/meta.json(from ceo,to supervisor,review-loop,workspace任务板状态假完成修复)。
- implement-1/result.md:implement 自报 done=true,诚实登记 `node tests/run.js` exit 1,并逐条列出 4 个既有非本任务红灯(office-experiment / workspace-taskboard 视图 / hardening-hooks / ceo-serial-lock)。changed_files 与专项测试 workspace-task-status-truth.test.js 均 exit 0。
- review-2/result.md:reviewer 用生产同款 executeEvidence=true 独立复核,verifyExecutableEvidence 因 `node tests/run.js` 实跑 exit 1 → 把 acceptance_table 第2行判「部分」、review.pass=false。期望返回=收口触发 rollup;实际返回=被 done gate 挡。
- done gate:validateStructuredAcceptanceTable 读 review.verification.acceptance_table,第2行非「完成」→ 打回,自动开本工单。

【需求传递判断】无需求传递遗漏/缩写/传错。CEO→主管→实现→复审 brief 完整,实现与复审均诚实报告真实退出码,没有假完成盖章。三方都按合同正确行事——这是一次「守卫按设计正确拦截」,不是链路漏传。

【严重度】严重(系统性、复发)。engine-events.jsonl 中 `证据命令实跑失败: node tests/run.js → 实际退出码 1` 与 `acceptance_table 第2行 未完成` 各出现多次(grep 命中数十条)。触及 done gate 核心机制,影响所有把全量套件当完成证据的任务。

【根因】双层:
- 直接触发:实现把聚合入口 `node tests/run.js`(全量套件,含 4 个既有红灯)写进 logic_chain.tests/evidence。done-gate 的 verifyExecutableEvidence(白名单含 /^node\s+tests\//)会重跑所有被引证据命令,全量套件 exit 1 → 硬失败(shared/engine/done-gate.js:1165-1198)。
- 系统层:玉兔6 是单一共享工作区多智能体系统。全量套件当前红是因 4 个非本任务区域正被并发任务改动(git status:office-experiment.html / workspace.html / server.js 均 M,workspace-taskboard.test.js M),这 4 红跨窗口「既有」。任一 scoped 任务若把全量套件当完成门,就被共享树的瞬时全局健康(它既不拥有也无法修)反噬。

【处理 / 是否派工】不派工、不盲修,依据充分:
- 4 个红灯归属并发任务(其源文件在工作树 M 状态),我去改会与在飞的任务撞车,属高影响面写码,红线上应先请示且非本工单目标 → 不动。
- done gate 行为正确(红树不该盖完成),放宽白名单=削弱防假完成守卫,属核心完成策略变更 → 按老板「自己拍板」偏好,只建议不擅改。
- 目标 supervisor 任务走 review-loop 自愈:重试时只引本任务 scoped exit-0 门禁 `node tests/workspace-task-status-truth.test.js`,把全量诊断挪到 remaining_risks 即可通过(reviewer improvement_points 已在环内给出),无需我代为完成该任务。

【复核验证】
- 亲跑 `node tests/run.js` → EXIT=1,4 红复现且与实现自报完全一致(office-experiment 40-tile grid / workspace 视图枚举 / hardening-hooks 期望 hook_gate_failed 实得 done_gate_failed / ceo-serial-lock 1200ms 唤醒时序)。
- 读 done-gate.js:1121-1198 确认白名单 /^node\s+tests\// 命中 run.js、非零即 ok:false;:1246-1252 确认 acceptance 门先于 execEvidence 门。
- 读 implement-1 与 review-2 result.md 全文;核 meta.json 交接信封;grep engine-events.jsonl 确认复发计数。
- git status 确认 4 红区域均处于并发 M 修改态,四个 .test.js 基线来自 d23cd29。

【架构判断】系统性,给两条改进建议(待老板拍板):
1) 完成门与仓库全局健康解耦:把聚合入口 `node tests/run.js` 移出 verifyExecutableEvidence 重跑白名单,只重跑 scoped `*.test.js`(scoped 门禁仍强制 exit 0,防假完成不削弱)。共享多智能体树天然长期部分脏/红,per-task 完成门不应依赖全局树。
2) 4 个既有红灯由各自归属任务收口归绿,或显式登记为 known-red 基线(白名单/allowlist),避免长期污染全量信号。此为调度/协同事项,非单次维修。

【知识沉淀候选】问题模式:共享树多智能体系统中,agent 把全量 `node tests/run.js` 当可执行完成证据 → 根因:done-gate 会重跑被引白名单命令,全量套件被并发任务的瞬时红灯拉挂 → 解法:完成证据只引本任务 scoped exit-0 门禁,全量诊断进 remaining_risks;机制侧考虑把聚合 runner 移出重跑白名单。

### 外部维修主管补充复核 2026-07-10T14:34:00Z

- **纠正根因**:除全量测试证据写法外,原 project-route 信封还存在真实循环门禁。`projects/控制台/engine-runner.js` 生成的 done 前验收要求系统增量更新 `board/status-rollup.md`,但 `projects/控制台/ceo-worker.js` 只会在 supervisor 引擎已经通过 done gate、`effectiveOk=true` 后调用 `appendProjectStatus()` 写 rollup。后置动作因此不可能在前置验收时成立。
- **需求传递判断**:老板的任务板状态真值需求从 CEO 到主管、实现和复审均完整;遗漏发生在系统生成验收协议这一环,它没有区分“执行者交付条件”和“成功后的系统 closeout”。
- **处理**:将 supervisor 信封改为“完成前更新项目 `status.md`”;把 rollup 明确登记为 `postCompletionCloseout` 元数据,继续由现有成功钩子写入。done gate 未放宽。另在后端程序员/主管提示词中固化:只有真实 exit 0 命令可进入可执行完成证据,非零诊断写入 `remaining_risks`。
- **回归证据**:`node tests/project-routing.test.js` PASS,断言新信封验收不含 `status-rollup.md` 且 closeout 元数据完整;`node tests/done-gate.test.js` PASS;`node tests/workspace-task-status-truth.test.js` PASS;`git diff --check` PASS。
- **功能复核**:独立查看 `projects/控制台/artifacts/task-status-truth-cr-1783692211429-034079bb/peekaboo-workspace-after.png`,页面在董事/主管链路进行中时显示“主管已接单、后端运行中、复审等上游”,未再把未发生阶段显示为完成。
- **剩余风险**:`office-experiment.test.js` 对应页面正有未收口的独立改动,不在本工单中回退或替它改测试;这类全量红只记诊断,不再伪装为本任务通过证据。
