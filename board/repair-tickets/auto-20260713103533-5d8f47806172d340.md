# 维修工单 auto-20260713103533-5d8f47806172d340 · 自动维修 supervisor-控制台/a88c0acd

- status: done
- created_at: 2026-07-13T10:35:33.859Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:done_gate.logic_chain: 视觉/UI 类验收缺少 runner 注入的 Codex CLI 图片输入回执

## 事件证据 / 路径
- queueAgent=supervisor-控制台
- queueId=a88c0acd
- task=cr-1783938132027-a88c0acd
- role=supervisor
- flow=review-loop
- project=控制台
- reason=done_gate.logic_chain: 视觉/UI 类验收缺少 runner 注入的 Codex CLI 图片输入回执
- engine_code=3
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1783938132027-a88c0acd

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Codex 特权),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260713103533-5d8f47806172d340.md)"
```

## 处理结果
- status: done

## 维修主管初查与派工 brief 2026-07-13

### 链路核查
- 原链路为 secretary → CEO 根队列 `ceo/b9846cff`（root task `cr-1783935491054-b9846cff`）→ 项目主管 `supervisor-控制台/a88c0acd` → `worker_code` implement / `supervisor` review；主管失败队列保留完整 `goal`、附件、结构化验收和 root/parent id，见 `projects/控制台/artifacts/queues/supervisor-控制台/failed/a88c0acd.json`。
- 同一 queueId 已运行三次：`cr-1783935602525-a88c0acd` 的 implement 完成后，review 把视觉行标为未完成；`cr-1783936898533-a88c0acd` 的 implement 完成且 review 自报 pass，但 review 证据未同时满足 Peekaboo 图片与 Codex 报告，被 gate 拒绝；`cr-1783938132027-a88c0acd` 则在 implement 结束即被 `done_gate.logic_chain` 拒绝。事件序列见 `projects/控制台/artifacts/engine-events.jsonl` 中 seq `234652-235648`、`235661-236661`、`236678-237272`。
- 第三轮 `implement-1/result.md` 已给出完成的业务实现、两张真实 Peekaboo PNG、Codex 对照报告和两行结构化验收；事件 seq `237268` 随后记录 runner.call，seq `237269-237270` 立即以“缺少 runner 注入的 Codex CLI 图片输入回执”失败，未进入 review。
- 该新 gate 来自前一严重维修单 `auto-20260713093810-8e1f550782c2d691` / `repair/1888e352`：它为视觉 review 新增 runner-owned `codex exec --image` 轨迹并于 10:32 完成；本单第三轮于 10:35 在 implement 阶段暴露了未覆盖的阶段回归。

### 需求传递判断
- 主人当前“任务板最新一行进展”目标、图片附件、控制台边界、status/rollup 约定和两行结构化验收均传到 CEO、supervisor 和 worker，没有被缩写、跳过或丢失；本次失败不是需求遗漏导致。
- 但 supervisor 的 `goal` 在主人当前目标后混入另一条“办公楼动画状态机”目标和大段秘书/队列上下文，属于交接污染。worker 实际仍按当前任务板目标执行，污染不是本次 gate 失败的直接原因；应作为后续 handoff 信封瘦身/防串单候选，不在本单跨模块扩修。

### 严重度 / 根因 / 架构风险
- 严重。缺陷位于公共 `shared/engine/cli-runner.js`、`shared/engine/done-gate.js` 与 review-loop 阶段门，影响所有带视觉/UI验收的任务；同一队列已连续三轮失败，并导致 CEO 根队列失败、自动维修开单和额外 token/时长消耗。
- 精确根因：`cli-runner.js` 的 `requiresVisualReview` / `buildVisualReviewInputManifest()` 明确只为 `node.id === 'review'` 生成 `runtime_visual_input`；但 `done-gate.js` 的 `validateImplementationLogicChain()` 在 implement 结束调用 `validateStructuredAcceptanceTable(..., {requireReview:false})`，而 `validateVisualAcceptanceEvidence()` 无视该阶段选项，仍无条件调用 `validateRuntimeVisualReviewReceipt()`。因此 implement 阶段被要求提供只有未来 review 阶段才能产生的回执，形成不可能前置条件。
- 架构风险：阶段专属证据未与 gate phase 绑定；公共门禁加固虽保持 fail-closed，却可能在证据生产节点之前消费证据。若只绕过本任务或伪造 receipt，会破坏上一单修复的可信看图防线，禁止采用。

### repair 执行范围与验收
- 在保留当前工作树既有未提交改动的前提下，最小修复阶段契约：implement 阶段仍必须核真实 Peekaboo 路径、Codex 对照报告、完成状态和硬指针，但不得要求 review-owned runtime receipt；进入 review/final completion 后仍必须校验 runner-owned `--image` trace、图片 SHA、全图片覆盖和逐图具体观察，继续 fail-closed。
- 首选在结构化验收/视觉证据校验中显式传递并执行 phase/`requireReview` 语义，不要用错误字符串特判，不要放宽现有视觉证据门。
- 补精确回归：① 只有 implementation + 合格视觉行时 `validateImplementationLogicChain` 可通过且无需 review receipt；② implement 缺 Peekaboo/Codex 证据仍失败；③ review 无/伪/不可用 receipt 仍失败；④可信 receipt + 逐图观察仍通过。必要时补最小 review-loop 端到端回归。
- 至少运行 `node --check shared/engine/done-gate.js`、`node --check shared/engine/cli-runner.js`、`node --check tests/done-gate.test.js`、`node tests/done-gate.test.js`、`node tests/cli-runner.test.js`、`node shared/engine/demo.js`；如全量测试有既有红灯须分账，不得冒充全绿。
- 不得修改原业务实现、failed queue/history、原任务结果或前一工单完成记录；不得自行调用本单 `repair-ticket-complete`；不得重启/杀进程。高危/不可逆操作停下给主人确认，密钥不回显。完成后在 repair 队列结果中逐项列 changed files、diff、命令退出码、残余风险和原任务恢复建议，等待主管独立复核。

### 结构化验收表（主管初查）
| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 部分 | projects/控制台/artifacts/engine-events.jsonl seq 234652-237272；projects/控制台/artifacts/queues/supervisor-控制台/failed/a88c0acd.json；shared/engine/cli-runner.js；shared/engine/done-gate.js | 已完成链路、分级和派工 brief；待 repair 执行、主管复核和唯一入口结案。 |

## 维修主管复核 2026-07-13

- 独立维修执行者已按 `repair/fc4c9318` 同一 brief 完成阶段契约最小修复；正式 repair 因 `codex-privileged` singleflight 自锁等待，seq `237527` 已 steer 为工单 done 后只读核对，禁止重复改写/重复结案。
- 主管已核 `shared/engine/done-gate.js:729-755,1127-1151`、`shared/engine/cli-runner.js:1265-1272`、`tests/done-gate.test.js:846-984`，确认 implement 仅跳过未来 review receipt，真实 Peekaboo/Codex 证据和 review 的 trace/SHA/逐图观察仍 fail-closed。
- 主管独立运行三项 `node --check`、`node tests/done-gate.test.js`、`node tests/cli-runner.test.js`、`node shared/engine/demo.js`、原失败 implementation 重放和 scoped `git diff --check`，全部 exit 0；完整记录见 `projects/控制台/artifacts/repair-supervisor-auto-20260713103533-5d8f47806172d340.md`。
- 全量残余两处既有红灯按 `board/repair-tickets/auto-20260713093810-8e1f550782c2d691.md:106` 分账，未冒充全绿。
- 不同故障已独立建单：`board/repair-tickets/repair-20260713-privileged-runner-singleflight.md`、`board/repair-tickets/repair-20260713-ticket-id-unicode-slug.md`。

## 结案协议
- 本维修请求单独建单,不同故障不得混入本单。
- 完成记录必须包含:链路证据、需求传递判断、严重度、根因、处理过程、复核验证、架构判断、知识沉淀候选、剩余风险 / 下一步。
- 使用 `repair-ticket-complete` 结案;系统生成固定 HTML,飞书发送摘要卡 + 附件,元宵同步报告文档。

### 完成记录 2026-07-13T10:53:06.706Z
- status: done
- report: projects/控制台/artifacts/repair-reports/auto-20260713103533-5d8f47806172d340.html
- report_sha256: 309882f1b20e110e3798984bd143eabe352b730eec37dbbf8f577da336285aba

链路证据:
secretary → CEO `b9846cff` → `supervisor-控制台/a88c0acd` → `worker_code/implement` → `supervisor/review`。三次运行分别见 `projects/控制台/artifacts/engine-events.jsonl:36088-37085,37098-38098,38115-38709`；第三次 runner 成功后，seq `237269-237270` 在 implement 阶段因未来 review 回执缺失失败。失败队列的完整 brief/附件/root-parent id 见 `projects/控制台/artifacts/queues/supervisor-控制台/failed/a88c0acd.json:10-38,49-56,97-105`；第三次 implementation 的 Peekaboo/Codex/验收硬证据见 `projects/控制台/artifacts/engine-runs/cr-1783938132027-a88c0acd/implement-1/result.md:1-3`。正式派工 `repair/fc4c9318` 见 eventlog `:38871,38875,38877,38964`。

需求传递判断:
主人当前“任务板最新一行进展”目标、附件、控制台边界、status/rollup 约定和两行结构化验收均传至 CEO、supervisor、worker；本次失败不是需求遗漏。supervisor goal 同时混入另一条“办公楼动画状态机”和大段队列上下文，属于交接污染但不是本 gate 失败的直接根因；已作为 handoff 信封瘦身候选保留，不在本单跨模块扩修。

严重度:
严重。缺陷位于公共 review-loop / done gate 阶段契约，影响所有视觉/UI任务；同一 queueId 连续三轮失败，拖累 CEO 根队列并产生自动维修和额外资源消耗。

根因:
`shared/engine/cli-runner.js:1265-1272` 只在 review 节点生产 runner-owned `runtime_visual_input`；`shared/engine/done-gate.js:1127-1151` 在 implement 阶段以 `requireReview:false` 校验结构化验收，但旧 `validateVisualAcceptanceEvidence` 仍无条件消费该未来回执，形成证据尚未生产就被前置消费的不可能条件。

处理过程:
主管按协议派 `repair/fc4c9318`，brief 限定只修阶段契约并保留 review fail-closed 防线。正式 repair 因与 repair-lead 共用 `codex-privileged` singleflight 被卡；按维修准则由独立维修执行者手动接管同一 brief，并 seq `237527` steer 正式队列为工单 done 后只读核对。最小补丁位于 `shared/engine/done-gate.js:749-755`：Peekaboo/Codex 证据仍先校验，仅 implement 的 `requireReview:false` 跳过未来 review receipt。回归位于 `tests/done-gate.test.js:846-984`，覆盖 implement 正/反例与 review 无/伪/不可用/仅元数据/覆盖不足/可信回执。

复核验证:
主管独立运行：`node --check shared/engine/done-gate.js` exit 0；`node --check shared/engine/cli-runner.js` exit 0；`node --check tests/done-gate.test.js` exit 0；`node tests/done-gate.test.js` exit 0 (`suite=done-gate`)；`node tests/cli-runner.test.js` exit 0 (`suite=cli-runner`)；`node shared/engine/demo.js` exit 0；原失败 `cr-1783938132027-a88c0acd/implement-1` 重放 `validateImplementationLogicChain` exit 0、`ok=true`；scoped `git diff --check` exit 0。完整记录：`projects/控制台/artifacts/repair-supervisor-auto-20260713103533-5d8f47806172d340.md`。

| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 完成 | `board/repair-tickets/auto-20260713103533-5d8f47806172d340.md`；`projects/控制台/artifacts/repair-supervisor-auto-20260713103533-5d8f47806172d340.md`；`projects/控制台/artifacts/engine-events.jsonl:38871,38964`；`node tests/done-gate.test.js` exit 0；`node tests/cli-runner.test.js` exit 0；`projects/控制台/artifacts/repair-reports/auto-20260713103533-5d8f47806172d340.html` | 本 `repair-ticket-complete` 事务将工单置 done、生成 HTML，并尝试飞书/元宵；成功或失败回执均在命令返回、eventlog 与 delivery-state 保留。 |

全量分账：`hardening-hooks.test.js` 既有 fixture 差异和 `ceo-serial-lock.test.js:513` 1200ms 时序断言仍 exit 1；前一工单 `board/repair-tickets/auto-20260713093810-8e1f550782c2d691.md:106` 已记录，且均不在本单目标 diff，未冒充全绿。

架构判断:
系统性阶段契约缺口。当前以显式 `requireReview` 最小修复；长期应为每种 evidence 声明 producer phase / earliest consumer phase，并补 implement→review 阶段矩阵门禁。另一个重复系统故障“repair-lead→repair 特权 runner 单飞自锁”已独立建单 `board/repair-tickets/repair-20260713-privileged-runner-singleflight.md`，不混入本单。

知识沉淀候选:
泛化判断：可泛化模式。问题模式：公共 fail-closed 门要求后续阶段才生产的专属证据，前置阶段因此永远无法通过。根因：证据只定义“必须存在”，未定义 producer phase / consumer phase，且安全加固缺少阶段矩阵回归。解法：显式传 phase/`requireReview`；早期阶段继续核当期硬证据，仅在生产阶段及最终完成核 runner-owned 回执；补早期正例、缺基础证据反例、后期无/伪回执反例、可信回执正例。项目技术映射：控制台 → Node.js review-loop done gate + Codex CLI runner-owned visual trace → `shared/engine/done-gate.js`、`shared/engine/cli-runner.js`。

剩余风险 / 下一步:
当前失败队列 `a88c0acd` 未在本维修单内篡改或伪造恢复；应由原业务调度重新执行 review，真实产生 runner-owned visual trace 后完成。handoff goal 污染保留为信封瘦身候选。特权 runner 单飞自锁已建 `repair-20260713-privileged-runner-singleflight`；本次发现的中文自动工单 ID 冲突已建 `repair-20260713-ticket-id-unicode-slug`。无高危或不可逆操作。
