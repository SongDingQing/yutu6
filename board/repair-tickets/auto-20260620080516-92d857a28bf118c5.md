# 维修工单 auto-20260620080516-92d857a28bf118c5 · 自动维修 supervisor-控制台/17aaa08b

- status: done
- created_at: 2026-06-20T08:05:16.581Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:node_failed

## 事件证据 / 路径
- queueAgent=supervisor-控制台
- queueId=17aaa08b
- task=cr-1781941816450-17aaa08b
- role=supervisor
- flow=review-loop
- project=控制台
- reason=node_failed
- engine_code=3
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1781941816450-17aaa08b

## 期望结果
维修员读取工单后先判断根因与架构风险;能安全修则修并验证;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- Starlaid 排除
- 不破现有功能; 能验证就写验证结果

## 维修员消费方式(v2 自动队列)
`repair` 是常驻 queueAgent,可通过 `queues/repair/` 自动消费;紧急时也可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260620080516-92d857a28bf118c5.md)"
```

## 处理结果
- status: done

### 完成记录 2026-06-20T08:08:52.992Z
- status: done

根因: source queue supervisor-控制台/17aaa08b 的 review-loop implement 节点并非 Codex 未启动; projects/控制台/artifacts/engine-runs/cr-1781941816450-17aaa08b/implement-1/process.log 显示 Codex 已启动并持续生成补丁,但 shared/engine/cli-runner.js 使用 spawnSync timeout 包住整个 CLI 生命周期,超过默认 nodeTimeoutSec=900 秒后把 ETIMEDOUT 统一写成 spawn codex 失败: 超时。engine-events.jsonl 中同 fingerprint 已在 2026-06-20 05:40、07:22、08:05 三次出现在 supervisor-控制台/review-loop implement,属于反复出现。处理: 最小修复 shared/engine/cli-runner.js,将 ETIMEDOUT 改为 runner 运行超时并带秒数和 run 目录,不再误报 spawn 失败;修复 projects/控制台/engine-runner.js,让 supervisor-* 的 review-loop 默认节点超时从 900 秒提升到 1800 秒并保留显式 nodeTimeoutSec,因此 nodeTimeoutSec=1 的烟测不受影响;新增 tests/cli-runner.test.js 并加入 tests/run.js,project-routing.test.js 覆盖默认超时。未重启服务,因为 engine-runner 是每个任务新进程,后续任务会读取新代码;未重跑原 UI 任务,避免扩大队列影响。验证: rg 查证 engine-events.jsonl 同类超时记录;读取本次 task/result/process.log 和前两次 process.log 尾部确认均为运行中超时;node --check shared/engine/cli-runner.js、node --check projects/控制台/engine-runner.js、node --check tests/cli-runner.test.js 均通过;node tests/run.js 全部通过(queue/project-routing/cli-runner/ceo-serial-lock);node shared/engine/demo.js PASS。架构判断: 系统性但可小修的问题,本质是控制台引擎把运行超时和启动失败混在同一错误桶,且主管复杂实现任务默认 900 秒低于 review-loop 1800 秒墙钟。后续建议把长任务拆分/进度心跳/剩余墙钟传入节点超时做成架构改进,并按新运行超时原因降噪自动维修工单。
