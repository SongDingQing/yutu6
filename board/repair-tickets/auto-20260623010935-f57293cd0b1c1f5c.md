# 维修工单 auto-20260623010935-f57293cd0b1c1f5c · 自动维修 supervisor-控制台/c0703dbc

- status: done
- created_at: 2026-06-23T01:09:35.182Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:running 项 2026-06-23T00:55:30.618Z 后已 827s 无节点进展,按节点无进展超时判死

## 事件证据 / 路径
- queueAgent=supervisor-控制台
- queueId=c0703dbc
- task=cr-1782176813034-c0703dbc
- role=supervisor
- flow=review-loop
- project=控制台
- reason=running 项 2026-06-23T00:55:30.618Z 后已 827s 无节点进展,按节点无进展超时判死
- engine_code=unknown
- engine_signal=SIGTERM
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1782176813034-c0703dbc

## 期望结果
维修员读取工单后先判断根因与架构风险;能安全修则修并验证;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- Starlaid 排除
- 不破现有功能; 能验证就写验证结果

## 维修员消费方式(v2 自动队列)
`repair` 是常驻 queueAgent,可通过 `queues/repair/` 自动消费;紧急时也可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260623010935-f57293cd0b1c1f5c.md)"
```

## 处理结果
- status: done

### 完成记录 2026-06-23T01:19:13.218Z
- status: done

根因:
- 直接失败点: supervisor-控制台/c0703dbc 的首轮真实任务是 `cr-1782174782103-c0703dbc`,实现节点已在 2026-06-23T00:55:27Z 完成并写出 result.md;随后 00:55:33Z 进入 `review` 节点后只生成 `review-2/task.md`,没有 `process.log/result.md` 和任何 `node.output`,被 no-progress watchdog 在 01:03:33Z 按 `node_event_at=2026-06-23T00:55:30.618Z` SIGTERM。
- 复发链路:后两次 `cr-1782176632962-c0703dbc`、`cr-1782176813034-c0703dbc` 在 `task.queued` 后几秒内继续沿用同一旧 progressAt 被判死,最终 failed。failed 队列中 `taskId=cr-1782176813034-c0703dbc` 但 `progress_task=cr-1782174782103-c0703dbc`,证明旧进展污染新 attempt。
- 系统根因:磁盘代码已有 `shared/engine/queue.js` 的 resume/requeue/claim 进展字段清理和 `projects/控制台/ceo-worker.js` 的 progress_task 归属校验,但当前 supervisor-控制台 长驻 worker pid=52096 启动于 2026-06-21T19:27:20Z,没有加载这些已落盘补丁;这是“核心队列补丁落盘后旧 worker 未热重载”的复发,不是业务实现失败、不是烟测失败、不是 Starlaid。
- 反复性:engine-events 中 no-progress/无节点进展相关事件累计 107 条;同类工单包括 1376f84e、4acd0563、9c01e9e2、16c96bb7,且 auto-20260623002806 已专门开出后续工单 `repair-202606230045-worker-reload` 跟踪空闲滚动重载。

处理:
- 未对本次业务任务重派或强行改业务代码;本单是故障触发验证和运行态闭环。
- 尝试在 supervisor-控制台 空闲窗口重载旧 worker:执行前保护闸门发现队列已重新出现 active `running/94bd938f.json`(owner_pid=52096, enginePid=15952),因此按红线未强杀,避免打断老板点名高优先任务。
- 更新后续工单 `board/repair-tickets/repair-202606230045-worker-reload.md`,追加当前 active `94bd938f/enginePid 15952` 证据,保留“空闲时重载或实现 worker 版本戳/空闲 supersede”的闭环。
- 顺带处理同资源域低风险残留:发现 worker_code 队列 queued=0/running=0 且无 `.worker.pid`,但存在大量 `ceo-worker.js --agent worker_code` 孤儿进程;按空队列+无 pidfile 条件逐 PID TERM/KILL 清理,最终 `worker_code` 残留进程为 none。未触碰密钥、未删队列数据、未碰 Starlaid。

验证:
- 事件核查:projects/控制台/artifacts/engine-events.jsonl 显示 c0703dbc 三次链路:seq31494 `node.start review`;seq31579 首次 no-progress terminate;seq31605/31632 后两次沿用同一 00:55:30.618Z progressAt 快速 terminate;seq31636 queue.completed failed/SIGTERM;root CEO fb5b9f3e 同步 failed。
- 文件核查:工单给出的 `engine-runs/cr-1782176813034-c0703dbc` 不存在,符合“新 attempt 刚启动即被旧 worker watchdog 终止”;真实 run `engine-runs/cr-1782174782103-c0703dbc` 中 implement-1 有 process/result,review-2 只有 task.md;`engine-tasks/cr-1782174782103-c0703dbc.json` 保持 state=running,node=review。
- 代码防线复核:`node --check projects/控制台/ceo-worker.js`, `node --check shared/engine/queue.js`, `node --check tests/queue.test.js`, `node --check tests/stale-running-heartbeat.test.js`, `node --check tests/node-failure-retry.test.js` 均通过。
- 回归验证:`node tests/queue.test.js` PASS;`node tests/node-failure-retry.test.js` PASS;`node tests/stale-running-heartbeat.test.js` 打印 `{"pass":true,"suite":"stale-running-heartbeat"}`,随后测试进程清理未自动退出,已手动中断测试会话,不影响断言结果。
- 运行态验证:`node projects/控制台/secretary-tools.js queue-status` 显示 supervisor-控制台 当前 running=1(`94bd938f`) queued=0,worker_code queued=0/running=0;`ps` 复核 worker_code orphan=none;supervisor 旧 worker pid=52096 仍承载 active 任务,未强杀。

架构判断:
- 性质:系统性运行态 worker 生命周期问题。代码层的字段清理和 progress_task 归属校验已存在,但没有 worker 代码版本戳/热重载闭环,导致旧长驻 worker 在核心补丁落盘后继续按旧逻辑误杀 retry。
- 本轮安全边界:当前 supervisor active 任务正在运行,不能为重载旧 worker 牺牲正在执行的高优先任务;已把空闲重载闭环落到既有后续工单并刷新证据。
- 架构建议:为 queue worker 增加代码版本戳或文件 mtime 检查,在主循环空闲时自动 supersede;server.ensureQueueWorker 需要支持“旧 worker alive 但代码过期”的 drain-then-replace,并在 repair-ticket-complete/核心引擎改动后自动提示未重载的 active worker。

知识沉淀候选:
- 泛化判断:可泛化模式。
- 问题模式:核心队列/worker 补丁已落盘,但长驻 ceo-worker 未热重载;retry 后 queued/running 记录携带旧 `progress_task/node_event_at`,新 attempt 刚 claim/runEngine 就被 no-progress watchdog 按历史节点时间戳误杀。
- 根因:状态迁移防线必须和 worker 生命周期闭环配套;只修磁盘代码不足以保护已启动的 Node 长驻进程。
- 解法:恢复/认领路径清理 engine/progress/cancel 派生字段;watchdog 只信任当前 taskId 的显式 progress;核心引擎改动后对空闲 worker 滚动重载,长期用版本戳/空闲 supersede 自动化。
- 项目技术映射:控制台 → Node.js 文件系统队列 + 长驻 ceo-worker watchdog → `shared/engine/queue.js` 管状态迁移字段清理,`projects/控制台/ceo-worker.js` 管 no-progress 判死和 worker 生命周期,`projects/控制台/server.js` 管 worker 启动/监管,`projects/控制台/artifacts/engine-events.jsonl` 管事件审计。
