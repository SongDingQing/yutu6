# 维修工单 auto-20260623120658-2db97efed70c9692 · 自动维修 insight-scout/insight-scout-repos-20260623-20

- status: done
- created_at: 2026-06-23T12:06:58.598Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:insight_scout_output_gate: missing insight_scout JSON

## 事件证据 / 路径
- queueAgent=insight-scout
- queueId=insight-scout-repos-20260623-20
- task=cr-1782216302340-insight-scout-repos-20260623-20
- role=insight-scout
- flow=agent-once
- project=控制台
- reason=insight_scout_output_gate: missing insight_scout JSON
- engine_code=3
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1782216302340-insight-scout-repos-20260623-20

## 期望结果
维修员读取工单后先判断根因与架构风险;能安全修则修并验证;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- Starlaid 排除
- 不破现有功能; 能验证就写验证结果

## 维修员消费方式(v2 自动队列)
`repair` 是常驻 queueAgent,可通过 `queues/repair/` 自动消费;紧急时也可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260623120658-2db97efed70c9692.md)"
```

## 处理结果
- status: done

### 完成记录 2026-06-23T12:23:20.354Z
- status: done

链路证据:
- 派发: insight-scout 定时扫描任务 `insight-scout-repos-20260623-20` 被队列派给 `role=insight-scout`，brief 要求围绕“多智能体编排 / 任务 DAG / 交接协议”产出 `insight_scout` JSON；证据 `projects/控制台/artifacts/engine-events.jsonl:44590`、`projects/控制台/artifacts/engine-runs/cr-1782216302340-insight-scout-repos-20260623-20/execute-1/task.md:1`。
- 期望返回: `task.md` 明确最后必须输出 JSON 代码块，并由引擎落盘到 `board/insights`、bulletin、seen-repos；证据 `projects/控制台/artifacts/engine-runs/cr-1782216302340-insight-scout-repos-20260623-20/execute-1/task.md:39`。
- 实际返回: 模型返回了分析文本和 JSON 开头，但 `result.md` 在 `analysis_markdown` 中途截断，未形成完整可严格解析 JSON；证据 `projects/控制台/artifacts/engine-runs/cr-1782216302340-insight-scout-repos-20260623-20/execute-1/result.md:39`、`:46`。
- 失败闭环: execute 节点先 `task.done`，随后输出门判 `missing insight_scout JSON`，任务失败并自动创建/入队维修工单；证据 `projects/控制台/artifacts/engine-events.jsonl:44649`、`:44651`、`:44654`、`:44657`、`:44658`。
- 恢复落盘: 前置维修执行已从 `result.md` 解析目标 run 并落盘到 `board/insights/insights.md`，事件 `insight_scout.output_applied` 记录 `insightsAppended=true`；证据 `projects/控制台/artifacts/engine-events.jsonl:44811`，落盘标记 `board/insights/insights.md:1033`。

需求传递判断:
- 未发现交接遗漏。任务 brief 保留了 Starlaid 排除、密钥不回显、不登录授权、输出 JSON 与自动落盘验收；洞察员实际回复也复述红线并尝试输出 JSON。遗漏不在派发链路，而在最终模型输出/runner 捕获到的 JSON 代码块不完整，导致机器输出门无法从 vars/严格 JSON 得到 `insight_scout`。

严重度:
- 严重。依据: 涉及 engine-runner 输出门和队列失败闭环；同类 `missing insight_scout JSON` 在 `2026-06-23T08:02:44Z` 的 16 档出现过，20 档在 `12:02/12:05/12:06` 三次尝试均失败并耗尽 maxRetry；证据 `projects/控制台/artifacts/engine-events.jsonl:39136`、`:44466`、`:44581`、`:44651`。

根因:
- 直接根因: zhipu-glm/agent-once 最终输出包含可用洞察内容，但 JSON 代码块被截断/未闭合，`engine-runner` 输出门从 ctx/result vars 取不到 `insight_scout`，于是报 `insight_scout_output_gate: missing insight_scout JSON`。
- 架构根因: 洞察员链路把“人读可用的 Markdown 报告”和“机器必须解析的 JSON 合同”绑在同一模型最终回复中，长 `analysis_markdown` 易被截断或破坏转义；若没有 `result.md` 兜底解析和幂等落盘，业务内容会因格式失败丢失。

处理:
- 不做重启、不清队列、不移动源失败队列状态，避免扩大影响。
- 复核当前代码已有最小兜底: `engine-runner` 在 vars 落盘失败后读取 `execute/result.md` 并调用 `extractInsightScoutOutputFromText`，证据 `projects/控制台/engine-runner.js:1141`；`insight-scout-repos` 支持 loose 解析截断/非严格 JSON，证据 `projects/控制台/insight-scout-repos.js:162`、`:190`。
- 对目标 run 执行幂等恢复校验: 当前解析器可从截断 `result.md` 提取 `slot=20260623-20`、`topic=multi-agent-orchestration`、`network_status=unavailable`、`analysisLength=1616`；再次 apply 返回 `already-applied`，说明已落盘且不会重复追加。

验证:
- `node projects/控制台/tools/repair-policy-check.js --ticket auto-20260623120658-2db97efed70c9692 --task cr-1782216302340-insight-scout-repos-20260623-20 --queue insight-scout-repos-20260623-20` -> severe/global_system_trace，证据 `projects/控制台/artifacts/engine-events.jsonl:41901`。
- 目标 `result.md` 提取验证 -> `hasOutput=true`、slot/topic/network_status 正确，证据 `projects/控制台/artifacts/engine-events.jsonl:41911`。
- 幂等落盘验证 -> `ok=true`、`insights.reason=already-applied`、`bulletinAdded=0`，证据 `projects/控制台/artifacts/engine-events.jsonl:41919`。
- `node tests/insight-scout-repos.test.js` -> pass，覆盖 malformed/truncated JSON 解析与截断落盘，证据 `tests/insight-scout-repos.test.js:146`、`:178`、`:196`，运行记录 `projects/控制台/artifacts/engine-events.jsonl:41911`。
- `node tests/run.js` -> All tests passed，证据 `projects/控制台/artifacts/engine-events.jsonl:41917`。

架构判断:
- 这是系统性输出契约问题，不是权限/重启/密钥问题，也不是 Starlaid 范围问题。
- 当前最小修复方向正确: 输出门失败前回读 run 目录 result.md，宽松提取可恢复 payload，再用 `applyInsightScoutOutput` 的 marker 保证幂等。
- 后续建议: 继续观察 zhipu-glm 洞察员 runner 的 JSON 截断率；若重复超过阈值，将洞察员产物改为“先写短 JSON envelope + analysis 单独 artifact”或降低 `analysis_markdown` 内嵌长度，并在门禁事件里明确 `strict_json_failed/recovered_from_result_md/unrecoverable_truncated` 三类原因。

知识沉淀候选:
- 泛化判断: 可泛化模式。
- 问题模式: agent 产出中有业务可用内容，但最终 JSON 合同因截断、未闭合或未转义引号而导致输出门误判失败，队列重试耗尽后自动开维修工单。
- 根因: 机器可解析合同与长篇自然语言内容混在同一最终回复；输出门只信严格 JSON/vars 时，缺少 run 目录证据恢复路径。
- 解法: 输出门失败前读取 `engine-runs/<task>/execute-*/result.md` 做宽松提取；落盘函数使用 marker/idempotency 防重复；补 malformed/truncated JSON 单测；保留失败事件与恢复事件用于 SLA 复盘。
- 项目技术映射: 控制台 → 洞察员定时借鉴扫描/输出门 → `projects/控制台/insight-scout-repos.js`、`projects/控制台/engine-runner.js`、`tests/insight-scout-repos.test.js`。

结构化验收表:
| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 设计对照 memory/decisions.md:15 失败必可观测 + 工单闭环 SLA | 完成 | `memory/decisions.md:15`; `projects/控制台/artifacts/engine-events.jsonl:44651`; `projects/控制台/artifacts/engine-events.jsonl:44657`; `projects/控制台/artifacts/engine-events.jsonl:41917` | 有明确失败原因、维修工单、run 目录和全量测试闭环。 |
| 设计对照 memory/decisions.md:74 重启需保活在途引擎 | 完成 | `memory/decisions.md:74`; `projects/控制台/artifacts/engine-events.jsonl:41910`; `projects/控制台/artifacts/engine-events.jsonl:41919` | 本单未重启、未 kill、未清队列，只做输出恢复与验证。 |
| 设计对照 memory/decisions.md:25 控制台范围/Starlaid 排除/密钥不回显 | 完成 | `memory/decisions.md:25`; `projects/控制台/artifacts/engine-runs/cr-1782216302340-insight-scout-repos-20260623-20/execute-1/task.md:5`; `projects/控制台/artifacts/engine-runs/cr-1782216302340-insight-scout-repos-20260623-20/execute-1/result.md:5` | 只处理控制台洞察员链路，无密钥、无登录授权。 |
| 设计对照 memory/decisions.md:33 控制台范围/Starlaid 排除/密钥不回显 | 完成 | `memory/decisions.md:33`; `projects/控制台/artifacts/engine-runs/cr-1782216302340-insight-scout-repos-20260623-20/execute-1/task.md:5`; `projects/控制台/artifacts/engine-runs/cr-1782216302340-insight-scout-repos-20260623-20/execute-1/result.md:18` | 同一边界在 brief 和产物中保留。 |
| 设计对照 memory/decisions.md:54 维修员职责培训 + 固化 | 完成 | `memory/decisions.md:54`; `projects/控制台/artifacts/engine-events.jsonl:41901`; `projects/控制台/artifacts/engine-events.jsonl:41911`; `projects/控制台/artifacts/engine-events.jsonl:41917` | 查重复、定严重度、做根因与近似问题验证。 |
| 设计对照 memory/decisions.md:77 控制台范围/Starlaid 排除/密钥不回显 | 完成 | `memory/decisions.md:77`; `projects/控制台/artifacts/engine-runs/cr-1782216302340-insight-scout-repos-20260623-20/execute-1/task.md:5`; `board/insights/insights.md:1036` | 落盘内容属于控制台洞察员 run。 |
| 任务验收: 工单 status 变 done; 写入根因、处理、验证、架构判断; 尝试飞书通知主人。 | 完成 | 本次 `repair-ticket-complete` 命令; `board/repair-tickets/auto-20260623120658-2db97efed70c9692.md` | 完成命令负责写回工单并尝试通知。 |
