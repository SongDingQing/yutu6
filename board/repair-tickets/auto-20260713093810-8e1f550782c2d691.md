# 维修工单 auto-20260713093810-8e1f550782c2d691 · 自动维修 supervisor-控制台/1100bcd4

- status: done
- created_at: 2026-07-13T09:38:10.871Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:done_gate.review_verification: review.verification.acceptance_table 第2行 未完成: 视觉/UI证据: peekaboo截图路径 + Codex对照设计挑错报告

## 事件证据 / 路径
- queueAgent=supervisor-控制台
- queueId=1100bcd4
- task=cr-1783934782401-1100bcd4
- role=supervisor
- flow=review-loop
- project=控制台
- reason=done_gate.review_verification: review.verification.acceptance_table 第2行 未完成: 视觉/UI证据: peekaboo截图路径 + Codex对照设计挑错报告
- engine_code=3
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1783934782401-1100bcd4

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Codex 特权),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260713093810-8e1f550782c2d691.md)"
```

## 处理结果
- status: done

## 维修主管初查与派工 brief 2026-07-13

### 链路核查
- secretary / CEO 根任务 `cr-1783933332488-83a37d46` 将老板原始目标、附件、边界和验收完整写入 `projects/控制台/brief.md:15381-15434`，再派给 `supervisor-控制台/1100bcd4`；`projects/控制台/artifacts/engine-events.jsonl:33466-33483` 记录 `queue.enqueued -> project.routed -> review-loop/implement`。
- 第一次执行 `cr-1783933466995-1100bcd4` 因 Codex runner 退出 1、GLM failover `fetch failed` 被重试，见 `projects/控制台/artifacts/engine-events.jsonl:34730-34740`。
- 第二次执行 `cr-1783933843134-1100bcd4` 完成状态机实现，但主管把视觉行判为部分，engine 按合同拒绝并重试，见 `projects/控制台/artifacts/engine-events.jsonl:35199-35432` 与对应 `review-2/result.md:1`。
- 第三次执行 `cr-1783934782401-1100bcd4` 只返工视觉证据；worker 返回完成，主管仍把视觉行判为部分，engine 正确触发 `done_gate.review_invalid` 并在重试耗尽后开本工单，见 `projects/控制台/artifacts/engine-events.jsonl:35768-35913`。

### 需求传递判断
- 老板需求、附件、状态机语义、文件边界、视觉验收和结构化验收表均从 secretary/CEO 完整传到 supervisor/worker，未发现缩写、跳过或传错。
- 遗漏发生在复审执行机制：`shared/engine/cli-runner.js` 的 CLI runner 没有把 implementation 视觉证据作为 `codex exec --image/-i` 初始输入；`shared/engine/cli-runner.js:398-430` 只为 OpenAI HTTP 的原始 `ctx.attachments` 构造图片内容，`shared/engine/cli-runner.js:702-712` 的 CLI 参数只追加 prompt。

### 严重度 / 根因 / 架构风险
- 严重。涉及公共 review-loop / runner / done gate，当前任务三次执行且两次视觉复审失败；历史存在多张视觉门禁维修单，影响所有需要 Codex 看图复审的 UI 任务，可能造成真实完成被误打回、重复重试、自动维修刷单和 token 浪费。
- 当前 typing 证据 `projects/控制台/artifacts/office-building-state-cr-1783934782401-1100bcd4/peekaboo-office-typing.png` 的 SHA-256 为 `28011eb55dd3708690d3d813d88ad7409e559a44adfeb842eca08d72dafa034a`，维修主管按原始分辨率复核可见标题、左侧办公室列表、完整状态条、舞台边框和地块；旧 reading 图反而存在合成缺层。主管 review 日志 `projects/控制台/artifacts/engine-runs/cr-1783934782401-1100bcd4/review-2/process.log:1083-1105` 只做 `stat/shasum/sips`，无 `view_image` 调用，却在 `:2057-2059` 声称“逐张打开”并把 typing 错判为缺层、reading 错判为完整；`review-2/result.md:1` 将该错误判断写入结构化结果。
- `shared/engine/done-gate.js:630-650,1076-1105` 目前只验证图片路径存在、文本包含 Codex、表格/硬指针齐全，不能证明复审模型实际获得过图像输入。因此门禁本次是正确失败关闭，但上游视觉裁决缺少可审计的“附图清单 + 逐图观察回执”。

### repair 执行范围与验收
- 先读取本工单和上述证据；先核当前 `git diff`，现有 `projects/控制台/config.json`、`shared/agents/supervisor/prompt.md`、`shared/engine/cli-runner.js`、`shared/engine/done-gate.js`、`shared/routing/runners.yaml`、`tests/done-gate.test.js` 均含其他任务未提交改动，禁止覆盖、回退或改写无关 hunk。
- 做最小系统修复：视觉/UI review 必须把 implementation 视觉验收行里的真实本地图像（及必要的用户原始附件）作为 Codex CLI 初始图片输入，并在 prompt / event / review verification 中保留路径映射，避免模型仅凭尺寸/哈希或上一轮 critique 猜图；非视觉 review、非 Codex runner 和图片不存在场景不得误加参数或泄露文件。
- 补守卫：只有当前轮确有可审计图像输入或显式真实图像查看证据，主管才能对画面内容下结论；`stat/shasum/sips` 只能证明文件元数据，不能证明画面完整。若选择新增结构化视觉观察回执，必须同步 done gate、review 合同和测试夹具，保持向后兼容边界清晰。
- 至少验证 `node tests/cli-runner.test.js`、`node tests/done-gate.test.js`（若改 gate）、`node tests/runner-failover.test.js`、`node shared/engine/demo.js`；新增正反回归须证明视觉 review 会收到准确有序的图片路径，普通 review 不附图，缺图不伪装已看图。
- 任务级复核：确认当前 typing 哈希 `28011e...` 的画面完整，旧 reading 哈希 `d67519...` 才是缺层；不得重写原状态机源码，不得改原 queue 历史，不得把原 failed 队列硬改 done。
- 高危/不可逆操作必须停下给主人确认；密钥/token/cookie/私钥不回显。完成后把 changed files、逐项 diff、测试命令/退出码、剩余风险写回本工单或 repair 队列结果，等待维修主管复核，不得自行结案。

## 结案协议
- 本维修请求单独建单,不同故障不得混入本单。
- 完成记录必须包含:链路证据、需求传递判断、严重度、根因、处理过程、复核验证、架构判断、知识沉淀候选、剩余风险 / 下一步。
- 使用 `repair-ticket-complete` 结案;系统生成固定 HTML,飞书发送摘要卡 + 附件,元宵同步报告文档。

### 完成记录 2026-07-13T10:36:14.955Z
- status: done
- report: projects/控制台/artifacts/repair-reports/auto-20260713093810-8e1f550782c2d691.html
- report_sha256: 36facad0ae17674990e6e333f176d4740bf066beaf68e72d882ccaf47c1c1360

链路证据：
- secretary/CEO 根任务 cr-1783933332488-83a37d46 把主人原始状态机目标、附件、边界和结构化验收完整写入 projects/控制台/brief.md:15381，并派给 supervisor-控制台/1100bcd4；projects/控制台/artifacts/engine-events.jsonl:33466-33484 记录 queue.enqueued → project.routed → review-loop/implement。
- 第一次 cr-1783933466995-1100bcd4 因 Codex ENOBUFS、fallback fetch failed 重试，见 engine-events.jsonl:34730-34740；第二次 cr-1783933843134-1100bcd4 与第三次 cr-1783934782401-1100bcd4 均在视觉行标部分后被 done gate 正确拒绝，见 :35424-35432、:35902-35913。
- 最终 reviewer 的 projects/控制台/artifacts/engine-runs/cr-1783934782401-1100bcd4/review-2/process.log:1082 只有 stat/shasum/sips，无 view_image 或 CLI --image，却在 :2057 后声称逐图打开并作视觉裁决。
- 维修主管已派 repair/1888e352；projects/控制台/artifacts/engine-events.jsonl:38516-38521 和 projects/控制台/artifacts/queues/repair/done/1888e352.json:15 证明 repair 执行、task.done、queue.completed。

需求传递判断：
- secretary → CEO → supervisor 的目标、附件、状态机语义、视觉验收和结构化验收表完整，无缩写、跳过或传错。
- 遗漏在公共执行机制：旧 CLI runner 没有把 implementation 视觉证据图作为 Codex CLI 图片输入，也没有 runner-owned 可审计轨迹；不是老板需求在交接中丢失。
- 维修主管初查曾把当前 28011e… typing 图误判完整；repair 和本主管均用 view_image(detail=original) 纠正：该图与旧 reading 都明显缺层，旧 typing 与 handoff 才完整。

严重度：
- 严重。缺陷位于公共 review-loop / CLI runner / done gate，影响所有视觉/UI复审；本链路连续两次视觉裁决失败并触发自动维修，既可能误打回，也可能误放行，伴随重复重试、刷单和 token 浪费。

根因：
- shared/engine/cli-runner.js 的 Codex CLI review 过去只追加 prompt，未从 implementation 验收行提取真实本地图像并以 codex exec --image 注入。
- shared/engine/done-gate.js 过去只核截图路径、Codex字样和表格文本，无法区分真实看图、元数据检查与模型自报。
- 当前图像事实同时表明原视觉素材确有 Safari 合成缺层；因此原任务的失败结果不能定性为完整图被误打回，但裁决过程不可审计仍是系统性根因。

处理过程：
- repair/1888e352 在不改 office-building 业务源码、不硬改 failed 队列、不重启服务的边界下完成系统加固。
- shared/engine/cli-runner.js：仅视觉 Codex review 按稳定顺序附加证据图/原始附件，生成 visual-input.json、图片与 trace SHA、runner.visual_input 事件和 runner-owned receipt；覆盖模型伪造 receipt，工具不可用即 partial/blocked。
- shared/engine/done-gate.js：校验 trace 路径/哈希/内容、当前图片 SHA、结论图片全覆盖和逐图具体观察；无轨迹、伪轨迹、纯 stat/shasum/sips、少附图片均 fail closed。
- tests/cli-runner.test.js 与 tests/done-gate.test.js 补正反回归；完整维修记录见 projects/控制台/artifacts/repair-visual-review-trace-cr-1783935705532-1888e352.md:1。

复核验证：
- 主管核对 git diff -- shared/engine/cli-runner.js shared/engine/done-gate.js tests/cli-runner.test.js tests/done-gate.test.js，且 git diff --check 退出 0；保留目标文件中既有无关 hunk。
- 主管亲自查看四张原图并复算 SHA：handoff 75f0e8… 完整；旧 reading d67519… 缺层；旧 typing 1d9945… 完整；当前 typing 28011e… 缺层。证据见 repair artifact:32-43 及四个 Peekaboo PNG 路径。
- 独立复跑 node --check 四个目标文件、node tests/cli-runner.test.js、node tests/done-gate.test.js、node tests/runner-failover.test.js、node shared/engine/demo.js，全部 exit 0。
- 独立复跑 node tests/run.js exit 1；本单相关套件全绿，残余仅 hardening-hooks.test.js 既有 fixture 期望差异和 ceo-serial-lock.test.js:513 的 1200ms 时序断言，两处与本单目标 diff 无关并如实保留。

架构判断：
- 可泛化系统性问题，已在公共 runner + gate 双层修复而非只改本任务结果；默认 fail closed。runner 轨迹证明图片被送入模型，但不能数学证明语义理解正确，仍需逐图观察和独立主管抽核。
- 原 supervisor-控制台/1100bcd4 的 failed 历史保持不变。原业务任务若恢复，应由 CEO 沿原 rootTaskId 新建 review-loop，重新捕获完整 reading/typing/handoff，并要求产生 review-N/visual-input.json、runner.visual_input、runtime_visual_input 和 visual_observations。

知识沉淀候选：
- 泛化判断：可泛化模式。
- 问题模式：视觉 reviewer 未获得真实图片，只跑元数据命令或复述旧 critique，却声称看图并形成完成/打回裁决。
- 根因：CLI runner 未注入验收图片，done gate 未校验 runner-owned 图像输入轨迹与逐图观察。
- 解法：runner-owned --image + trace SHA + 当前图片 SHA + 全图片覆盖 + 逐图具体观察；工具不可用即 partial/blocked；复核时把素材缺层与审查链路缺图分账。
- 项目技术映射：控制台 → Node.js review-loop CLI runner / done gate → shared/engine/cli-runner.js、shared/engine/done-gate.js。

剩余风险 / 下一步：
- 全仓仍有上述两处既有无关红灯；负责人应另单评估，不扩入本维修单。
- 非 Codex CLI 视觉 fallback 无同等可信轨迹时会安全阻断，可能增加 partial/blocked；原业务任务需重拍完整图后重新走 review-loop。

结构化验收表：
| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 完成 | board/repair-tickets/auto-20260713093810-8e1f550782c2d691.md:1; projects/控制台/artifacts/engine-events.jsonl:38516-38521; projects/控制台/artifacts/queues/repair/done/1888e352.json:15; projects/控制台/artifacts/repair-visual-review-trace-cr-1783935705532-1888e352.md:18; node tests/cli-runner.test.js exit 0; node tests/done-gate.test.js exit 0; projects/控制台/artifacts/repair-reports/auto-20260713093810-8e1f550782c2d691.html | repair-ticket-complete 是唯一结案/飞书/元宵投递入口；投递成功或失败回执均须保留。 |
