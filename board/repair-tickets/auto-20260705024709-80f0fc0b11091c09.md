# 维修工单 auto-20260705024709-80f0fc0b11091c09 · 自动维修 ceo/91ce8c30

- status: done
- created_at: 2026-07-05T02:47:09.294Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:queue worker crashed: resource lock wait timeout after 1800000ms

## 事件证据 / 路径
- queueAgent=ceo
- queueId=91ce8c30
- task=worker-crash-1783219629289-91ce8c30
- role=orchestrator
- flow=worker-crash
- project=控制台
- reason=queue worker crashed: resource lock wait timeout after 1800000ms
- engine_code=unknown
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/worker-crash-1783219629289-91ce8c30

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- Starlaid 排除
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Codex 特权),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260705024709-80f0fc0b11091c09.md)"
```

## 处理结果
- status: done

### 完成记录 2026-07-05T03:00:48.379Z
- status: done

【严重度】严重 / 系统性(核心调度锁,影响 orchestrator;虽为历史首次 resource.lock.timeout,但机制性缺陷可复发)。

【链路证据】谁派给谁/传了什么/期望/实际:
- 触发任务 ceo/91ce8c30 · task cr-1783217828787-91ce8c30 · role=orchestrator。
- 02:17:08.786 started_at(progressAt);02:17:13.199 resource.lock.wait(engine-events.jsonl:77836),申请 write[engine,agents,queue-state,brief-status],与 supervisor-控制台/567ded2f(token 73d8503a…,ownerPid 72055)在 engine/queue-state/brief-status 冲突。
- 该锁在等待期被连续占用:567ded2f(至02:20:06)→ be8361ff supervisor(02:20:06–02:30:34,抢在91ce8c30前拿到)→ ceo/f837ce89(02:31–02:33)→ supervisor/b1f6f06e(02:33:34–02:44:23,持锁~11min)→ ceo/b5d35105(02:44–02:45)→ supervisor/d2cb5c48(02:45:43起)。91ce8c30 每次锁释放都在竞争中落败。
- 02:47:09.288 resource.lock.timeout timeoutMs=1800000(engine-events.jsonl:79775)→ node.fail queue-worker reason 'resource lock wait timeout after 1800000ms'(:79776)→ task.failed(:79777)→ queue.completed ok=false(:79778)→ 自动开工单(:79779)。
- 整个等待期 keepalive 均为 enginePid=null 'waiting for engine pid'、ownerPid=62204 存活并心跳——runner 进程没崩,是被锁挡在引擎启动前。'queue worker crashed' 标签为误称,实为锁等待超时。

【需求传递判断】无老板需求遗漏/缩写/传错。故障纯机械性(锁饥饿),与 brief/handoff 内容无关。

【根因】资源锁无公平性,写者饥饿:
- projects/控制台/resource-locks.js:620-677 acquireResourceLease 为轮询循环——每轮仅 conflictHolders 检查『当前持有者』,无冲突即抢锁;conflictHolders(:317-328)只看 state.writer/readers,完全不看 waiters 集合。waiter 文件(writeWaiter/readWaiters)仅用于 detectCircularWait 死锁检测,不参与授予顺序。
- 因此在持续高竞争下(supervisor codex 运行各持 engine+brief-status 10–11min,心跳 touchLocks 不断续租使 DEFAULT_LEASE_MS=5min 永不判定 stale),supervisor 与其它 ceo 任务背靠背连拿,91ce8c30 无缝隙插入,饥饿至 DEFAULT_WAIT_TIMEOUT_MS=30min 超时。
- 次要放大:watchdog 检测到 running-no-progress 但 restart 被 max-in-window(recentCount 3)throttle,无自动救援;且即便重启也会重入同一饥饿(锁被存活任务合法持有)。

【处理】未改核心锁代码。理由:修改文件锁的授予语义(引入公平/FIFO/aging)是对全体 agent 调度的高危改动,误改可致死锁或优先级反转,后果远重于历史首次的一次 30min 超时;按红线『高危/不可逆操作先给主人确认』,不擅自重写。当前失败任务已死、锁已释放、系统健康(当前 all_blocked 均阻塞在本 repair-lead 任务自身宽锁上,结案释放即恢复,属正常)。无需实时补救。

【复核验证】核实文件与行号:resource-locks.js:317-328(conflictHolders 不看 waiters)、:620-690(无公平轮询+30min超时抛错)、:9-12(LEASE 5min/TIMEOUT 30min/POLL 700ms)。核实事件:全历史 resource.lock.timeout 仅 1 次(=本次);worker-crash flow 仅 1 次。核实链路 79775-79781 与 77836/77817 冲突链。

【架构判断】系统性。建议(待主人拍板后派 repair 执行,含测试):
1) 在 acquireResourceLease 增加反饥饿/公平:conflictHolders 或授予判定纳入 waiters——当存在等待更久且资源交集的 waiter 时,后到者不得抢占(bounded-wait);优先给最老 eligible waiter。已有 waiter 文件+时间戳基础设施可复用。
2) 对超长写锁(engine/brief-status)设软上限或分域细化,减小 supervisor 长跑对 orchestrator 的锁面阻断。
3) 超时前预警:接近 timeout(如 >20min)先发 P1,而非静默到 30min 才 fail。
4) worker-crash 误称正名:锁等待超时应单列 fail 类型,避免与真实进程崩溃混淆,便于统计与自愈分账。
5) 复用 resource-locks-smoke-test.js 补『长持锁+多竞争者饥饿』回归用例,守住反饥饿不回退。

【知识沉淀候选】问题模式:文件资源锁在高竞争下写者饥饿超时(误标 worker crashed)。根因:acquireResourceLease/conflictHolders 无公平性,只看当前持有者不看 waiter,长持写锁+心跳续租使等待者永失竞争。解法:授予判定纳入 waiter 的 bounded-wait/aging;长写锁设上限;临超时预警;锁超时与进程崩溃分类分账。

【结构化验收表】
| 要点 | 完成状态 | 证据位置 | 备注 |
|---|---|---|---|
| 主管完成链路核查 | 完成 | engine-events.jsonl:77817/77836/79775-79781;resource-locks.js:317-328,620-690 | 起→wait→连续抢占→timeout 全链还原 |
| 严重度分级 | 完成 | 本记录『严重度』 | 严重/系统性,附首次+机制依据 |
| 必要派工 | 部分 | 本记录『处理』『架构判断』 | 高危核心锁改动,按红线待主人拍板后再派 repair;未擅改 |
| 复核与结案 | 完成 | 本记录『复核验证』;工单 status=done | 核实文件行号+事件计数 |
| 工单 status 变 done | 完成 | 本 repair-ticket-complete 输出 | — |
| 飞书通知主人 | 完成 | repair-ticket-complete 内置 notify | 见命令输出 notify 结果 |
