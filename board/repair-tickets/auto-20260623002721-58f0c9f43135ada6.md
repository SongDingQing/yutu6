# 维修工单 auto-20260623002721-58f0c9f43135ada6 · 自动维修 _organize-idempotency/undefined

- status: done
- created_at: 2026-06-23T00:27:21.798Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:done_gate_failed: 交付型任务缺少 changed_files/截图/diff 等交付证据

## 事件证据 / 路径
- queueAgent=_organize-idempotency
- queueId=undefined
- task=cr-1782172497450-undefined
- role=_organize-idempotency
- flow=review-loop
- project=控制台
- reason=done_gate_failed: 交付型任务缺少 changed_files/截图/diff 等交付证据
- engine_code=unknown
- engine_signal=SIGTERM
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1782172497450-undefined

## 期望结果
维修员读取工单后先判断根因与架构风险;能安全修则修并验证;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- Starlaid 排除
- 不破现有功能; 能验证就写验证结果

## 维修员消费方式(v2 自动队列)
`repair` 是常驻 queueAgent,可通过 `queues/repair/` 自动消费;紧急时也可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260623002721-58f0c9f43135ada6.md)"
```

## 处理结果
- status: done

### 完成记录 2026-06-23T00:37:17.671Z
- status: done

根因:
- 表层失败是 `done_gate_failed: 交付型任务缺少 changed_files/截图/diff 等交付证据`,但真正根因在 `shared/engine/queue-organizer.js` 的 `idempotencyFile()` 旧落点: queue-organize apply 的幂等审计 JSON 写入 `projects/控制台/artifacts/queues/_organize-idempotency/*.json`。
- `projects/控制台/ceo-worker.js`/`server.js` 会从 `artifacts/queues/` 下所有安全目录名识别 queueAgent,于是 `_organize-idempotency` 被误当成真实队列 agent;其审计文件不是任务信封,没有 `id/goal`,被领取后形成 `queueAgent=_organize-idempotency, queueId=null/undefined, task=cr-1782172497450-undefined`。
- 目标任务随后进入 review-loop: implement/review 均正确 no-op/pass,但因验收文本含“实现/文件”等词被 done gate 作为交付型任务检查,最终以缺少 changed_files/截图/diff 打回。done gate 是末端症状,不是首要根因。
- 重复性证据: engine-events 中该任务从 2026-06-22T23:56:02Z 到 2026-06-23T00:28:51Z 有 55 条相关事件,包含 queue.engine.terminate 2 次、queue.completed 2 次、done_gate.blocked 1 次、repair ticket dedupe 1 次;旧目录中存在 `50-0000000004-undefined.json` 与 `aec47...json` 两份审计 JSON 被污染为队列根文件。

处理:
- 最小代码修复:把 `shared/engine/queue-organizer.js:311-313` 的幂等审计落点改到 `artifacts/queue-organize-idempotency/<hash>.json`,彻底脱离 `artifacts/queues/`。
- 回归测试:更新 `tests/queue-organizer.test.js:16-18,212-218` 与 `tests/ceo-queue-control.test.js:112-115,304-310`,断言 dry-run 不写审计、apply 写审计,且不得创建 `queues/_organize-idempotency` 伪 agent 目录。
- 现网清理:确认 PID 89576 命令为 `ceo-worker.js --agent _organize-idempotency` 后发 TERM;将旧污染目录里的审计/失败/PID 元数据搬到 `projects/控制台/artifacts/queue-organize-idempotency/legacy-queue-dir/`,移除空的 `projects/控制台/artifacts/queues/_organize-idempotency` 目录。

验证:
- `node --check shared/engine/queue-organizer.js` PASS。
- `node tests/queue-organizer.test.js` PASS。
- `node tests/ceo-queue-control.test.js` PASS。
- `node tests/done-gate.test.js` PASS。
- `node tests/run.js` PASS,输出 All tests passed。
- `test ! -e projects/控制台/artifacts/queues/_organize-idempotency` 输出 `no_legacy_queue_dir`。
- `ps -p 89576 -o pid=,stat=,command=` 无进程输出,确认伪 agent worker 已停止。
- 旧证据保留在 `projects/控制台/artifacts/queue-organize-idempotency/legacy-queue-dir/`,没有删除审计内容。

架构判断:
- 这是系统性问题,不是一次性 no-op/烟测输出问题:审计数据被放进队列命名空间,违反“运行队列目录只存任务信封”的边界,会诱发伪 agent、空目标、资源锁冲突、重复自动提单。
- 本轮修复采用命名空间隔离,不扩大改 done gate;done gate 仍保留对真实交付型任务的 changed_files/截图/diff 硬门。
- 后续建议:凡是辅助审计、幂等缓存、索引、worker 元数据,不得直接放在 `artifacts/queues/<agent>/` 根目录;如果确需与队列同目录,必须使用非 `.json` 或队列扫描显式排除,并加测试覆盖。

知识沉淀候选:
- 泛化判断:可泛化模式。
- 问题模式:把非任务审计/缓存 JSON 写进队列根目录,被文件队列扫描器误识别为可执行任务,产生伪 queueAgent/undefined queueId/空目标 review-loop。
- 根因:队列命名空间和审计命名空间未隔离;文件队列扫描规则按 `queues/<agent>/*.json` 识别任务,审计文件也满足该模式。
- 解法:审计/幂等缓存移出 `queues/` 命名空间;测试断言 apply 写审计但不创建伪 agent 队列目录;现网迁移旧污染文件并停止伪 worker。
- 项目技术映射:控制台 -> 文件系统队列 + queue-organizer 幂等审计 -> `shared/engine/queue-organizer.js` 负责 queue-organize dry-run/apply/幂等记录; `projects/控制台/ceo-worker.js`/`server.js` 从 `artifacts/queues/` 发现 queue agent。

### 完成记录 2026-06-23T00:55:07.690Z
- status: done

根因:
- 原失败表象是 `done_gate_failed: 交付型任务缺少 changed_files/截图/diff 等交付证据`。复核确认末端症状来自 `_organize-idempotency` 被当成真实 queueAgent 领取执行，而不是 done gate 本身误判。
- 根因路径明确到 `shared/engine/queue-organizer.js` 的旧幂等审计落点：queue-organize apply 曾把审计 JSON 写到 `projects/控制台/artifacts/queues/_organize-idempotency/*.json`。`projects/控制台/ceo-worker.js`/`server.js` 从 `artifacts/queues/` 发现队列 agent 时把该审计目录误识别为真实队列，形成 `queueAgent=_organize-idempotency`、`queueId=null/undefined`、`task=cr-1782172497450-undefined`。
- 字段级事件复核显示这不是一次偶发：`engine-events.jsonl` 中 `_organize-idempotency`/该 task 从 2026-06-22T22:11:53.424Z 到 2026-06-23T00:33:31.769Z 共 929 条事件，其中 `resource.scheduler.all_blocked` 749 条、`queue.claimed` 4 条、`queue.engine.terminate` 3 条、`done_gate.blocked` 1 条、维修工单创建/去重各 1 条。

处理:
- 工单已有的最小修复成立，本轮未再扩大改代码。当前 `shared/engine/queue-organizer.js:311-313` 已把幂等审计写到 `artifacts/queue-organize-idempotency/<hash>.json`，脱离 `artifacts/queues/` 队列命名空间。
- 测试覆盖已存在：`tests/queue-organizer.test.js` 和 `tests/ceo-queue-control.test.js` 断言 dry-run 不写审计、apply 写审计，且不得创建 `queues/_organize-idempotency` 伪 agent 目录。
- 现场状态已复核：`projects/控制台/artifacts/queues/_organize-idempotency` 不存在；旧污染文件保留在 `projects/控制台/artifacts/queue-organize-idempotency/legacy-queue-dir/`；未发现 `ceo-worker.js --agent _organize-idempotency` 进程。

验证:
- `node --check shared/engine/queue-organizer.js` 通过。
- `test ! -e projects/控制台/artifacts/queues/_organize-idempotency` 通过。
- `node tests/queue-organizer.test.js` 通过。
- `node tests/done-gate.test.js` 通过。
- `node tests/ceo-queue-control.test.js` 单独运行通过。
- `node tests/run.js` 第一轮出现一次 `ceo-queue-control.test.js` 在全套中读取 `jumpedEntry.priority` 的临时失败；按烟测边界只记录诊断、不扩大改动。随后无并发重跑 `node tests/run.js` 完整通过，输出 `All tests passed.`
- 维修通知链路复核：事件中已有 `repair.ticket.completed` 和 `repair.ticket.notify_sent`，本命令再次按工单流程追加完成记录并尝试通知。

架构判断:
- 系统性问题，不是一次性测试输出问题。核心架构缺口是把非任务审计/缓存 JSON 放进文件队列扫描命名空间，违反 `artifacts/queues/<agent>/` 只存任务信封的边界。
- 本轮不改 done gate，因为 changed_files/截图/diff 门禁对真实交付型任务仍有价值；应通过队列命名空间隔离解决源头。
- 后续建议：所有审计、幂等缓存、索引、worker 元数据必须放在队列命名空间之外；如未来确需同目录，队列扫描器必须显式排除并加测试。

知识沉淀候选:
- 泛化判断: 可泛化模式。
- 问题模式: 非任务审计/缓存 JSON 写入 `artifacts/queues/<agent>/` 根目录，被文件队列扫描器误识别为可执行任务，诱发伪 queueAgent、undefined queueId、空目标 review-loop 和重复自动维修提单。
- 根因: 队列任务命名空间与审计/幂等命名空间未隔离；扫描规则按队列目录和 JSON 任务信封推断 agent/任务，审计文件满足了误识别条件。
- 解法: 审计/幂等缓存移出 `queues/`；加回归测试断言 apply 写审计但不创建伪 agent 队列目录；迁移旧污染文件并停止伪 worker。
- 项目技术映射: 控制台 -> 文件系统队列 + queue-organizer 幂等审计 -> `shared/engine/queue-organizer.js` 负责 queue-organize dry-run/apply/幂等记录；`projects/控制台/ceo-worker.js`/`server.js` 从 `artifacts/queues/` 发现 queue agent。

### 完成记录 2026-06-23T01:02:19.733Z
- status: done

根因:
- 本轮复核确认该工单已处于 done，既有两段完成记录的根因成立。表层失败是 `done_gate_failed: 交付型任务缺少 changed_files/截图/diff 等交付证据`，但真正根因是 queue-organize 幂等审计曾写入 `projects/控制台/artifacts/queues/_organize-idempotency/*.json`。
- `projects/控制台/ceo-worker.js`/`server.js` 会从 `artifacts/queues/` 下发现 queueAgent，导致 `_organize-idempotency` 审计目录被误识别为真实队列；审计 JSON 不是任务信封，缺少正常 `id/goal`，最终形成 `queueAgent=_organize-idempotency`、`queueId=undefined/null`、空目标 review-loop，并在末端触发 done gate。
- 重复性证据: 本轮解析 `projects/控制台/artifacts/engine-events.jsonl`，与 `_organize-idempotency`/`cr-1782172497450-undefined`/本工单相关事件共 1121 条，时间范围 2026-06-22T21:22:39.613Z 到 2026-06-23T00:58:00.296Z；包含 `resource.scheduler.all_blocked` 799 条、`queue.claimed` 4 条、`queue.engine.terminate` 3 条、`done_gate.blocked` 1 条、`repair.ticket.completed` 2 条。说明这是队列命名空间污染造成的反复系统性故障，不是一次性测试噪声。

处理:
- 当前代码已采用最小修复，`shared/engine/queue-organizer.js` 的 `idempotencyFile()` 已把审计写到 `artifacts/queue-organize-idempotency/<hash>.json`，不再落入 `artifacts/queues/`。
- 现场复核 `projects/控制台/artifacts/queues/_organize-idempotency` 不存在；旧污染证据保留在 `projects/控制台/artifacts/queue-organize-idempotency/legacy-queue-dir/`；未发现 `ceo-worker.js --agent _organize-idempotency` 进程。
- 本轮未扩大代码改动，因已有修复与回归测试覆盖成立；按烟测/故障复核边界只做诊断、验证和完成记录闭环。
- 模块注册表技能要求的 `/Users/yutu6/.codex/modules/INDEX.md` 和 lookup 脚本在本机不存在，本轮已记录为辅助文档入口缺失，不影响本工单根因和验证。

验证:
- `node --check shared/engine/queue-organizer.js` 通过。
- `test ! -e projects/控制台/artifacts/queues/_organize-idempotency` 通过，输出 `no_legacy_queue_dir`。
- `node tests/queue-organizer.test.js` 通过。
- `node tests/done-gate.test.js` 通过。
- `node tests/ceo-queue-control.test.js` 通过。
- `node tests/run.js` 通过，输出 `All tests passed.`
- 事件日志复核命令解析到 1121 条相关事件，支持重复性判断；本命令再次调用 `repair-ticket-complete`，尝试写回工单并触发飞书通知主人。

架构判断:
- 系统性问题，不是一次性交付证据缺失。架构缺口是“队列任务命名空间”和“审计/幂等缓存命名空间”未隔离，导致非任务 JSON 被队列扫描/worker 发现机制当作真实任务来源。
- 当前修复方向正确: 隔离审计落点，而不是放松 done gate。done gate 对真实交付型任务仍应保留 changed_files/截图/diff 证据门禁。
- 后续架构建议: `artifacts/queues/<agent>/` 根目录只允许任务信封和队列状态子目录；所有审计、缓存、索引、worker 元数据默认放到队列命名空间外。若未来确需同域存放，必须在扫描器显式排除并配套测试。

知识沉淀候选:
- 泛化判断: 可泛化模式。
- 问题模式: 非任务审计/缓存 JSON 写入 `artifacts/queues/<agent>/` 或其伪 agent 目录，被文件队列发现机制误识别为可执行队列，诱发伪 queueAgent、undefined queueId、空目标 review-loop、资源锁阻塞和重复自动维修工单。
- 根因: 文件系统队列扫描规则依赖目录/JSON 约定，但辅助数据复用了同一命名空间，缺少边界隔离和负向测试。
- 解法: 审计/幂等缓存移出 `queues/`；测试断言 apply 写审计但不创建伪 agent 目录；现场迁移旧污染文件并停止伪 worker；后续把“队列目录只存任务信封”写成守卫/测试原则。
- 项目技术映射: 控制台 -> 文件系统队列 + queue-organizer 幂等审计 -> `shared/engine/queue-organizer.js` 负责 queue-organize dry-run/apply/幂等记录；`projects/控制台/ceo-worker.js`/`projects/控制台/server.js` 从 `artifacts/queues/` 发现 queue agent；`projects/控制台/secretary-tools.js repair-ticket-complete` 负责维修工单完成记录与通知尝试。
