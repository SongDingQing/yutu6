# 维修工单 auto-20260623051327-41a31128d0eb5fcd · 自动维修 ui_optimizer/2316f69d

- status: done
- created_at: 2026-06-23T05:13:27.206Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:running 队列文件已不存在,终止对应 engine 并释放 slot

## 事件证据 / 路径
- queueAgent=ui_optimizer
- queueId=2316f69d
- task=cr-1782191510223-2316f69d
- role=ui_optimizer
- flow=agent-once
- project=控制台
- reason=running 队列文件已不存在,终止对应 engine 并释放 slot
- engine_code=unknown
- engine_signal=SIGTERM
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1782191510223-2316f69d

## 期望结果
维修员读取工单后先判断根因与架构风险;能安全修则修并验证;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- Starlaid 排除
- 不破现有功能; 能验证就写验证结果

## 维修员消费方式(v2 自动队列)
`repair` 是常驻 queueAgent,可通过 `queues/repair/` 自动消费;紧急时也可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260623051327-41a31128d0eb5fcd.md)"
```

## 处理结果
- status: done

### 完成记录 2026-06-23T05:19:46.126Z
- status: done

根因:
- 本单不是 UI 评审业务失败。ui_optimizer/2316f69d 在 2026-06-23T05:11:50Z 被认领后,先等待 frontend_designer/1eba2dec 持有的资源锁;此时 engine 尚未启动, running 记录没有 enginePid。
- projects/控制台/ceo-worker.js 的 sweepStaleRunning()/runningEngineHeartbeat() 只看 engine_heartbeat_at/engine_started_at/started_at,没有识别“已认领但仍在资源锁/runner/slot 等待阶段”的合法 pre-engine 等待。05:12:57Z running 记录因 started_at 超 60s 被误判为 engine 心跳失联并 requeue;原 worker 随后在 05:13:20Z 拿到资源锁并启动 engine,但 running 文件已被移走,runEngine() 轮询触发 queue-running-missing,于是 SIGTERM 终止并生成 failed 终态/维修工单。
- 同类“running 队列文件已不存在”在 24h 内多次出现,本次是资源锁等待与 running 恢复逻辑之间的系统性竞态,不是一次性配置问题。

处理:
- 最小修复 projects/控制台/ceo-worker.js:新增 pre_engine_wait_heartbeat_at 续租;handle() 从 noteRunningSpec 后到资源锁、runner 单飞锁、engine slot 拿到前持续写 pre-engine 等待心跳;runningEngineHeartbeat() 在没有 enginePid 时优先使用 pre_engine_wait_heartbeat_at,有 enginePid 时仍以 engine_heartbeat_at 为准,避免掩盖真实 engine 失联。
- 补 projects/控制台/tools/resource-locks-smoke-test.js 断言:started_at 已过期但 pre_engine_wait_heartbeat_at 新鲜时不判 stale;一旦 enginePid 存在,仍优先 engine_heartbeat_at。
- 现场止血:清理非 repair 且无 running 队列项的旧 ceo-worker.js 进程;保留当前 repair worker/engine。清理后 ps 仅剩 repair worker 与本维修 engine,后续非 repair 入队会由 server 按新源码重新拉起 worker。

验证:
- 查证事件:engine-events seq 36109~36129 显示 ui_optimizer/2316f69d claimed 后 resource.lock.wait,随后 queue.recovered 原因为 engine_heartbeat_at 66s 未续约;seq 36143~36151 显示原 worker 获锁后启动 engine 又因 running 文件缺失终止;同一队列项 retry=1 后 done,engine_code=0,finished_at=2026-06-23T05:14:23.811Z。
- 业务闭环:projects/控制台/artifacts/queues/ui_optimizer/done/2316f69d.json 为 done;ui_optimizer.md 已写明因 repair/002511e6 running 触发空闲硬门跳过,未截图、未派 issue,符合低优先级评审规则。
- 运行验证:node --check projects/控制台/ceo-worker.js PASS;node --check projects/控制台/tools/resource-locks-smoke-test.js PASS;node projects/控制台/tools/resource-locks-smoke-test.js PASS。
- 队列验证:node projects/控制台/secretary-tools.js queue-status 显示除 repair/002511e6 外无 queued/running;ui_optimizer queued=0 running=0 done=5 failed=1。
- 残余测试说明:node projects/控制台/tools/mechanisms-smoke-test.js 失败在既有断言 checkAutoOptimizer 期望 disabled 但当前返回 enqueued,与本次 pre-engine 心跳修复无关,已记录为残余测试债。

架构判断:
- 系统性问题。队列状态机把“claimed/running 但尚未启动 engine 的资源锁等待阶段”和“engine 已启动后心跳失联”混在同一 running 判死通道里,导致合法等待被清扫。已用 pre-engine 等待心跳把两个阶段分开;后续建议把 running 子状态显式化为 claimed_waiting_resource / waiting_runner_slot / engine_running,并让 sweep 按子状态判死。

知识沉淀候选:
- 泛化判断:可泛化模式。
- 问题模式:队列项 claim 后进入资源锁/runner/slot 等待,enginePid 尚未写入;等待超过 RUNNING_ENGINE_HEARTBEAT_STALE_MS 后被恢复清扫误判为失联并 requeue,原 worker 之后启动 engine 因 running 文件已不存在而自杀式失败。
- 根因:running 恢复逻辑缺少 pre-engine 等待阶段心跳/子状态,只用 started_at/engine heartbeat 判断生命周期。
- 解法:在 engine 启动前持续写 pre_engine_wait_heartbeat_at;无 enginePid 时用该心跳保活,有 enginePid 时仍以 engine_heartbeat_at 判定真实 engine;补 smoke 断言防回归。预防建议:队列状态机显式拆分 pre-engine waiting 与 engine-running,并为资源锁等待超时单独告警。
- 项目技术映射:控制台 → Node.js 文件队列 + 资源锁 + launchd 本机守护 → projects/控制台/ceo-worker.js(队列 worker/恢复清扫/engine 启动), projects/控制台/resource-locks.js(资源域锁), shared/engine/queue.js(queued/running/done 文件状态)。
