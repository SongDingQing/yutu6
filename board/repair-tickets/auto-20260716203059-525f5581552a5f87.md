# 维修工单 auto-20260716203059-525f5581552a5f87 · 自动维修 supervisor-控制台/53c05325

- status: done
- created_at: 2026-07-16T20:30:59.194Z
- source: 自动故障触发
- priority: high


## 问题
队列任务失败,系统自动开维修工单。失败原因:done_gate.logic_chain: implementation.acceptance_table 第6行 证据对不上: 视觉/UI 行证据未同时包含可核 peekaboo 图片截图和 Codex 复核报告; failure.json/截图失败标记不能当截图完成证据: 视觉/UI证据: peekaboo截图路径 + Codex对照设计挑错报告

## 事件证据 / 路径
- queueAgent=supervisor-控制台
- queueId=53c05325
- task=cr-1784221196753-53c05325
- role=supervisor
- flow=review-loop
- project=控制台
- reason=done_gate.logic_chain: implementation.acceptance_table 第6行 证据对不上: 视觉/UI 行证据未同时包含可核 peekaboo 图片截图和 Codex 复核报告; failure.json/截图失败标记不能当截图完成证据: 视觉/UI证据: peekaboo截图路径 + Codex对照设计挑错报告
- engine_code=3
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1784221196753-53c05325

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Codex 特权),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260716203059-525f5581552a5f87.md)"
```

## 处理结果
- status: done

## 维修主管诊断（2026-07-17）
- 链路：CEO 根任务 `cr-1784206833452-4c8eb313` 在 `engine-events.jsonl:12743-12746` 将纯引擎优化 brief 派给 `supervisor-控制台/53c05325`；队列终态文件保留的生产者信封明确为 `visual_acceptance.required=false`，验收末行为 `视觉/UI证据: not_applicable`。
- 需求传递漂移：三次 engine-run 的 `meta.json` / `task.md` 却把同一 `spec_fingerprint=e6ce6acb...` 重算为 `required=true`，将 N/A 行替换成 Peekaboo+Codex 强制行；漂移发生在 queue producer 到长期运行的 supervisor worker consumer 之间，不是 CEO brief 本身遗漏。
- 实际返回：第一次 implement 已提交实现与视觉证据后被 done gate 打回；第二次因 `zhipu-glm fetch failed` 重试；第三次 implement 再次给出真实 `.jpg` 与 `visual-review.md`，但 seq=320536 仍以视觉证据行不匹配终止。
- 严重度：严重。涉及 queue/runner/done gate，已连续三次影响同一任务，并可能让所有非视觉任务被错误升级、长期占锁和重复消耗 runner。
- 根因：`projects/控制台/engine-runner.js` 的冻结 `visual_acceptance` 审计保护已于 03:37 落盘并由 `node tests/visual-acceptance.test.js` 验证，但 `supervisor-控制台` worker PID 91762 自前一日 23:25 运行，04:17 第三次执行仍加载旧模块。`ceo-worker.js` 当前只在 `activeHandles.size===0` 时检查代码修订；持续有并发任务时永远不进入换代窗口，形成“补丁落盘但运行态不生效”的系统性代际漂移。
- 处理计划：派 `repair` 将热重载改为“运行中检测变更→进入 drain、不再 claim→在途归零后优雅退出”，补连续负载回归；随后验证 stale supervisor worker 已安全换代，禁止强杀在途任务。
- 红线：不覆盖现有无关脏改动；不强杀运行中的 engine/worker；无在途归零证据不得重启；密钥不回显。

## 执行与阶段验证（2026-07-17）
- 派工证据：首次 `repair/0d3f920f` 因缺少签名 repairTicketId 被 `invalid_scope_schema` 回落到 `ceo/867c0cc0`，主管立即取消误路由；第二次 `repair/256ac06d` 已通过 `route.scoped_bypass.consumed`，但停在 `runner-singleflight`。锁文件显示 `codex-privileged` 持有者正是当前 `repair-lead/22eef214`，主管不结束就无法让维修员启动，形成自锁。
- 接管说明：为避免释放安全锁后出现双写，主管未删除 runner lock、未并行启动特权 runner；改为接管同模块最小修复。`repair/256ac06d` 在结案握手中只能转只读 no-op，不能把它声称为已执行维修。
- 代码处理：`projects/控制台/source-revision.js:39-49` 增加可测的 reload 决策；`projects/控制台/ceo-worker.js:5009-5052` 改为运行中发现源码变化即进入 drain、停止 claim，在途归零才发 `queue.worker.code_reload` 并优雅退出；`tests/worker-code-reload.test.js:50-89` 覆盖连续负载、禁止新 claim、自然退出和未变更对照。
- 专项验证：`node --check projects/控制台/source-revision.js` exit 0；`node --check projects/控制台/ceo-worker.js` exit 0；`node tests/worker-code-reload.test.js` exit 0；`node tests/visual-acceptance.test.js` exit 0；`node tests/ceo-queue-control.test.js` exit 0；三文件 `git diff --check` exit 0。
- 核心回归：`node tests/run.js --profile lean` exit 1，27 个测试文件中 26 个通过；唯一失败为既有 `tests/hardening-hooks.test.js:142`（先触发 changed-files done gate 而不是 hook gate），与本次三文件改动无关且和原任务 `test-report.json` 记录一致，未包装为通过证据。
- 运行态待核：旧 `supervisor-控制台` PID 91762 仍有合法在途 `7ec7ee7f` / engine PID 64356；按红线等待自然结束，之后核 `queue.worker.code_reload` 和新 PID，不强杀。

## 结案协议
- 本维修请求单独建单,不同故障不得混入本单。
- 完成记录必须包含:链路证据、需求传递判断、严重度、根因、处理过程、复核验证、架构判断、知识沉淀候选、剩余风险 / 下一步。
- 使用 `repair-ticket-complete` 结案;系统生成固定 HTML,飞书发送摘要卡 + 附件,元宵同步报告文档。
- 收口握手启用后,`repair-ticket-complete` 遇到 running repair 子任务会返回 `closing_pending_child`;子任务进入终态后用 `repair-ticket-child-confirm --mode read-only-no-op-confirmed` 确认,再重试结案。
- TTL 超时只生成签名主人决策卡;`force-read-only` 必须消费持久化拍板回执,并保留 unconfirmed child warning。

### 完成记录 2026-07-16T20:48:15.623Z
- status: done
- report: projects/控制台/artifacts/repair-reports/auto-20260716203059-525f5581552a5f87.html
- report_sha256: 8d9962869eb7e00e17f8cf510159fe5ed510d6d76b10bb25cf34494ff241a559

链路证据: CEO 根任务 cr-1784206833452-4c8eb313 于 engine-events.jsonl seq 302316 派 supervisor-控制台/53c05325，生产者信封 visual_acceptance.required=false；同队列三次 consumer meta 以同一 spec_fingerprint 漂移为 required=true，seq 320536 因视觉行被 done gate 终止。需求传递判断: CEO brief 无遗漏，漂移发生在 producer 到旧 supervisor worker consumer；repair 首次 0d3f920f 因缺签名误路由后取消，第二次 256ac06d 通过签名但在 runner-singleflight 被本主管锁自锁。严重度: 严重，涉及 queue/runner/done gate/特权 closeout，多次复发且有广泛假打回与锁饥饿风险。根因: 旧 supervisor worker 只在 activeHandles=0 时检查源码修订，持续负载使 visual_acceptance 冻结补丁落盘但运行态不生效；repair pre-engine child 等特权锁时不消费 cancel，结案握手亦缺少无 engine 的可信终态证明。处理: ceo-worker 改为运行中检测 revision 后 drain、停止 claim、在途归零优雅退出；补 pre-engine cancel 轮询和终态证明；repair-closeout-handshake 仅对同票、无 engine、未启动 engine 的 pre-engine child 安全取消并支持可信确认；补专项回归。复核验证: node --check 两核心文件通过；worker-code-reload、repair-closeout-handshake、visual-acceptance、ceo-queue-control 专项通过；lean 27 文件中 26 通过，唯一既有 hardening-hooks.test.js:142 单红灯如实保留。架构判断: 系统性代际漂移加特权单槽收口自锁；建议后续分离 repair-lead/repair 特权槽并把 sourceRevision/runtime generation 暴露为健康门。知识沉淀候选: 补丁落盘但常驻 worker 持续繁忙 -> 空闲才检查 revision -> 运行中检测并 drain；pre-engine 同票 child 自锁 -> 等锁循环不消费 cancel且无 engine proof -> 窄条件安全 cancel、轮询退出、结构化 no-engine 证明。剩余风险: 等待旧 supervisor 合法在途自然结束并核新 PID；不强杀。

### 完成记录 2026-07-16T21:06:36.341Z
- status: done
- report: projects/控制台/artifacts/repair-reports/auto-20260716203059-525f5581552a5f87.html
- report_sha256: 3576b5f55d2b0d22ef704ed747bfae0a12cb867b65e84b59e94cebc0406db280

链路证据:
CEO 根任务 cr-1784206833452-4c8eb313 于 projects/控制台/artifacts/engine-events.jsonl seq=302316 派给 supervisor-控制台/53c05325；终态队列文件 projects/控制台/artifacts/queues/supervisor-控制台/failed/53c05325.json 的生产者 task 信封明确 visual_acceptance.required=false，验收末行为 视觉/UI证据:not_applicable。旧 worker 消费时 projects/控制台/artifacts/engine-runs/cr-1784221196753-53c05325/meta.json 却在相同 spec_fingerprint 下变成 required=true；seq=320536 因错误视觉证据门失败。首次派 repair/0d3f920f 因 invalid_scope_schema 回落到 ceo/867c0cc0 后已取消；第二次 repair/256ac06d 通过 seq=320873 trusted scope，但 seq=320875 等待 runner-singleflight，最终 seq=322008 安全取消、seq=322026 完成只读 no-op 确认。

需求传递判断:
CEO 到 supervisor 的原始 brief 与非视觉验收没有遗漏；漂移发生在生产者信封到旧常驻 supervisor worker 的消费执行之间。维修派工首单遗漏 repairTicketId/scope proof，导致错误回落 CEO；补齐签名后的第二单需求完整，但 repair-lead 与 repair 共用 codex-privileged singleflight，形成主管持锁、维修员等锁的执行自锁。

严重度:
严重。故障涉及 queue/runner/done gate/常驻 worker 热重载与特权结案握手，同一 supervisor 队列连续三次复发，并具有非视觉任务被假打回、长期占锁、重复 runner 消耗和结案自锁的跨任务风险。

根因:
projects/控制台/ceo-worker.js 原逻辑只在 activeHandles.size===0 时检查源码修订；持续负载使 visual_acceptance 冻结保护虽然落盘，却长期未进入旧 worker 运行态。派工收口的次级根因是 repair 子任务在 pre-engine runner-singleflight 等待循环中不消费 cancel，且结案握手此前缺少无 engine 启动的可信终态证明。

处理过程:
上一轮已在 projects/控制台/source-revision.js 增加可测的 codeReloadDecision；projects/控制台/ceo-worker.js 改为运行中检测 source revision 后立即 drain、停止新 claim、在途归零后优雅退出，并在 pre-engine runner lock 等待中轮询 cancel、写入 pre_engine_cancel_confirmed。projects/控制台/repair-closeout-handshake.js 仅对同票、已安全取消、没有 engine_started_at/PID 的子任务接受 terminal_pre_engine_cancel_and_no_engine_started 证明。补 tests/worker-code-reload.test.js 与 projects/控制台/tests/repair-closeout-handshake.test.js。旧 supervisor PID 91762 未强杀，在合法在途 7ec7ee7f 完成后自然换代。

复核验证:
本轮独立复核：node --check projects/控制台/source-revision.js、ceo-worker.js、repair-closeout-handshake.js 均 exit 0；node tests/worker-code-reload.test.js、projects/控制台/tests/repair-closeout-handshake.test.js、tests/visual-acceptance.test.js、tests/ceo-queue-control.test.js、tests/repair-department.test.js 均 exit 0。事件 seq=321951 记录旧 PID 91762 code_reload，seq=322078/322079 记录新 PID 86566 spawned/start 且 sourceRevision=82067074abfa；当前 ps 仍显示 PID 86566 正常运行。握手账本 projects/控制台/artifacts/repair-closeout-handshakes/auto-20260716203059-525f5581552a5f87.json 为 ready_to_close，repair/256ac06d 终态含 pre_engine_cancel_confirmed=true、无 engine PID。

架构判断:
这是系统性代际漂移叠加特权单槽收口自锁，不是一次性截图失败。当前守卫已让常驻 worker 在连续负载下也能安全换代，并让 pre-engine 子任务可证明地取消收口。后续建议将 repair-lead 与 repair 的特权执行槽拆分，或显式设计主管让锁后的派工阶段；同时把 worker sourceRevision/runtime generation 暴露进健康门。

知识沉淀候选:
可泛化模式一：补丁已落盘但常驻 worker 持续繁忙 -> 仅空闲时检查 revision -> 运行中检测变更、停止 claim、drain 后优雅换代。可泛化模式二：主管派出的同票 pre-engine child 等待同一特权锁 -> 等锁循环不消费 cancel 且无 no-engine proof -> 窄条件轮询 cancel、结构化 pre-engine 终态证明、结案确认后幂等关闭。项目技术映射：控制台 -> source revision drain 热重载与 repair closeout handshake -> projects/控制台/source-revision.js、ceo-worker.js、repair-closeout-handshake.js。

剩余风险 / 下一步:
即时阻塞已解除，当前无未确认 repair 子任务；不需要强杀或不可逆操作。架构残余是 repair-lead 与 repair 仍共享 codex-privileged singleflight，负责人建议为控制台架构改进项，复查方法是并发主管派工烟测中确认 repair 不再长期等待同一 runner type lock。
