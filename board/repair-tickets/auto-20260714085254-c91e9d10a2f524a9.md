# 维修工单 auto-20260714085254-c91e9d10a2f524a9 · 自动维修 supervisor-控制台/397d53ad

- status: done
- created_at: 2026-07-14T08:52:54.819Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:failed

## 事件证据 / 路径
- queueAgent=supervisor-控制台
- queueId=397d53ad
- task=cr-1784018284182-397d53ad
- role=supervisor
- flow=review-loop
- project=控制台
- reason=failed
- engine_code=3
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1784018284182-397d53ad

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Codex 特权),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260714085254-c91e9d10a2f524a9.md)"
```

## 处理结果
- status: done

## 维修主管初查与派工 brief 2026-07-14

### 链路证据
- CEO 根任务 `ceo/34ff7914` 将 AHR-26..30 任务派给 `supervisor-控制台/397d53ad`；原目标与“不需要视觉时无需截图”仍在 `projects/控制台/artifacts/queues/ceo/failed/34ff7914.json:4-12`，旧 supervisor 信封则把 orchestrator 具体验收缩成通用 review-loop 行并误加视觉行，见 `projects/控制台/artifacts/queues/supervisor-控制台/failed/397d53ad.json:10-28`。这项上游 done-gate/验收传递缺陷已有同源主工单 `auto-20260714085254-fb6ba867125a78f0` 独立处理，本单不重复写码。
- 第三次 implement 如实把“完整 review 尚未执行”标为部分后，`projects/控制台/artifacts/engine-events.jsonl:52364-52374` 先记录 done-gate 打回、engine 结束与带完整原因的第一次 `queue.completed failed`；系统随即生成主工单 `...fb6ba867...`，见同文件 `:52375-52376`。
- 在第一次完成收口期间，sweeper 看到刚退出的 enginePid，未等待活跃 worker 完成 `Q.finish`，于 `projects/控制台/artifacts/engine-events.jsonl:52369-52372` 将同一 running 项重新入队；它又在 `:52381-52389` 被认领并命中 `task.resume_terminal`，随即以泛化原因 `failed` 再次结束。
- 第二次 `queue.completed failed` 位于 `projects/控制台/artifacts/engine-events.jsonl:52395`，随后创建本工单并入队维修主管，见 `:52396-52398`。`projects/控制台/artifacts/repair-auto/index.json:364-371` 与 `:742-749` 证明两个 fingerprint 指向完全相同的 sourceQueueAgent/sourceQueueId/taskId。

### 需求传递判断
- 原业务目标存在 upstream orchestrator → supervisor 具体验收丢失，但已由同源主工单独立处理。
- 本单新增的直接传递缺陷是“真实 done-gate 原因 → terminal resume 二次完成”被缩成裸 `failed`；`autoRepairFingerprint()` 又按 reason 计算 fingerprint，导致同一 source queue/task 因原因文案不同绕过冷却去重。没有项目串单，但诊断信息丢失并重复派工。

### 严重度 / 根因 / 架构风险
- 严重。问题横跨公共 queue sweeper、engine task terminal resume、queue finish 与自动维修幂等入口；一次竞态就重复生成工单和 `repair-lead` 特权任务，存在重复写码、重复结案/通知、维修队列刷屏及共享工作树冲突风险。
- 根因一：`projects/控制台/ceo-worker.js:4404-4509` 仅对“没有 enginePid 且心跳新鲜”保留 running；当 enginePid 已登记、进程刚退出但心跳仍新鲜时，直接按 orphan 释放锁并重排，和同一 worker 的正常完成收口竞态。
- 根因二：`projects/控制台/ceo-worker.js:1831-1832` 会让带 `recovered_at/recovered_reason` 的队列项复用原 taskId；`shared/engine/engine.js:147-150` 对失败终态返回 `reason=failed`，从而产生第二次泛化终态。
- 根因三：`projects/控制台/ceo-worker.js:3512-3518` 的自动维修 fingerprint 只含 queueAgent/role/reason，不含稳定 incident key（queueId/taskId）；同一事件的详细原因与泛化原因得到不同 fingerprint。当前全量事件/repair-auto 状态抽样只发现这一组重复，但公共入口影响面广，必须同时修竞态守卫和事故级去重。

### repair 执行范围与验收
- 先读本工单与当前 scoped diff；`projects/控制台/ceo-worker.js` 已含其他任务的未提交 hunk，必须保留，不得回退、覆盖或格式化重写。不得改 `shared/engine/done-gate.js`、`projects/控制台/engine-runner.js` 及同源主工单范围。
- 最小修复 sweeper 竞态：enginePid 刚退出但 running heartbeat/worker 收口仍新鲜时，不得立即 requeue；应保留 running 并留下明确 keepalive/settling 事件，超过既有心跳/结算窗口后仍允许真正 orphan 恢复。若 taskstore 已是 terminal，也不得通过 resume 重复执行或重复完成。
- 增加事故级自动维修幂等：同一 `queueAgent + queueId + taskId` 在冷却期内只能有一张 auto repair ticket，即使 reason 从详细错误退化为 `failed`；不同 queue/task 的同类原因不得被误合并。保留现有 reason fingerprint 用于问题模式节流，但不能让它成为唯一事故键。
- 补回归：复现“taskstore failed + 已登记但刚退出的 enginePid + 新鲜 heartbeat”的 running 项，sweep 后不得进入 queued、不得再次 `task.resume_terminal/queue.completed`；同 source queue/task 先用详细原因、再用 `failed` 调 `openAutoRepairTicket`，第二次须返回 null 并 emit 可核 dedupe 事件；不同 taskId 仍可独立开单。
- 至少运行 `node --check projects/控制台/ceo-worker.js`、`node tests/stale-running-heartbeat.test.js`、`node tests/notify-severity-tiers.test.js`、与新增/调整的精确测试；再运行 `node tests/crash-recovery-idempotency.test.js` 确认真正 crash resume 仍不重复副作用。测试必须全为 exit 0，`git diff --check` 通过。
- 禁止修改 queues/engine-runs 历史、两张既有工单身份、repair-auto 历史条目；禁止重启/杀进程/重派原业务任务/自行 `repair-ticket-complete`。高危不可逆操作停下给主人确认；密钥不回显。完成后在 repair 队列结果列 changed files、scoped diff、命令退出码和残余风险，等待主管独立复核。

### 维修执行与主管独立复核 2026-07-14

- 已通过 `secretary-tools queue-enqueue` 派给 `repair/d6be4147`，派工与认领证据见 `projects/控制台/artifacts/engine-events.jsonl:54892`、`:54896-54898`；因 `repair-lead` 与 `repair` 共用 `codex-privileged` singleflight，队列维修员预执行阶段会等待主管释放 runner，限定补丁改由独立 repair 接管槽执行。已向 `repair/d6be4147` 注入只读复核 steer，见其 running 队列记录的 `steer` 字段和 `queue.steered` 事件，防止主管退出后重复改写。
- 实际修复位于 `projects/控制台/ceo-worker.js:3521-3553`、`:3592-3611`、`:3679-3703`：增加稳定 incident identity（`queueAgent + queueId + taskId`）、兼容扫描旧 repair-auto state，并保留 reason fingerprint 作为诊断元数据；同事故在冷却期内不再因 reason 文案变化重复开单。
- sweeper 修复位于 `projects/控制台/ceo-worker.js:4454-4547`：已登记 enginePid 刚退出但 heartbeat 新鲜时只记可审计 settling keepalive，不立即 requeue；窗口过期且 taskstore 已终态时直接结算 queue，禁止再走 `task.resume_terminal`。
- 精确回归位于 `tests/repair-incident-idempotency.test.js:21-160`，覆盖新鲜 settling、过期 terminal reconciliation、同事故详细原因→裸 `failed` 去重、不同 task 独立开单和双 fingerprint 状态记录。
- 主管实际复跑均为 exit 0：`node --check projects/控制台/ceo-worker.js`；`node tests/repair-incident-idempotency.test.js`；`node tests/stale-running-heartbeat.test.js`；`node tests/notify-severity-tiers.test.js`；`node tests/crash-recovery-idempotency.test.js`；`git diff --check -- projects/控制台/ceo-worker.js tests/stale-running-heartbeat.test.js tests/crash-recovery-idempotency.test.js`；新测试独立行尾空白检查也为 exit 0。
- 全仓裸 `git diff --check` 另报 `projects/控制台/brief.md:15509` 的并发任务既有行尾空格；该文件不在本工单授权范围，未越权修改。任务 scoped diff 无 whitespace error，且未修改 `shared/engine/done-gate.js`、`projects/控制台/engine-runner.js`、历史 queues/engine-runs/repair-auto，也未重启、杀进程或重派原业务任务。

### 结构化验收表（主管复核）

| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 完成 | `projects/控制台/artifacts/engine-events.jsonl:52364-52398`; `projects/控制台/artifacts/engine-events.jsonl:54892`; `projects/控制台/artifacts/engine-events.jsonl:56146-56152`; `projects/控制台/ceo-worker.js:3521-3553`; `projects/控制台/ceo-worker.js:4454-4547`; `tests/repair-incident-idempotency.test.js:21-160`; `projects/控制台/artifacts/repair-reports/auto-20260714085254-c91e9d10a2f524a9.report.json:3-10`; `board/repair-tickets/auto-20260714085254-c91e9d10a2f524a9.md:3` | 已完成全链路核查、严重度分级、repair/d6be4147 派工与独立接管、主管 scoped diff/专项测试复核及唯一入口结案；工单为 done，HTML SHA 匹配且缺失字段为空，飞书和元宵均发送成功。全仓 diff check 的并发 brief 行尾空格已在剩余风险中分账，本工单范围检查通过。 |

## 结案协议
- 本维修请求单独建单,不同故障不得混入本单。
- 完成记录必须包含:链路证据、需求传递判断、严重度、根因、处理过程、复核验证、架构判断、知识沉淀候选、剩余风险 / 下一步。
- 使用 `repair-ticket-complete` 结案;系统生成固定 HTML,飞书发送摘要卡 + 附件,元宵同步报告文档。

### 完成记录 2026-07-14T09:32:34.905Z
- status: done
- report: projects/控制台/artifacts/repair-reports/auto-20260714085254-c91e9d10a2f524a9.html
- report_sha256: bf2599641585df3cc9978440749027b745d71535b4d95fb4433bb9b7c2e35559

链路证据: CEO 34ff7914 将 AHR-26..30 交给 supervisor-控制台 397d53ad；engine-events.jsonl:52364-52376 记录真实 done-gate 原因、第一次 queue.completed 和主工单，:52381-52398 记录 sweeper 重排、task.resume_terminal、裸 failed 第二次完成及本工单；repair-auto/index.json:364-371 与 :742-749 证明两单同 source queue/task。repair d6be4147 的派工与认领见 engine-events.jsonl:54892、:54896-54898。；需求传递判断: 原业务 orchestrator→supervisor 的具体验收丢失已由同源主工单 auto-20260714085254-fb6ba867125a78f0 独立处理；本单直接缺陷是第一次真实 done-gate 原因经 terminal resume 被缩成裸 failed，导致诊断信息丢失并重复维修派工，无项目串单。；严重度: 严重。故障横跨公共 sweeper、engine terminal resume、queue finish 与 auto repair 幂等，可能对任意队列制造双完成、双工单、重复通知和共享工作树冲突。；根因: ceo-worker.js 旧 sweeper 在已登记 enginePid 刚退出但 heartbeat 仍新鲜时立即按 orphan 重排，与活跃 worker 的 Q.finish 收口竞态；复用原 taskId 后 engine 对既有 failed 终态只返回泛化 failed；autoRepairFingerprint 仅按 agent、role、reason，缺少 queueId/taskId 稳定事故键。；处理过程: 已派 repair/d6be4147；因 repair-lead 与 repair 共用 codex-privileged singleflight 形成预执行自锁，限定补丁由独立 repair 接管槽完成，并向原维修单注入只读复核 steer。ceo-worker.js:3521-3553、3592-3611、3679-3703 增加兼容旧状态的事故级去重和双 fingerprint 元数据；:4454-4547 增加 fresh settling 守卫及过期 terminal taskstore 直接结算；新增 tests/repair-incident-idempotency.test.js。；复核验证: 主管逐段核对 scoped diff 与文件内容；node --check projects/控制台/ceo-worker.js exit 0；node tests/repair-incident-idempotency.test.js exit 0；node tests/stale-running-heartbeat.test.js exit 0；node tests/notify-severity-tiers.test.js exit 0；node tests/crash-recovery-idempotency.test.js exit 0；任务范围 git diff --check exit 0，新测试行尾检查 exit 0。结构化验收：链路核查、严重度、必要派工、独立复核与唯一入口结案已完成；本命令负责置 done、生成 HTML 并尝试通知。；架构判断: 系统性问题，已在恢复守卫和事故身份两层修根因。附带发现 repair-lead→repair 共用特权 runner singleflight 会形成主管等待下属的自锁，建议另开架构改进：为维修主管派工提供独立执行槽或可观测的让锁/接管协议。；知识沉淀候选: 泛化判断=可泛化模式；问题模式=终态 engine 退出与 queue 收口竞态导致同一 task 被恢复并以退化原因二次完成；根因=仅看 PID 不看新鲜 heartbeat/taskstore 终态，且自动工单按可变 reason 而非稳定 incident 去重；解法=设置 settling 窗口、终态直接 reconcile、以 queueAgent+queueId+taskId 做事故键并保留 reason fingerprint 诊断、补竞态和幂等回归。项目技术映射=控制台→队列恢复与自动维修幂等→projects/控制台/ceo-worker.js 与 tests/repair-incident-idempotency.test.js。；剩余风险 / 下一步: 未重启或杀进程，源码修复由后续新 worker 装载；全仓裸 git diff --check 仍被并发任务既有 projects/控制台/brief.md:15509 行尾空格阻断，本工单 scoped diff 已通过且未越权改该文件。repair/d6be4147 仍在 pre-engine wait，但已 steer 为只读核验，避免主管退出后重复改写；飞书和元宵投递结果以本次唯一入口的幂等回执为准。
