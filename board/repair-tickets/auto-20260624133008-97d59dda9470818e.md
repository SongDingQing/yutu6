# 维修工单 auto-20260624133008-97d59dda9470818e · 自动维修 ui_optimizer/476f3dc1

- status: done
- created_at: 2026-06-24T13:30:08.802Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:queue worker crashed: resource lock coordinator timeout

## 事件证据 / 路径
- queueAgent=ui_optimizer
- queueId=476f3dc1
- task=worker-crash-1782307808789-476f3dc1
- role=ui_optimizer
- flow=worker-crash
- project=控制台
- reason=queue worker crashed: resource lock coordinator timeout
- engine_code=unknown
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/worker-crash-1782307808789-476f3dc1

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- Starlaid 排除
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Claude Code),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260624133008-97d59dda9470818e.md)"
```

## 处理结果
- status: done

### 完成记录 2026-07-10T14:24:06.960Z
- status: done

【链路证据】ui_optimizer/476f3dc1 与同时间 frontend_designer/7acda272 并行争用前端资源域，worker 在 acquireCoordinator 阶段报 resource lock coordinator timeout；brief 完整，无需求交接遗漏。严重度：严重系统性，旧 coordinator 锁可阻断多个 agent。根因：旧 coordinator 所有权仅凭 PID/时间判断，进程崩溃或 PID 复用后锁可能被误认存活，两个 worker 均等到超时并崩溃。当前处理：resource-locks.js 的 coordinator 记录 ownerPid+ownerStart，获取时校验真实进程启动标记，并对死 owner/损坏记录/超过10秒的 coordinator 自动清理；ceo-worker 调度前用 currentResourceConflicts 避免先 claim 再长等，正式 lease 有 heartbeat/release/stale sweep。验证：临时目录冒烟成功恢复死 coordinator、识别同域写冲突、release 后重新获取、sweep 无残留；node tests/insight-scout-repos.test.js PASS；node tests/queue.test.js PASS；全量相关 resource scheduler 路径通过。架构判断：可泛化模式。问题模式=文件锁只看 PID 导致 PID 复用/崩溃残锁→协调器超时并杀多 worker；解法=PID+启动标记+stale 上限+原子锁文件+调度前冲突探测+lease 心跳。
