# 维修工单 auto-20260701214738-c8046cb60e0dde76 · 自动维修 supervisor-控制台/64cf1296

- status: done
- created_at: 2026-07-01T21:47:38.504Z
- source: 自动故障触发
- priority: high

## 问题
队列任务失败,系统自动开维修工单。失败原因:done_gate_failed: 硬回归测试覆盖不足: queue_merge_integrity: implementation 缺少合并后任务数/需求保留/状态迁移/幂等等硬证据摘要; queue_merge_integrity: review 未逐项核合并完整性硬证据

## 事件证据 / 路径
- queueAgent=supervisor-控制台
- queueId=64cf1296
- task=cr-1782942016269-64cf1296
- role=supervisor
- flow=review-loop
- project=控制台
- reason=done_gate_failed: 硬回归测试覆盖不足: queue_merge_integrity: implementation 缺少合并后任务数/需求保留/状态迁移/幂等等硬证据摘要; queue_merge_integrity: review 未逐项核合并完整性硬证据
- engine_code=3
- engine_signal=none
- events=projects/控制台/artifacts/engine-events.jsonl
- runs=projects/控制台/artifacts/engine-runs/cr-1782942016269-64cf1296

## 期望结果
维修主管读取工单后先判断根因、需求传递遗漏与架构风险;能安全修则修并验证,需要执行时分派 Codex 维修员;高危/不可逆操作停止并请求主人确认;完成后写回工单并通知主人。

## 红线
- 高危/不可逆操作必须先给主人确认
- 密钥/token/cookie/私钥不回显、不写日志
- Starlaid 排除
- 不破现有功能; 能验证就写验证结果

## 维修部门消费方式(v3 主管先行)
`repair-lead` 是维修主管队列(Codex 特权),所有工单默认先进主管:链路核查、根因分析、严重度分级、必要时分派 `repair` Codex 维修员执行。紧急时仍可由独立 Codex 特权会话手动接管。推荐手动命令:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -C /Users/yutu6/玉兔6工作区 "$(cat /Users/yutu6/玉兔6工作区/board/repair-tickets/auto-20260701214738-c8046cb60e0dde76.md)"
```

## 处理结果
- status: done

### 完成记录 2026-07-01T21:53:39.507Z
- status: done

链路证据:
- CEO/root `b765f60b` 派到 `supervisor-控制台/64cf1296`, engine task `cr-1782942016269-64cf1296`;失败队列文件记录 role=supervisor、flow=review-loop、rootQueueAgent=ceo/rootTaskId=cr-1782940637744-b765f60b、验收表三行与红线,见 `projects/控制台/artifacts/queues/supervisor-控制台/failed/64cf1296.json:1`、`:24`、`:62`、`:86`。
- implement brief 要求最后输出 implementation/receipt/acceptance_table/logic_chain,见 `projects/控制台/artifacts/engine-runs/cr-1782942016269-64cf1296/implement-1/task.md:76`。实现结果声明补齐 queue_merge_integrity,见 `.../implement-1/result.md:5`;其证据 artifact 在 `projects/控制台/artifacts/self-reflection-optimizer/ui-optimizer-self-reflection-b765f60b-64cf1296-current-1782942016269-20260702.md:63` 至 `:69` 明确列出 merged_from/reviewChecklist、planned_groups、queued_after、任务数减少、需求保留、状态迁移、幂等、running 只读。
- review brief 要求逐项核验 implementation.logic_chain/acceptance_table/changed_files/测试证据,见 `projects/控制台/artifacts/engine-runs/cr-1782942016269-64cf1296/review-2/task.md:60` 至 `:72`;review 结果 pass=true 并列出验收表/证据/测试复核,见 `.../review-2/result.md:1` 至 `:2`。
- 事件日志显示 review 已收敛并走到 done,随后 done_gate.blocked: seq 129471-129475,失败原因为 queue_merge_integrity 缺少硬证据摘要/复核逐项核验;随后自动创建本维修工单并派给 repair-lead,见 `projects/控制台/artifacts/engine-events.jsonl:31571` 至 `:31579`。
需求传递判断:
- 未发现 secretary/CEO/supervisor/worker_code 之间遗漏老板需求: scope、Starlaid 排除、密钥不回显、结构化验收表、去重与视觉证据要求均传到 implement/review task。
- 实际断点在 done gate 机制: `shared/engine/done-gate.js` 的 `validateHardRegressionCoverage` 原先只看 implementation/review 顶层 JSON 文本,不读取已引用的证据 artifact;因此 artifact 中已有逐项硬证据时仍被判缺摘要。
严重度:
- 严重。依据: 涉及 `shared/engine/done-gate.js` 公共完成门禁,影响 review-loop/queue_merge_integrity 类任务;会造成假阴性打回、CEO/root 项目路由失败和自动维修噪音。`repair-policy-check` 也给出 severe/global_system_trace。
根因:
- 代码路径 `shared/engine/done-gate.js::validateHardRegressionCoverage` 的 hard regression 文本收集范围过窄:收集 implementation.summary/tests/logic 与 review.notes/verification 文本,但未使用已有 `pointerSnippet` 读取工作区内 `.md/.json/.txt` 证据文件片段;真实任务把 queue merge 完整性摘要放在 artifact 中并由 logic_chain/review 指针引用,门禁没有水合这些指针。
处理:
- 未派 repair 维修员;本次是小范围、低风险机制 patch,主管直接修复。
- 在 `shared/engine/done-gate.js:241` 增加 hard regression evidence artifact snippet 读取,只允许工作区内 `.md/.json/.jsonl/.txt`,跳过 `tests/` 源码路径,保留 Starlaid/上级目录拒绝与 1MB 限制。
- 在 `shared/engine/done-gate.js:887` 起把 implementation receipt/artifacts 与 review checked/evidence 中引用的证据片段纳入 hard regression 核验,仍要求 implementation/review 都显式出现 `node tests/queue-organizer.test.js` 和 `node tests/ceo-queue-control.test.js`。
- 在 `tests/done-gate.test.js:313` 添加回归:硬证据摘要只在被引用 artifact 内时应通过,且 review 也必须引用该 artifact 与两个硬回归测试。
复核验证:
- `node tests/done-gate.test.js` exit 0,输出 `{"pass":true,"suite":"done-gate"}`。
- `node tests/queue-organizer.test.js` exit 0,输出 `{"pass":true,"suite":"queue-organizer"}`。
- `node tests/ceo-queue-control.test.js` exit 0,输出 `{"pass":true,"suite":"ceo-queue-control"}`。
- 原失败 task vars 重放: `DoneGate.validateHardRegressionCoverage(...)` 返回 `ok:true`;将内存中历史 task.state 临时置为 done 后 `validateReviewLoopCompletion(...)` 返回 `ok:true`。原落盘 task 仍因历史状态 `state=failed` 被完整门禁拒绝,这是历史终态字段,不是 hard regression 仍失败。
- `node --check shared/engine/done-gate.js` exit 0; `node --check tests/done-gate.test.js` exit 0; `git diff --check -- shared/engine/done-gate.js tests/done-gate.test.js` exit 0。
- `node tests/run.js` 曾跑到 `auto-page-review.test.js` 之前全部 PASS,随后超过 90 秒无新输出并被 Ctrl-C 中断(exit 130);不作为通过证据,记录为剩余聚合入口耗时/悬挂风险。
架构判断:
- 系统性门禁证据水合缺口,已在 done gate 层补机制和回归测试。当前修复不修改队列运行语义、不重启服务、不触碰 Starlaid/密钥/登录授权。后续建议单独评估 `tests/run.js` 后段无输出超时,给聚合测试入口加 per-test timeout/当前测试名心跳,避免维修验证悬挂。
知识沉淀候选:
- 可泛化模式: agent 将硬证据写入 artifact 并在 structured output 引用路径,done gate 只看顶层摘要导致假阴性打回。
- 根因: 完成门禁证据解析没有水合可核 artifact 指针。
- 解法: 对 hard regression 这类需要结构化硬证据的规则,在保持命令显式要求的同时读取受限类型、受限大小、工作区内的证据文件片段;跳过测试源码避免把源码内容误当执行摘要;补真实失败形态回归。
- 项目技术映射: 控制台 -> review-loop done gate / queue_merge_integrity -> `shared/engine/done-gate.js`, `tests/done-gate.test.js`。
结构化验收表:
| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: 主管完成链路核查、严重度分级、必要派工、复核和结案; 工单 status 变 done; 尝试飞书通知主人。 | 完成 | `projects/控制台/artifacts/engine-events.jsonl:31571`; `projects/控制台/artifacts/engine-events.jsonl:31575`; `shared/engine/done-gate.js:241`; `tests/done-gate.test.js:313`; commands: node tests/done-gate.test.js/queue-organizer/ceo-queue-control exit 0; this repair-ticket-complete command | 已完成链路核查、严重度=严重、直接最小机制修复、复核验证; repair-ticket-complete 会更新工单 status 并触发通知尝试。 |
