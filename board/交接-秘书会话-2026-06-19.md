# 玉兔6 · 秘书会话交接稿(2026-06-19)

> 把这份整段交给新会话即可继承。新会话角色 = **秘书**:把老板(Joe Song)的话路由成任务、写进队列、做诊断与文件级落地;不替老板做登录/转账/授权这类操作。

## 你是谁、系统是什么
- 老板:Joe Song(yutu6,songchengzuo@hotmail.com)。在 Mac mini 上搭「玉兔6」——多智能体、多模型、"文件=共享大脑"系统。工作区根:`~/玉兔6工作区`(沙箱里映射为 `/sessions/.../mnt/玉兔6工作区`)。U盘「月饼」= `/Volumes/月饼`。
- 四层组织:老板 → **CEO**(原名总管,日常对接=Claude Code CLI)→ 主管 → 员工;靠 need-to-know 文件信封(task.md/result.md/questions.md)协作。
- 你(本会话)是**秘书**:文件/队列/代码/分析直接做;唯独"重启 Mac 常驻进程、登录、授权"这类需老板手动。
- 双执行模型:你的 `bash` 跑在独立 Linux 沙箱(能读写工作区挂载,**够不到 Mac 进程/launchctl/Mac localhost**);Codex 在 Mac 上执行;真 CLI(claude/codex)只在 Mac 上有。

## 关键路径
- 引擎核心:`shared/engine/`(yaml-lite/condition/eventlog/taskstore/cli-runner/engine/agents/**queue.js**)。零依赖 Node,spawnSync 跑 CLI。
- 队列核心:`shared/engine/queue.js`(文件系统队列,原子 mv;enqueue/list/jump/reorder/steer/cancel/claim/complete)。每 agent 一条队列,文件名 `PP-序号-id.json`。
- 智能体定义:`shared/agents/`(orchestrator=CEO、supervisor、quality-ops、governance、ui-optimizer)。
- 控制台 WebUI(服务在 Mac 上 launchd 常驻,端口 41218):`projects/控制台/`
  - `server.js`、`ceo-worker.js`(CEO 队列消费者)、`engine-runner.js`(每任务 fresh spawn)、`config.json`(claude runner=`claude -p --dangerously-skip-permissions` 完全权限)。
  - 页面:`public/index.html`、`public/workspace.html`(脑图+队列面板)、`public/control-room.html`。
  - 队列目录:`projects/控制台/artifacts/queues/ceo/`(running/done/failed/canceled + `.worker.pid`)。
  - 事件日志:`artifacts/engine-events.jsonl`(页面轮询它点亮工位)。
  - launchd:`com.yutu6.console`,KeepAlive+RunAtLoad,代理 127.0.0.1:10808。重启命令(只能老板在 Mac 终端跑):`launchctl kickstart -k gui/$(id -u)/com.yutu6.console`。
- 自优化循环:`shared/agents/ui-optimizer/`(loop.sh 生产者按 10 轮、dev-worker.sh 消费者、notify-feishu.sh、架构 v2 文档)。四智能体:优化师 claude 挑错+出测试路线 / Peekaboo 截图+测试点击 / 开发 codex 异步改 / Hermes 飞书报信。
- 知识库:`knowledge/`(embed_provider/ingest/query/schema/build.sh,优雅降级到 FTS5)。归档 38 个旧 codex skills 在 `knowledge/corpus/codex-skills/`。
- Simulaid 游戏项目:`projects/Simulaid/`(团结引擎 1.8.5,结论=不换引擎)。
- Peekaboo 实际路径:`/Users/yutu6/.local/bin/peekaboo`(npm 装)。

## 当前最重要的现状(刚排障完)
- **正卡在这一件、只能老板做**:Mac 上 `claude` 命令行**登录态过期**(报 `Not logged in · Please run /login`)。CEO 拆解步骤靠 claude,一掉线整条 CEO 队列就退出码 1 全失败。
  - 老板要做:终端 `claude` → `/login` → 浏览器登录完 → 告诉你。然后你**重新入队**那两个被打断的任务 + 新任务 #21,worker 就能跑通。
- 本轮已修进代码(下次任务/worker 自动生效,无需重启):
  1. `shared/engine/cli-runner.js`:runner 退出码非 0 且 stderr 空时,**回退抓 stdout 尾部**当原因(就是这条让我们看到"未登录");加了 signal/超时分支。
  2. `projects/控制台/ceo-worker.js`:worker 启动先 `sweepOrphans()`——pkill 遗留 engine-runner + 把卡在 running 的任务标记 failed,杜绝"重启后两路 claude 并发打架"。
  3. 卡死的孤儿任务 554f9cdb(工作区脑图,**其实已跑通+主管评审 PASS**)已清理标记完成。
- 队列里两个任务(循环状态显示、队列收尾通用化)登录后需**重新入队**(原 payload 在 `artifacts/queues/ceo/failed/` 里可读回)。

## 待办(任务清单)
- #21【新】引擎/队列报错显示在 WebUI 页面:把 task.failed/node.fail/queue.completed(ok:false)/queue.swept 做成红色失败卡(任务/节点/runner/原因/时间)。后端"报错可见"已修好,缺前端面板。
- #19 循环/定时任务状态集中显示:loop.sh 每轮写 STATUS.json + WebUI 面板。
- #18 队列收尾通用化:端到端验证 + 下沉为所有 agent 通用 + 面板打磨(reorder/插队/引导/取消)。
- #5 知识库 vector/GraphRAG 升级(等另一台电脑出 embedding endpoint)。
- 长期:多智能体自动创建+CLI 连接、硬化/监管闭环。

## 老板定的规矩(务必照办)
- 密钥只本机用、**绝不外传或贴进聊天**;脚本处理密钥不回显值。统一保险库 `~/.config/yutu6-secrets`,加密备份到夸克网盘,桌面原件已删。
- OAuth/扫码/2FA/各平台登录 → 只列清单交老板**手动**,你不碰。
- **Starlaid 全程硬排除**。
- 旧机用户名 `yutu`、新机 `yutu6`(repath 时注意)。
- 出错或拿不准就**停下问老板**。
- 老板偏好:**极简直接,少废话**。能删的字就删。
- 老板明确希望:你能直接做的(写文件、进队列、调顺序、诊断)就别老卡他确认;真做不了的(Mac 进程/登录/授权)才找他。

## 给新会话的第一步建议
1. 读本稿 + `board/搭建路线图.md` + `projects/控制台/status.md`。
2. 问老板 claude 是否已 `/login`;已登录则重新入队失败任务 + #21,然后盯队列(`artifacts/queues/ceo/` running→done)确认跑通。
