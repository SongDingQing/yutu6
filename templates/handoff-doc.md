# Agent handoff 文档模板

> 用途:跨会话或跨 agent 交接时,把当前状态、阻塞点、下一步和证据压成可恢复的最小文档。
> 边界:这是纯 Markdown 模板;不引入依赖、不安装 hook、不改 runner、不替代 queue/taskstore/eventlog/status 的权威状态。

## 0. 基本信息

| 字段 | 填写 |
|---|---|
| projectId |  |
| taskId / rootTaskId |  |
| from_agent |  |
| to_agent |  |
| queueAgent / queueId |  |
| handoff_at |  |
| handoff_reason |  |
| scope |  |

## 1. 当前现状

- 当前状态:
- 已完成:
- 已修改文件:
- 未修改但相关的输入/参考:
- 当前权威状态来源:

## 2. 阻塞与风险

| 阻塞/风险 | 影响 | 谁处理 | 停止条件/升级条件 | 证据 |
|---|---|---|---|---|
|  |  |  |  |  |

必须显式写清:

- 密钥、token、cookie、验证码不复制进交接文档。
- 登录、OAuth、扫码、2FA、系统授权交给主人手动。
- 未登记或未获授权的项目与路径一律不碰。

## 3. 下一步

| 顺序 | 动作 | 目标文件/系统 | 验证方式 | 完成证据 |
|---|---|---|---|---|
| 1 |  |  |  |  |
| 2 |  |  |  |  |
| 3 |  |  |  |  |

## 4. 变更与 commit 边界

- 本次 scoped 变更范围:
- 不应进入本次变更的文件/目录:
- 如果任务要求 commit,commit 只包含上述 scoped 变更:
- 如果任务不要求 commit,证据使用 `git diff -- <path>`、文件行号、命令输出或截图路径:

## 5. 证据清单

| 声称 | 证据位置(文件:行 / git diff / 命令 exit 0 / 截图路径) | 备注 |
|---|---|---|
|  |  |  |

规则:

- 没有证据不得声称完成。
- 证据必须能指回真实文件、命令、截图或事件日志。
- 证据与声称不对应时,按未完成处理。

## 6. 交接验收表

按 `templates/structured-acceptance-table.md` 的列头逐行填写。不要用段落声明替代表格。

| 要点 | 完成状态(完成/部分/未完成) | 证据位置(文件:行 / git diff / 截图路径) | 备注 |
|---|---|---|---|
| 任务验收: handoff 文档覆盖现状/阻塞/下一步 | 未完成 |  |  |
| 任务验收: scoped 变更或 scoped commit 边界已写清 | 未完成 |  |  |
| 任务验收: 无证据不得声称完成 | 未完成 |  |  |
