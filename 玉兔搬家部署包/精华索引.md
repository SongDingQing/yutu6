# 玉兔搬家 · 精华索引

> 换机不丢的积累:在哪、是什么、新机怎么启用。完整原始资料见本包 `迁移记录/`(00–08)。
> 密钥真值不在本包,在单独的 `MacMini-Secrets-PRIVATE` 目录。

## 1. Hermes 改版(默认对话 runner)
- 源码:`/Users/yutu/.hermes/hermes-agent`(基于 `NousResearch/hermes-agent`,MIT,`main`)
- 改动:5 个文件(`feishu.py` / `run.py` / `plugins.py` / `transcription_tools.py` / 测试)→ 做成 patch
- 额外功能:飞书消息队列/忙碌提示、Codex handoff、飞书确认卡片、语音唤醒发声、语音同步飞书、ASR 热词纠错、Brave Search、定时任务、文件/APK 交付意识(详见 `迁移记录/08` §2.2)
- 复刻:clone 官方 + 应用 patch → 恢复 plugins / voice-wake / `.codex` modules&skills → 重配密钥

## 2. 自定义 skills(41 个)+ 模块索引
- skills:`~/.codex/skills`(架构 `SKILL_ARCHITECTURE.md`)—— 玉龙/玉灵/玉凤/玉鼠等 wrapper、`simulaid-*`、`yuanxiao-*`、`嫦娥改装计划` 等(清单见 `迁移记录/08` §4.3)
- 模块:`~/.codex/modules`(`INDEX.md` + `registry.json` + `module_lookup.py`)—— multi-agent-collaboration-contract、hermes-yutu-voice-bridge、chang-e-android-control-plane、simulaid-* 等
- 全局只一个指令补齐入口:`instruction-expansion-router`
- 新机:拷 `~/.codex/modules` 与 `~/.codex/skills` 到同位置

## 3. 项目仓库
- `SongDingQing/YuanXiao`、`SongDingQing/Simulaid`、Hermes(NousResearch fork)、`SongDingQing/Starlaid`(**默认硬排除**)
- `agent-game-research-watch`:保留 `seen-repos.json` + 最近 reports

## 4. 优秀外部参考库清单
- 见 `迁移记录/08` §7(6 大类几十个库:状态机 / 上下文检索 / 多 agent 编排 / 安全审计 / UI 回归 / 游戏 Unity / 浏览器知识抓取)
- 原则:**不打包**,授权 GitHub 后按需 clone 到 `~/Projects/reference-repos`

## 5. 失败案例 / 避坑
- 见 `workspace-模板/shared/reference/失败案例库.md`(十几条,已纳入监管人员误区库)

## 6. 密钥与配置(只记位置,真值在私密目录、不在本包)
- 私密目录:`MacMini-Secrets-PRIVATE-2026-06-18`(`secrets.env` + 密钥清单 + 需手动重连清单)
- 代理:`代理/`(v2rayN)
- 还原与授权步骤:见 `新机迁移执行清单.md`
