# 新机迁移执行清单(玉兔搬家)

> 在新 Mac mini 上照着勾。**私密凭证在单独的 `MacMini-Secrets-PRIVATE` 目录(不在本包)**,只拷到新机本地、别外发。
> 完整原始指南见本包 `迁移记录/`(00–08);凭证索引见私密目录的《密钥清单.md》《需手动重新连接清单.md》。

## A. 装环境(自动)
1. 解压本包 → 双击 `安装.command`(装 Homebrew / Node / Git / Python / Claude Code / Codex + 建工作区)。
2. 安装中按提示选装:**代理**(放了 v2rayN 才问)、**开发软件**(Android Studio + Unity Hub)。
3. **团结引擎**到 unity.cn 自行下载安装(无 Homebrew 包)。

## B. 还原凭证(手动,从私密目录)
- 把 `MacMini-Secrets-PRIVATE-2026-06-18/` 拷到新机本地(别外发、别进 Git)。
- `secrets.env` 按每行「来源文件」分发回:`~/.hermes/.env`、`~/Projects/YuanXiao/ops/config/yuanxiao.env`。
- SSH key(`id_ed25519_starlaid_github` 等)→ `~/.ssh/` 并 `chmod 600`。

## C. 重新登录 / 授权(必须本人 · 见《需手动重新连接清单》)
- `claude auth login` · `codex login` · `gh auth login`(或新机生成 SSH key 加 GitHub)
- 飞书开放平台、Gmail connector、Brave 控制台、Quark 扫码、Apple Developer、Unity/Tuanjie、`npm login`。

## D. 迁移代码与能力(见 `迁移记录/08`)
- clone:`SongDingQing/YuanXiao` → `~/Projects/YuanXiao`;`SongDingQing/Simulaid` → `~/Simulaid`;Hermes(NousResearch 官方或你的私有 fork)→ `~/.hermes/hermes-agent`,再应用本机 **5 个文件的 patch**(feishu.py / run.py / plugins.py / transcription_tools.py / 测试)。
- 拷到新机同位置:`~/.codex/modules`、`~/.codex/skills`(41 个 skill)、`~/.hermes/plugins`、`~/.hermes/voice-wake`。
- **Starlaid 默认不进自动研究(硬排除)**。

## E. 验证(见 `迁移记录/08` §10.4)
Hermes 启动 · 飞书收发 · 语音播报后能再唤醒 · 忙碌时新消息进队列不打断 · Codex handoff 能建任务回传 · YuanXiao 能构建 APK · Simulaid 能开 Unity/Tuanjie。

## 参考
- 优秀外部参考库清单:`迁移记录/08` §7(授权 GitHub 后按需 clone 到 `~/Projects/reference-repos`,**不打包**)。
- 失败案例避坑:`workspace-模板/shared/reference/失败案例库.md`(已接入监管人员误区库)。
- 精华总览:`精华索引.md`。
