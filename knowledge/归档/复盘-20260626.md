# 每日复盘 · 2026-06-26(覆盖 2026-06-25T05:00 — 2026-06-26T05:00 北京时间 / UTC 2026-06-24T21:00 — 2026-06-25T21:00)

> 监管/复盘智能体每日定时复盘。对事不对 agent、改流程不追责。规则要可执行、可验证。
> 证据来源:`projects/控制台/artifacts/engine-events.jsonl`(窗口内 5298 事件,逐类计数+失败/重试/门禁/维修事件逐条抽取)、`board/repair-tickets/`(当天 2 张工单 + 完成记录)、`knowledge/归档/硬化建议-20260626.md`、`knowledge/归档/复盘-20260625.md`(承接 NR1/NR8/NR9/NR10/NR5)、`git log`(窗口内落地)、相关源码/配置定位。
> 红线遵守:Starlaid 未读取/未评估/未修改(注:Simulaid≠Starlaid,Simulaid 是在编 Unity 项目,在范围内);无密钥/token/cookie 回显;无登录/授权操作;只复盘+沉淀,未做任何业务修复或不可逆删除。

## 0. 当天概况(24h 窗口事件计数)

| 指标 | 今日(06-26 窗口) | 昨日(06-25 窗口) | 走向 |
|---|---|---|---|
| `task.done` | 47 | 36 | ↑ 有真实吞吐(非冻结夜) |
| `queue.completed` | 50 | 60 | ≈ |
| `task.failed` | **1** | 16 | ↓↓ |
| `node.fail` | 3 | 23 | ↓↓ |
| `queue.retry` | 7 | 31 | ↓↓ |
| `resource.scheduler.all_blocked` | 277 | 627 | ↓ 继续退烧(基线 595) |
| `watchdog.restart.start` | 3 | — | 正常恢复(ui_optimizer/repair 心跳超时) |
| `runner.tool_harness.upgrade` | **9** | 0(历史) | ↑↑ 昨日可写 runner/failover 修复**真触发**(强证据) |
| `done_gate.*` 打回 | 7(self_report 4 + logic_chain 2 + review_invalid 1) | — | 门禁在工作 |

**关键判断(诚实口径,与昨日「部署即改善」警戒对照):本窗口是真实正向窗口,不是低吞吐误判。** 失败率 1/(47+1)≈**2.1%**(昨日 30.8%),且 `task.done=47` 说明有真实吞吐量、不是冻结夜的低量假象。昨日命名的 Meta 误区「部署即改善」在本(较高吞吐)窗口被反向证伪——**韧性提升是真的**:queue-running-missing 不再判死、`runner.tool_harness.upgrade` 实际触发 9 次、all_blocked 627→277。这是 [[沉淀≠落地]] → [[复盘规则落地追踪门]] 的闭环验证窗口。

## 1. 上一日(06-25)防复发规则落地核验(decisions.md:538 强制项)

| 规则 | 核验结论 | 证据 |
|---|---|---|
| **NR1 · 复盘规则落地追踪门**(续核 P1-C + failover) | ✅ **已落地并验证** | ① queue-running-missing 本窗口 `task.failed`=**0**,仅 `queue.recovered`(enginePid 55536 已不存在→重新入队,01:20:25Z 11:20 本地)与 `taskstore.running.recovered`(stale 8811s→恢复),按 infra 噪声分流、**未开重复工单**;② `50b65386540b23c8` 同签名 todo 工单本窗口**无新增**(现存全部 ≤06-24 23:36);③ failover:`runner.tool_harness.upgrade` 本窗口 **9 次**(历史 0),纯文本 GLM 升级到带工具 harness 的链路真跑通(repair 工单 1 LIVE smoke 证) |
| **NR8 · 部署期锁协调器退避 + respawn 限流** | ⚠️ **本窗口症状未复发,但落地未验证** | 01:30–02:48Z 有提交批次(`3ade8ab/c49a78a/26b268b`,09:30–10:48 本地),同段有 `resource.lock.wait`(01:43)+ `resource.lock.bypassed`(privileged-maintenance-channel,01:51),但**无 `resource lock coordinator timeout`/worker-crash 判死**——本窗口 churn 较小;未见 NR8 专项代码落地证据,保留观察 |
| **NR9 · 验收表模板剔除「董事会评议行」作设计对照** | ⚠️ **匹配侧已缓解,模板侧未落地** | 见 §2 P-B:repair 工单 2 已修 `done-gate.js` 锚点回退匹配(不再因截断误判);但模板仍把 `decisions.md:25/33/77/385/420/532`(均为同一条「董事会评议」纪要行)注入为「设计对照」要点——**本任务自己的验收表即是证据**(6 行设计对照全指向这些纪要行)。模板侧根因未除 |
| **NR10 · mechanisms-smoke 失败归因 owner** | ❌ **未闭环** | `硬化建议-20260626.md`:mechanisms-smoke 仍 `pass=false code=1 AssertionError`(连续第 3+ 窗口红);本窗口**未见 mechanisms-smoke 维修工单**,断言未定位、未归因 |
| **NR5 · all_blocked 锁持有者审计** | ⚠️ **持锁者快照已具备,top-N 阈值输出待确认** | `all_blocked` 事件现带 `conflicts:[{token,mode,domain,queueAgent,queueId,taskId,ownerPid}]`(持锁者快照已落);但未见风暴阈值 top-N 长期持锁输出。本窗口 277 次中 ui_optimizer 占 **187**,见 P-D |

## 2. 当天问题清单(按集群)

### ✅ P-A · frontend_designer 自报未完成 3 连失败 → 已开单·主管直修+LIVE验证·闭环
- 现象:定时页面评审 `scheduled-20260625012947` 派给 frontend_designer,01:29–01:33Z 三次 `done_gate.self_report_incomplete`(`implementation.done=false`),`queue.retry` ×3(a348a530 / ff194c92 ×2),末次开自动工单 `auto-20260625013315-dc613b9ef8b5260c`。
- 根因:**runner 可写性由 goal 文本启发式猜测,未遵守 config 显式声明**。`config.json roleRouting.frontend_designer.execution.requiresWritableRunnerForImplement=true` 已声明,但全仓 .js **0 命中**该字段——`shared/engine/cli-runner.js` 的 `nodeNeedsWritableRunner` 只跑文本启发式,评审类 goal 被读作只读→不升级 harness→落纯文本 GLM(无 fs/shell)→模型自报 done=false。影响 5 个声明可写的 GLM 角色,每 4h 定时评审复发。
- 维修结果(**closed**,工单 done):`cli-runner.js` 新增 `roleDeclaresWritable`,角色声明可写即恒判 needsWritable 触发既有 harness 升级;`engine-runner.js` 新增 `roleExecMetaFromConfig` 注入;新增 `tests/runner-writable-flag.test.js`(5/5 PASS);可逆开关 `YUTU6_HONOR_WRITABLE_FLAG=0`。并派 repair 做 zhipu-glm-tools LIVE 端到端冒烟(task `cr-live-glm-tools-smoke-20260625095348`),`runner.tool_harness.upgrade` 事件 + execute 落盘 + done-gate 通过均有证。

### ✅ P-B · Simulaid done_gate「设计对照行缺少要点」误报 → 已开单·主管直修·闭环(匹配侧)
- 现象:supervisor-Simulaid review-loop,08:39Z `done_gate.logic_chain_missing`(acceptance_table 第9行 视觉/UI 证据 + 缺逻辑链),attempt2 08:57Z `done_gate.review_invalid`(第3行 设计对照 `decisions.md:65`)。开工单 `auto-20260625085725-8ba9be40a639d621`。
- 根因:`shared/engine/done-gate.js findAcceptanceRow` 对设计对照行只做**全文 containment 匹配**;设计对照要点=整条超长 `decisions.md` 文本,执行/复核方惯用省略号 `…` 截断重述→合法行被误判「缺少要点」。**同一 decisions.md:65 行 06-24 与 06-25 连续复发**(误报放大为假失败 + 无谓工单)。
- 维修结果(**closed**,工单 done):`findAcceptanceRow` 增「按 `decisions.md:行号` 唯一锚点回退匹配」(exact/containment 命中优先,失配再按锚点);要点正确性仍由 status+证据可核+`validateAcceptanceEvidenceAlignment` 兜底,未削弱门禁。新增 `done-gate.acceptance-match.test.js`(6/6 PASS)。

### ⛔ P-C · Simulaid「运行时长」任务实质性失败 → task.failed/route.failed · **未闭环(交 Simulaid CEO/主管重做)**
- 现象:08:57Z `done_gate.review_invalid`(attempt2)→ `node.fail` → `task.failed` + `project.route.failed`(task `cr-1782377169375-49f20b13`,project=Simulaid)。
- 实质内容(**门禁的正确拦截,非误判**):复核判定 CEO 根因诊断经代码核实**为错**——后端多处写 `started_at` 且 `server.js:1929` 已做 `enqueued_at` 兜底;被否两轮的「`enqueued_at` 喂运行芯片」方案仍在方案里,配合宽松验收会放行「刚启动就显示运行 N 分钟」的错误执行。修订口径=「只用真实运行起点 + 静态兜底文案 + 收紧验收」后可执行。
- 缺口:repair 工单 2 修的是**门禁匹配 bug**(P-B),让任务能重过门;但**底层 Simulaid 运行时长方案本身仍需按修订口径重做**——属 Simulaid CEO/supervisor 范围,监管不代为重入队、不替 CEO 拆解。**未闭环**。

### ⚠️ P-D · all_blocked 277 次,ui_optimizer 独占 187 — 反复·退烧未根治
- 现象:`resource.scheduler.all_blocked` 277(21:00:08Z 起持续到 17:38:08Z),按发起方:ui_optimizer **187** / frontend_designer 44 / quality_ops 38 / memory-officer 7 / supervisor-Simulaid 1。典型 conflicts:写锁 `config/agents/tests` 与 governance(gov-review 持 config 读+写)冲突。
- 判断:总量 627→277 继续退烧,且事件已带持锁者快照(NR5 部分落地)。但 **ui_optimizer 是活瓶颈**(占 67%),其反复 read/write `engine/console-project/config` 与监管/质量运营写锁互斥。NR5 的「风暴阈值 top-N 长期持锁输出」仍缺,无法定位是否存在长期持锁者。

### ⚠️ P-E · repair-lead 自身 600s 无节点进展被判死 — 单发·关注
- 现象:01:43:16Z `queue.engine.terminate` + `queue.retry`(repair-lead / b222a39a):「running 项 01:33:16Z 后已 600s 无节点进展,按节点无进展超时判死」。该 queueId 正是承接 P-A 工单的维修主管任务。
- 判断:维修主管(Claude Code)做根因分析 + 直修 + 派工耗时超 600s 无中间节点事件,被无进展超时判死后重试。属「重活/长思考被节点无进展门误判」——单发,但提示**维修主管这类长任务需更长 no-progress 阈值或心跳节点**(对照 [[补丁已落盘 ≠ 运行态已生效]] 家族的判死阈值问题)。

### ❌ P-F · mechanisms-smoke 连续第 3+ 窗口红 — 反复·NR10 未闭环
- 现象:`硬化建议-20260626.md` mechanisms-smoke `pass=false code=1`(`AssertionError [ERR_ASSERTION]`),resource-locks/project-guard/serial/long-run 四项 PASS。
- 缺口:连续多窗口红,**仍无维修工单定位失败断言**;daily 脚本只给摘要、无根因 owner。承接昨日 NR10,继续未闭环。

## 3. 根因分析(误区诊断 + 根因)

**Meta 正例(本窗口闭环)**:昨日「部署即改善」警戒在本窗口被**正向证伪**——这次是真改善:有真实吞吐(done 47)且失败率 2.1%,running-missing/failover/harness-upgrade 修复在生产实际生效。**[[沉淀≠落地]] → 落地 → 高吞吐窗口验证** 全链路首次走完。应固化为「复盘规则落地追踪门」的标准成功范例。

**新 Meta 误区命名:「声明未被运行时遵守」(declared-but-not-honored)**。P-A 与 P-B 是同一族根因:**显式声明/锚点已存在,但运行时代码用启发式/全文匹配替代,从不读取权威声明**。
- P-A:`config` 声明 `requiresWritableRunnerForImplement=true`,运行时用 goal 文本启发式猜可写性 → 声明形同虚设。
- P-B:验收要点带 `decisions.md:行号` 唯一锚点,done-gate 用全文 containment 而非锚点 → 截断即误判。
- 共性解法:**让运行时遵守显式声明/锚点为权威输入,启发式仅兜底,并加回归测试锁定**。这是 decisions.md:538「沉淀≠落地」在代码层的同族——「config/锚点新增字段必须有对应 .js 消费点 + 回归测试,否则等于未落地」。

**门禁健康度判断**:本窗口 7 次 done_gate 打回中,P-A(self_report 真失败,可观测合规)、P-C(review_invalid 实质拦截 CEO 错误诊断)是**门禁正确工作**;P-B(设计对照截断误报)是**门禁过严的假失败**,已修。门禁整体在「拦真 + 误报已收敛」的健康区间。

## 4. 防复发规则(可执行、可验证)

- **NR11 · 「声明未被运行时遵守」体检门(针对 P-A/P-B 同族,新增,高优先)**:凡 `config.json`/`decisions.md 锚点`/角色合约中新增**能力或门控声明字段**,必须同时存在:① 对应 `.js` 运行时消费点(grep 该字段名 ≥1 命中);② 锁定该消费的回归测试。验证:写一个 `config-field-consumed` 自检脚本/测试,枚举 `roleRouting.*.execution.*` 等关键声明字段,断言每个在引擎代码有 grep 命中,缺失即红。owner:质量运营硬化为检查脚本。承接 decisions.md:538。
- **NR9(续)· 验收表模板侧仍未落地 — 提级续核**:匹配侧(done-gate 锚点回退)已由 repair 工单 2 落地;但**模板仍注入「董事会评议」纪要行作设计对照**(本任务验收表 6 行即证)。续 NR9①②:模板自检脚本拒绝把 `decisions.md` 的「董事会评议」流水行当设计对照要点,设计对照只引可核设计规格/结论行。验证:含评议行的模板生成任务,断言模板自检拒绝注入。owner:质量运营。
- **NR10(续)· mechanisms-smoke 必须开单归因(连续第 3+ 窗口红,强制)**:不再只在硬化报告写「建议开单」,**本条作为 owner 动作**:维修主管/质量运营开维修工单读 mechanisms-smoke 全量断言、定位失败断言、归因(回归/环境/真 bug),结案附断言定位 + 修复/豁免 + 复跑绿。验证:下窗口 mechanisms-smoke 转绿或有结案工单。owner:维修主管。
- **NR12 · 维修主管/长思考任务的 no-progress 判死阈值(针对 P-E,新增,中优先)**:repair-lead 这类「根因分析 + 直修 + 派工」长任务,600s 无节点事件即判死偏紧。建议:① 对 repair-lead/governance 等长推理队列提高 no-progress 阈值或加中间心跳节点事件;② 区分「真卡死」与「长思考无中间产物」。验证:模拟 repair-lead 长任务,断言不被 600s 无进展门误杀。owner:维修主管/质量运营。
- **NR5(续)· all_blocked 持锁者 top-N 输出 + ui_optimizer 锁面收敛**:持锁者快照已落(本窗口已验证 conflicts 带 owner)。续:① 风暴达阈值输出长期持锁 top-N;② 审 ui_optimizer 为何独占 187 次阻塞(其 read/write `engine/console-project/config` 锁面是否过宽、可否缩小写锁域或错峰)。验证:top-N 输出非空可定位;ui_optimizer all_blocked 占比下降。owner:质量运营/维修主管。

## 5. 维修结果与未闭环缺口

- **已闭环(代码级,本窗口)**:
  - P-A 可写 runner 声明被遵守(`cli-runner.js` + `roleExecMetaFromConfig` + 5 测 + LIVE smoke)— 工单 `auto-20260625013315` done。
  - P-B done-gate 设计对照锚点回退匹配(`done-gate.js` + 6 测)— 工单 `auto-20260625085725` done。
  - **NR1 落地核验通过**:queue-running-missing 不判死、failover/harness-upgrade 真触发(9 次)、同签名工单零新增。
- **未闭环缺口清单(按优先级)**:
  1. **P-F / NR10 · mechanisms-smoke 连续第 3+ 窗口红、仍无归因工单** —— 最久悬而未决,提级为强制开单。
  2. **P-C · Simulaid 运行时长方案需按「真实运行起点 + 静态兜底 + 收紧验收」重做** —— 门禁已拦,实质修复属 Simulaid CEO/主管范围,监管不代办。
  3. **NR9 模板侧 · 「董事会评议行」仍被注入为设计对照** —— 匹配侧已修,模板根因未除,持续制造不可核种子行。
  4. **NR11(新)· 「声明未被运行时遵守」体检门未建** —— P-A/P-B 同族,缺自检脚本防再发。
  5. **P-D / NR5 · all_blocked top-N 持锁者输出仍缺 + ui_optimizer 独占 187** —— 退烧未根治,活瓶颈未定位。
  6. **NR8 · 部署期锁协调器退避落地未验证** —— 本窗口症状未复发(churn 小),保留观察。
  7. **P-E / NR12(新)· 维修主管长任务 600s 无进展判死偏紧** —— 单发,待阈值/心跳调整。

## 6. 沉淀去向
- 经验:① Meta 正例「沉淀→落地→**高吞吐窗口验证**」全链路首次走完(NR1 闭环,反向证伪「部署即改善」);② 新 Meta 误区「声明未被运行时遵守」(config/锚点声明 + 运行时启发式替代 = 假落地,P-A/P-B 同族);③ 维修主管长任务被 no-progress 门误杀(P-E)→ `memory/experience.md`(2026-06-26)。
- 决策:① 凡新增能力/门控声明字段必须有 .js 消费点 + 回归测试(NR11,承接 538);② 连续多窗口红的 smoke 必须强制开单归因,不停在硬化报告建议(NR10 提级)→ `memory/decisions.md`(2026-06-26)。
- 实体/术语:「声明未被运行时遵守(declared-but-not-honored)」、「设计对照行锚点回退匹配」、「可写 runner 声明门(requiresWritableRunnerForImplement)」、「runner.tool_harness.upgrade(可写 harness 升级)」→ `memory/entities.md`(2026-06-26)。
</content>
</invoke>
