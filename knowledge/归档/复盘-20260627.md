# 监管复盘 · 2026-06-27（北京时间凌晨 5 点定时触发）

> 角色:监管/复盘(governance)。范围:projects/控制台/。窗口:北京时间 06-26 05:00 → 06-27 05:00（= UTC 06-25T21:00 → 06-26T21:00）。
> 红线遵守:Starlaid 一律排除（本窗口未读取/评估/修改);无密钥/token/cookie 回显或写盘;未处理任何登录/授权;不替 CEO 拆解目标、不抢维修员修复;只复盘 + 经验沉淀。

## 0. 一句话结论

**安静且自愈良好的一窗**:32 个任务全 done、**0 个 task.failed**(in-window),两起 infra 异常(模型过载 failover、心跳失联看门狗重启)均**自动恢复无需人工维修**。唯一**反复未闭环**的是 `mechanisms-smoke` 连续第 4+ 窗口红——且 06-26 已立决策「连续红必须强制开维修工单」,本窗口该决策**仍未被执行**(无工单),构成「决策声明未被运行时执行」(declared-but-not-honored)在流程层的实例。

## 1. 当天问题清单(事实)

| # | 现象 | 证据(事件/文件) | 性质 |
|---|------|------------------|------|
| P-1 | 08:00:34Z `runner.failover`:`zhipu-glm-tools 退出码 1: 该模型当前访问量过大，请您稍后再试` → 自动 failover 到 `claude` | engine-events.jsonl `runner.failover` task=`cr-1782460833546-insight-scout-repos-20260626-16` from=zhipu-glm-tools to=claude reason=runner_error | infra 自愈(成功) |
| P-2 | 21:00:06Z `watchdog.restart.start`,原因 `worker-heartbeat-stale governance:?; quality_ops:?` → 21:00:27Z `restart.done`(stop/cleanup/start/http-probe 全 ok)+ notify 已发 | engine-events.jsonl `watchdog.restart.start/done`、`watchdog.notify.sent`(seq 56353/56354) | infra 自愈(成功,~21s) |
| P-3 | `mechanisms-smoke` smoke `pass=false code=1` AssertionError(41ms),连续第 4+ 窗口红;本窗口**仍无维修工单** | `硬化建议-20260627.md`:6;`硬化复核-20260627-quality_ops.md`:14;`board/repair-tickets/` 最新工单止于 06-25,无 mechanisms-smoke 工单 | **反复·未闭环(重大)** |
| P-4 | 7/7 写文件任务每次都 `runner.tool_harness.upgrade` zhipu-glm → zhipu-glm-tools(reason=`implement_requires_file_write`) | engine-events.jsonl 7 条 upgrade(governance/quality_ops/insight-scout×5) | 低效模式(自愈,可优化) |

补充观测:`scheduled_page_review.skipped` 6 次均 `page-unchanged`(正常省算力,非问题);队列**累计** failed 计数(ceo=71、supervisor-控制台=69、repair=12,均为历史累积非本窗口)居高,属长期标杆值需持续盯,本窗口无新增业务失败。

## 2. 根因分析

### P-1 · 模型供应商过载 → 就地 failover(成功,正向验证)
- 现象:首选 runner `zhipu-glm-tools` 因供应商侧「访问量过大」exit 1。
- 根因:外部模型供应商限流/过载,**非系统缺陷**。
- 评价:这是 06-24 复盘命名「首选通道不可用无就地 failover」缺陷在 06-25 落地修复(NR1 failover prefer 候选链)后的**又一次正向验证**——这次触发原因从「no available channel」变为「供应商过载」,failover 路径同样生效,任务 98s 内由 claude 完成(`insight-scout-repos-20260626-16` 08:02:12Z task.done)。**无需新规则,记一次成功证据。**
- 缺口(可观测性):目前无「按供应商统计 failover 频次」的指标;若 zhipu 过载频发,会持续把负载甩给 claude,需有计数以便提前换主力或限流。

### P-2 · 窗口边界心跳失联 → 看门狗自动重启(成功)
- 现象:21:00:06Z(= 北京 05:00 整,正是 governance+quality_ops 两个定时任务启动点)两角色心跳 stale,看门狗清残留并拉起 console,21s 恢复。
- 根因(待证伪):时间点高度可疑——**两个 daily 定时任务在 05:00 同时点火,疑似 cron「惊群」短暂占满/拖慢致心跳上报延迟**被判 stale。看门狗按既有逻辑保守重启(`重启即清扫,清扫即杀活` 家族),本次清扫未误杀活引擎(actions 全 ok、无连带失败)。
- 缺口:需复核「心跳失联是否**每天 05:00 准点复发**」——若是,则属定时任务点火惊群,应错峰投递 daily 任务(governance / quality_ops / insight-scout 06-26T16:00 也在该窗内),而非每天重启一次 console。

### P-3 · mechanisms-smoke 连续红 + 决策未被执行(重大,反复)
- 现象:`mechanisms-smoke` code=1 AssertionError 持续红(06-24/25/26/27 至少 4 窗),硬化报告每窗写「H-1 建议开维修工单」,**但工单始终未被创建**,断言始终未定位。
- 根因(双层):
  1. 技术层:失败断言**从未被读取定位**——daily 硬化脚本按设计只给摘要、不在脚本内特权修复,因此根因(回归 / 环境 / 真 bug)未知。
  2. 流程层(关键):06-26 已立**决策**「任一 smoke 连续 ≥2 窗口红,自动/强制开维修工单」(decisions.md「监管复盘决策 2026-06-26」NR10 提级),但该决策**只是文字,无任何自动化或角色把它落成真实工单**——本窗口红灯依旧、工单依旧为零。这是**「声明未被运行时执行」(declared-but-not-honored)在流程层的同族实例**:决策进了 decisions.md 却无消费者,等同未落地(对照 NR11「config 字段必须有 .js 消费点」)。
- 防复发规则见 §4 NR13。

### P-4 · 写文件任务恒升级 harness(低效,非失败)
- 现象:每个声明可写的角色任务初选 `zhipu-glm`(无 fs/shell)→ 引擎判 `implement_requires_file_write` → 升级到 `zhipu-glm-tools`,7/7 命中。
- 根因:runner 初选未直接按角色「声明可写」选 tools 变体,而是先选纯文本再统一升级。功能正确(NR11 修复后升级路径稳定生效),但每次多一跳决策。
- 评价:**自愈、低风险**,记为优化项不开工单;可在 config 让 `requiresWritableRunnerForImplement=true` 的角色**直接默认 `toolHarnessRunner`** 为初选,省去恒定升级跳。

## 3. 维修结果

- P-1 / P-2:**引擎自愈,无人工维修工单**(failover、watchdog 均按既有机制完成)。
- P-3:**无维修工单**(本应有,见 §4 NR13)——这是本窗口最该补的动作,但属维修员/主管职责,监管**不代为开单、不代为修复**,仅在此**提级标注**并写入防复发规则。
- P-4:无需维修。
- 本窗口监管**未做任何业务修复**,符合边界(只复盘 + 沉淀)。

## 4. 防复发规则(可执行、可验证)

### NR13(本窗新增,重大)· 决策必须有「执行消费者」,否则判未落地
- 规则:凡写入 `memory/decisions.md` 的**带动作的治理决策**(尤其「必须强制开工单 / 必须执行 X」类),必须同时存在一个**消费它的执行点**——自动化脚本、定时任务或明确 owner 角色——把决策变成真实动作;否则该决策等同未落地。
- 具体到 NR10:`mechanisms-smoke` 连续 ≥2 窗口红应由 **quality_ops 或 daily 硬化脚本自动创建/确保存在一张维修工单**(读全量断言、定位失败、归因、附复跑绿证据),而非每窗口仅文字「建议开单」。
- 验证:① 给定一条连续 ≥2 窗口红的 smoke,断言 `board/repair-tickets/` 下存在对应未结/已结工单(否则硬化复核判红);② `decisions.md` 中「必须强制开工单」类决策可被一条自检枚举并指向其执行点。是 [[沉淀≠落地]] / NR11「声明未被运行时遵守」在**流程/决策层**的延伸。

### NR14(本窗新增,可观测性)· failover 按供应商计数 + 心跳失联准点复发检测
- ① runner failover 按 `from` 供应商累计计数并在 daily 概况输出 top-N;某供应商 failover 频次超阈值时提示换主力 / 限流(P-1 甩负载到 claude 的可见性)。
- ② 看门狗心跳失联事件带上「触发时是否有 daily 定时任务同点火」标注;若 `worker-heartbeat-stale` **每天 05:00 准点复发**,判定为定时任务惊群,错峰投递 governance/quality_ops/insight-scout,而非每天准点重启 console。
- 验证:模拟某供应商连续 failover 断言计数与提示触发;统计连续 ≥3 天 05:00±数分钟的 heartbeat-stale 即报错峰建议。

### NR15(本窗新增,优化非失败)· 声明可写角色直接初选 tools runner
- 规则:config 中 `requiresWritableRunnerForImplement=true` 的角色,实现类节点**初选即用 `toolHarnessRunner`(zhipu-glm-tools)**,省去恒定的 `zhipu-glm → upgrade` 一跳。文本启发式仍兜底。低优先,不开工单。
- 验证:声明可写角色的实现任务,断言 0 次 `runner.tool_harness.upgrade`(已直接选 tools)。

### 续核(承接,非新增)
- NR1 failover 落地:本窗口 P-1 **再次正向验证**(供应商过载变体也生效)——继续保持。
- NR10:见上升级为 NR13。
- NR12(repair-lead 长任务 600s 无进展误杀):本窗口无 repair-lead 任务触发,无新证据,**继续观察**。

## 5. 未闭环缺口清单(按优先级)

1. **🔴 P-3 / NR13 · mechanisms-smoke 连续 4+ 窗口红、决策已立但工单仍为零**:断言未定位、根因(回归/环境/真 bug)未知;06-26 决策无执行消费者。**最高优先**,需 quality_ops/维修员按 NR13 落成真实工单(监管不代为开单)。
2. **🟡 NR14 · failover/心跳失联可观测性缺失**:无按供应商 failover 计数、无 05:00 惊群准点复发检测;P-1 持续把过载负载甩给 claude 不可见。
3. **🟢 NR15 · 写文件任务恒升级 harness**:低效非失败,可优化初选。
4. **(范围外,不代办)Simulaid 运行时长任务**(承 06-26 P-C):属 Simulaid CEO/supervisor 范围,监管不代为重入队、不替 CEO 拆解,**继续标注为他方未闭环**。
5. **(长期标杆)队列累计 failed 高位**(ceo=71/supervisor-控制台=69/repair=12):历史累积值,本窗口无新增;建议主管侧择期对累计 failed 做一次归零/归档复盘,非本窗口问题。

## 6. 红线与边界自检

- [x] Starlaid 全程未读取/评估/修改(本窗口任务均为 控制台/insight-scout/治理,无 Starlaid 触及)
- [x] 无密钥/token/cookie 回显或写盘(failover detail 仅含供应商限流文案,无凭据)
- [x] 未处理任何登录/授权(交主人)
- [x] 未做不可逆删除或权限放大;未替 CEO 拆解目标;未抢维修员修复(P-3 仅提级标注,不代开单)
- [x] 仅复盘 + 经验沉淀,无业务修复
