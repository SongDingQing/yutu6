# 每日复盘 · 2026-06-25(覆盖 2026-06-24T05:00 — 2026-06-25T05:00 北京时间 / UTC 2026-06-23T21:00 — 2026-06-24T21:00)

> 监管/复盘智能体每日定时复盘。对事不对 agent、改流程不追责。规则要可执行、可验证。
> 证据来源:`projects/控制台/artifacts/engine-events.jsonl`(窗口内事件)、`board/repair-tickets/`(当天工单)、`knowledge/归档/硬化建议-20260625.md`、`knowledge/归档/复盘-20260624.md`、`git log`(06-24 落地批次)、相关源码/配置定位。
> 红线遵守:Starlaid 未读取/未评估/未修改;无密钥/token/cookie 回显;无登录/授权操作;只复盘+沉淀,未做任何业务修复或不可逆删除。

## 0. 当天概况(24h 窗口事件计数)

| 指标 | 今日(06-25 窗口) | 昨日(06-24 窗口) | 走向 |
|---|---|---|---|
| `task.done` | 36 | 408 | ↓ 量级骤降(低吞吐窗口) |
| `queue.completed` | 60 | 532 | ↓ |
| `task.failed` | 16 | 241 | ↓ 绝对值大降 |
| `node.fail` | 23 | 177 | ↓ |
| `queue.retry` | 31 | 193 | ↓ |
| `resource.scheduler.all_blocked` | 627 | 1961 | ↓ 退烧(仍高于 6/22 基线 595) |
| `watchdog.restart.throttled` | 16 | 149 | ↓ console-http 风暴基本消退 |
| `queue.worker.superseded` | 235 | — | 滚动重载在运行(见正向闭环) |

**失败原因桶(task.failed + node.fail 共 39):**
| 次数 | 原因桶 | 归属 |
|---|---|---|
| 12 | `running 队列文件已不存在`(queue-running-missing) | P2 残留竞态,仍发(P1-C 落地于窗口尾,下窗口验证) |
| 10 | `resource lock coordinator timeout`(worker-crash) | **新增·部署期资源锁协调器超时** |
| 7 | `done_gate.logic_chain: 使用不可核声明作证据` | 门禁生效(拦截),但**模板种子行复发** |
| 4 | `done_gate.review_verification` 缺要点/缺 hard verification | 门禁生效(拦截) |
| 4 | `engine-runner 被信号 SIGTERM 终止` | 部署清扫连带杀活(ui_optimizer) |
| 2 | `运行超时`(codex 900s / claude-code 600s) | P4 残留,较昨日 11 次大降 |

**关键判断(诚实口径):绝对失败数大降主因是低吞吐 + 部署冻结夜(`task.done` 408→36),不是纯韧性提升。** 失败率 16/(36+16)≈**30.8%**,昨日 241/(408+241)≈37.1%——失败率仅小幅改善,且高度集中在 06-24 晚的部署 churn 窗口(13:20–14:20Z)。真正的结构性好转来自隔夜落地的稳定性批次(见 §1 正向闭环),但需下一个高吞吐窗口才能确证。

## 1. 当天问题清单(按集群)

### ✅ 正向闭环(重大)· 昨日「沉淀≠落地」Meta 误区被付诸行动 — 应固化为正例
- 事实:`git log`(06-24 21:26 — 06-25 02:09 本地)显示隔夜落地一大批稳定性补丁,直接对应昨日 NR3/NR4/NR6/P6:
  - `2dfb715/671828b/e75c64f` P0-B:**常驻 worker 核心代码热重载(空闲优雅重启不杀活引擎)** → 闭合 P6 元根因「补丁落盘≠运行态生效」。
  - `80818db` worker 孤儿回收器 worker-reaper + `46ff467` ensureQueueWorker spawn-lock 根治成对 spawn → 闭合 worker 重载/竞态。
  - `e1c8743` P1-C:**queue-running-missing 归为 infra 噪声(竞态重入队不开重复工单)** → 对应 NR3/NR7。
  - `1fe31e3` **runner failover 落地:prefer 候选链真生效** → 闭合 NR4/P5(glm-5.2 no-channel 空耗)。
  - `7d7f676` P0-B 收口:修锁时间窗错配 + abort 定向解暂存 → 对应锁竞态。
  - `e75c64f/3fa5f5a` done gate 真执行(executeEvidence + gitVerify)。
- 意义:昨日命名的 Meta 误区「沉淀≠落地」**本窗口被实际打破**——规则从 memory 变成了代码/测试。窗口内 `watchdog.restart.throttled` 149→16、`all_blocked` 1961→627、console-http-failed 风暴消失,与批次落地时序吻合。**这是「改流程不追责」的标准正例**:规则有了 owner、变成 diff、可回归。

### P-NEW · 部署期「资源锁协调器超时」churn 窗口(13:20–14:20Z)— 新增 · 重大(短时)
- 现象:`resource lock coordinator timeout` 致 `worker-crash-*` **10** 次(repair-lead ×多、frontend_designer、ui_optimizer),交织 `queue-running-missing` 12 次、`engine-runner SIGTERM` 4 次,集中在 **13:20–14:20Z 一小时**。
- 时序根因:该窗口正是核心代码大同步/解冻批次落地点(`6522ae4` 06-24 21:26 本地=13:26Z「解除 v0.0.0.3 长期冻结·同步核心改动」)。事件流显示同时段 `queue.worker.spawned` 密集(repair-lead/repair 反复 spawn)、`resource.lock.acquired/released` 高频翻动、`all_blocked` 叠加 → **滚动重载/部署对 worker 的批量 supersede+respawn,把资源锁协调器打到超时,正在跑的 engine 被连带 SIGTERM**。属「[[重启即清扫,清扫即杀活]]」叠加锁协调器在部署期的瞬时争用。
- 性质:**部署副作用(短时 churn)**,非常态业务 bug;但 `resource lock coordinator timeout` 直接把任务判 `task.failed`(而非退避重试),代价被放大。

### P2 · queue-running-missing 仍发 12 次 + `50b65386540b23c8` todo 工单堆积 24 张 — 反复 · 收口中
- 现象:`running 队列文件已不存在` node.fail/task.failed **12**;`board/repair-tickets/` 当前 `50b65386540b23c8` 同签名 todo 工单 **24** 张(多在 06-24 23:36 批量回填)。
- 进展:`e1c8743` P1-C「queue-running-missing 归为 infra 噪声(竞态重入队不开重复工单)」落地于窗口尾(23:35 本地≈15:35Z),**晚于本窗口绝大多数 running-missing 事件(09–14Z)**,故本窗口仍计入;预期下窗口该签名停止刷新工单——**需下一日复盘核验落地效果**(承接 NR1 落地追踪门)。
- 缺口:已堆积的 24 张同签名 todo 仍待批量结案/归档(infra 噪声分类后的清库动作)。

### P3 · done_gate「不可核声明作证据」复发 7 次 — 根因在**验收表模板种子行**,非门禁误判 — 新根因
- 现象:`done_gate.logic_chain: implementation.acceptance_table 第N行 使用不可核声明作证据`,内容恒为 `设计对照 memory/decisions.md:532 任务:修引擎项目归属判断…CEO plan 红线复述…`,本窗口 **7** 次,eventlog 历史累计 **112** 次。
- 核实根因:`memory/decisions.md:25/33/77/385/420/532` 经核对**全是同一条「董事会评议」记录**(`修引擎项目归属判断…`),属**评议流水/会议纪要,不是可核的设计规格**。结构化验收表模板把这些「董事会评议行」当成「设计对照」验收要点注入(本复盘任务自身收到的验收表即含 6 行 `设计对照 memory/decisions.md:XXX`),执行 agent 把它们原样抄进 `acceptance_table` 当证据 → `done_gate` 正确判「拿不可核声明充证据」并打回。**门禁在正确工作;缺陷在模板:把会议纪要行当成设计对照验收点,种子行天然不可核。**
- 性质:流程/模板缺陷,直接拉高 done_gate 打回率与重试空耗(7 次/窗口 + 历史 112)。

### P4 · mechanisms-smoke 仍红(code 1, AssertionError)— 反复 · 昨日缺口未闭环
- 现象:`knowledge/归档/硬化建议-20260625.md` 报 `mechanisms-smoke pass=false code=1`(`node:assert AssertionError`),已开 H-1 建议交维修员/主管复核。承接昨日复盘缺口 #7,**连续两窗口红**。
- 缺口:具体断言失败点未定位归因(硬化脚本只给摘要),需维修员读全输出。

### P5 · 运行超时残留 2 次(codex 900s / claude-code 600s)— 反复 · 大幅收敛
- 现象:`codex 运行超时(900s)` 1 次(09:35)、`claude-code 运行超时(600s)` 1 次(13:46)。较昨日 11 次大幅收敛,但 P4「长运行 runner 挂起喂判死」未根除。

## 2. 根因分析(误区诊断 + 根因)

**Meta 正例(本窗口)**:昨日「**沉淀≠落地**」Meta 误区被实际打破——规则绑上了 owner、变成代码 diff。应作为「NR1 复盘规则落地追踪门」的首个成功验证记录。**但要警惕新 Meta 误区:「部署即改善」**——绝对失败数下降很大程度来自低吞吐 + 部署冻结夜,失败率仅 37%→31%,真实韧性需高吞吐窗口确证,不能凭一夜低量数据宣布胜利。

分项根因:
1. **部署期资源锁协调器争用 + 重启清扫连带杀活(P-NEW)**:核心代码滚动重载/解冻批次对 worker 批量 supersede+respawn,资源锁协调器在 spawn 风暴下超时,活 engine 被连带 SIGTERM;`resource lock coordinator timeout` 直接判 task.failed 而非退避重试,放大代价。
2. **验收表模板把「董事会评议纪要行」当设计对照验收点(P3)**:`decisions.md` 的评议流水被模板注入为 `设计对照 decisions.md:行` 种子行,天然不可核,执行 agent 原样抄作证据 → done_gate 必打回。门禁对、模板错。
3. **running 文件竞态收口落地于窗口尾(P2)**:P1-C 已落代码但晚于本窗口多数事件;堆积的同签名 todo 工单待清库。
4. **mechanisms-smoke 断言失败未归因(P4)**:连续两窗口红,硬化脚本只给摘要、无根因定位 owner。
5. **长运行 runner 挂起残留(P5)**:codex/claude-code 超时喂 no-progress,虽大幅收敛但未根除。

## 3. 防复发规则(可执行、可验证)

- **NR1(续)· 复盘规则落地追踪门 — 本窗口首次正向验证 + 下窗口续核**:昨日 NR3/NR4/NR6 已落代码(见 §1 正向闭环),NR1 机制有效,继续保留。**下一日(06-26)复盘必须核验**:① P1-C(queue-running-missing→infra 噪声)落地后 running-missing 失败数与 `50b65386540b23c8` 新增工单是否归零;② runner failover(`1fe31e3`)在 glm-5.2 no-channel 场景是否真降级不失败。证据=下窗口事件计数对比。
- **NR8 · 部署/滚动重载期间资源锁协调器退避 + worker respawn 限流(针对 P-NEW,新增,最高优先)**:① `resource lock coordinator timeout` 不应直接判 `task.failed`,应**退避重排/限速重试**,部署 churn 类与真业务失败分流(同 infra_restart 思路);② 滚动重载/解冻批次落地时,worker supersede+respawn 必须**串行/限流 + drain**,避免 spawn 风暴打爆锁协调器;③ 部署窗口对在跑 engine 走优雅 drain 而非批量 SIGTERM。验证:模拟部署期并发 supersede,断言锁协调器不超时判死、活 engine 不被连带杀、失败归 infra 类不开重复工单。owner:维修主管/质量运营开工单。
- **NR9 · 验收表模板剔除「董事会评议纪要行」作设计对照(针对 P3,新增)**:① 结构化验收表模板**不得**把 `memory/decisions.md` 的「董事会评议」流水行注入为 `设计对照` 验收要点(它们是会议纪要,不可核);设计对照只引用**可核的设计规格/决策结论行**;② 若必须保留对照,模板须改为「要点 + 必填真实证据列」,种子行默认空证据、由 agent 补可核 diff/文件:行,而非抄纪要文本充证据;③ done_gate 可对「纯模板种子行无新增证据」给出更明确的打回提示(指向补证据而非误判)。验证:用含「董事会评议行」的模板生成任务,断言 done_gate 不再因种子行批量打回;模板自检脚本拒绝注入评议流水行。owner:质量运营硬化模板。
- **NR10 · mechanisms-smoke 失败归因 owner(针对 P4,提级)**:连续两窗口红,必须开维修工单读 `mechanisms-smoke` 全量断言输出、定位失败断言并归因(回归/环境/真 bug),不在 daily 脚本内特权修复。验证:工单结案附断言定位 + 修复/豁免决定 + 复跑绿。
- **NR5(续)· all_blocked 锁持有者审计**:all_blocked 退烧到 627 但仍高于基线,且 P-NEW 证明锁子系统是活瓶颈。保留昨日 NR5:事件带 reason + 持锁者快照,风暴达阈值输出长期持锁 top-N。验证:风暴事件 reason 非空、可定位持锁队列/任务。

## 4. 维修结果与未闭环缺口

- **已落地正例(代码级,本窗口隔夜)**:P0-B worker 热重载 / worker-reaper / spawn-lock / P1-C queue-running-missing 分流 / runner failover / 锁时间窗修正 —— 昨日 NR3/NR4/NR6/P6 由建议变代码 diff,应作为「沉淀→落地→验证」全链路标准范例固化。
- **未闭环缺口清单(按优先级)**:
  1. **P-NEW 部署期资源锁协调器超时 + 连带杀活(NR8)** —— 部署 churn 把 10 次 worker-crash 判 task.failed,锁子系统活瓶颈,最高优先。
  2. **P3 验收表模板种子「董事会评议行」作设计对照(NR9)** —— 复发 7 次/窗口、历史 112,持续拉高 done_gate 打回率,流程缺陷。
  3. **P2 queue-running-missing 收口效果待验 + 24 张同签名 todo 待清库** —— P1-C 落地于窗口尾,NR1 下窗口核验;堆积工单待批量归档。
  4. **P4 mechanisms-smoke 连续两窗口红、未归因(NR10)** —— 待维修员定位断言。
  5. **NR5 all_blocked 持锁者审计仍缺** —— 退烧但未根治,无 reason 无法定位长期持锁者。
  6. **P5 长运行 runner 超时残留(2 次)** —— 大幅收敛未根除。
- **诚实缺口(方法论)**:本窗口低吞吐(done 36),韧性改善的结论强度有限,**必须用下一个高吞吐窗口的失败率复核**,防「部署即改善」Meta 误区。

## 5. 沉淀去向
- 经验:① Meta 正例「沉淀→落地→验证」首次闭环(NR3/NR4/NR6 由建议变 diff);② 新 Meta 误区「部署即改善」(低吞吐/部署夜的绝对失败下降≠韧性提升,看失败率);③ 部署期资源锁协调器超时连带杀活(NR8);④ 验收表模板种子「董事会评议行」作设计对照导致 done_gate 批量打回(NR9)→ `memory/experience.md`(2026-06-25)。
- 决策:复盘失败口径以**失败率 + 分窗口对比**为准、防低吞吐误判;部署/滚动重载期 infra 类失败(锁协调器超时/SIGTERM/running-missing)统一退避分流不判死、不开重复工单 → `memory/decisions.md`(2026-06-25)。
- 术语:`部署期资源锁协调器超时(deploy-churn lock-coordinator timeout)`、`验收表模板种子行不可核(董事会评议行当设计对照)`、`部署即改善(低吞吐误判韧性)` → `memory/entities.md` 故障模式段(2026-06-25)。
</content>
</invoke>
