# 工作区整体重设计 + 生图换 emoji + 交互改进 · 规格(给 codex)

> 秘书出规格 → 老板转交 codex → 秘书验证。出规格 2026-06-19。
> 改 `projects/控制台/public/workspace.html`(+ 头像产物目录);排在「公告板 / 组织层级工位」之后(同文件,避免抢)。

## A. 交互改进(先做,体感最强)
1. **输入框移到页面底部**:派单/输入区从顶部挪到**页面下方固定输入栏**(聊天式:团队/输出/事件在上,输入栏固定在底)。顶部那条「派给 + 输入 + 派单」整体下沉。
2. **默认对象 = 秘书**:「派给」下拉**默认选「秘书」**(保留可切其他角色)。呼应 老板 → 秘书 → CEO 的层级——老板平时对秘书说话。
3. **输入框自动高度**:随输入行数自增减(1 行起,约 6~8 行后内部滚动);`Enter` 发送、`Shift+Enter` 换行不变。去掉现在固定高度的僵硬感。

## B. 整体重设计(办公室风格)
- 工位卡片视图整体视觉重做:办公室风格——精致卡片、统一调色(贴现有深色控制台)、合理留白、组织层级清晰(董事长办公室 / CEO / 项目主管区 / 外围)。
- 不动数据/事件/队列逻辑,只改呈现层 + 布局 + 头像。

## C. 生图换 emoji(用 codex 自带 imagegen)
- 用 **codex 自带的 imagegen** 生成办公室风**角色头像**,替换现在所有 emoji(🐇🧑‍💼🧠👷🐭🔧🛡️🖥️⚙️🧩✉️🎨 等)。
- **尺寸(已定,提前给好)**:每张头像 **512×512 px、PNG、透明背景**;前端显示约 **44px 圆角**。统一画风 = **扁平现代 Q 版办公室角色**、与深色 UI 协调、同视角 / 同光影 / 同调色板。
- **按角色类型各生成一张**(同类型实例共用):董事长、秘书、CEO、项目主管、写码员工、窄任务员工、架构/推理、质量运营、监管/复盘、Peekaboo、优化师、自优化开发、Hermes、智谱设计师。
- **保留辨识度**:对应原 emoji 含义(CEO 兔子、秘书信封/书、设计师调色板、Peekaboo 显示器…)。
- 存 `projects/控制台/artifacts/avatars/`(或 `public/assets/avatars/`),前端按角色 id 引用;**图未加载时 emoji 兜底**(不破现有渲染)。

## D. 风格自查(codex 自己做)
- codex 生成头像 + 改完页面后,**用 Peekaboo 截图自查整体风格是否统一**(配色 / 卡片 / 头像画风 / 间距);不统一就重生头像 / 再调页面,直到协调。

## 红线
- 不破现有「工位 / 链路图」数据、事件、队列、双视图切换;不回退 #18 / #22 / 组织层级 / 公告板 / new-api。
- imagegen 产物存本地;密钥 / token 不回显;零依赖或仅 CDN。
- 带视觉改动须 Peekaboo 截图对照。

## 验收
1. 输入框在**页面底部**、默认派给**秘书**、**随行数自动变高**;`Enter`/`Shift+Enter` 正常。
2. 整体办公室风;**所有 emoji 换成统一风格的生成头像**(512×512 透明,显示 44px),emoji 兜底仍在。
3. Peekaboo 截图风格自查通过;`git diff` 仅 `workspace.html` + 头像产物;无回退、无密钥。
