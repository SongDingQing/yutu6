# 控制台项目简报

## 目标

维护玉兔6本地控制台、文件队列、任务编排和工作区界面。

## 范围

- `projects/控制台/`
- 与控制台直接相关的 `shared/engine/`、`shared/routing/` 和 `tests/`

## 验收标准

- 变更具有实际文件或命令证据。
- 匹配风险的测试通过。
- 不回显或提交密钥、token、cookie、私钥与登录信息。
- 不跨入未注册项目部门。

## 当前任务

由 CEO 按任务增量写入；公共发行版不携带任何机器的历史任务。
