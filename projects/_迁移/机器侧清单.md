# 机器侧清单(玉兔搬家 · 需在 Mac 本机/登录完成)

> 这些动作**总管在沙箱里做不了**(要 Mac 终端、要登录、要网络授权)。按顺序勾。
> 脚本在 `projects/_迁移/scripts/`。密钥**只本机用、不外传、不贴聊天**。
>
> **进度(2026-06-18)**:✅ §0 Gitee SSH、§1 改版 Hermes(clone+plugins/voice-wake)、§2 `.env` 还原、依赖安装、`hermes doctor` 已完成,Hermes 自检通过(未接飞书)。详见 `status.md`。下面未打勾的是剩余项(多为授权 + YuanXiao/Simulaid)。

## 0. 登录 / 授权(本人 · OAuth/扫码/2FA)

来源:密钥目录《需手动重新连接清单.md》。**不要依赖拷贝旧 token**,逐项重登:

- [ ] **Claude Code**:`claude auth login` →(浏览器 OAuth)→ `claude auth status` 验证
- [ ] **Codex / OpenAI**:打开 Codex Desktop 登录,或 `codex login` → `codex login status`
- [ ] **GitHub**:`gh auth login`(或 `ssh-keygen -t ed25519` 后把公钥加 GitHub)→ `ssh -T git@github.com`
- [ ] **Gitee(改版 Hermes 用)**:公钥已生成(见 `shared/config/ssh-and-remotes.md`)→ 加到 Gitee→设置→SSH 公钥 →(在 Mac)`bash projects/_迁移/scripts/place-gitee-key.sh` → `ssh -T git@gitee.com`
- [ ] **飞书/Lark**:开放平台确认应用凭证 / 事件订阅 / 回调地址 / 机器人权限
- [ ] **Gmail connector**:Codex/ChatGPT 的 Connectors 里重连 Gmail(Google 2FA)
- [ ] **Quark 夸克网盘**:扫码登录,确认根目录有 `元宵` 文件夹
- [ ] **Apple Developer / TestFlight**:Xcode 登录;证书/profile 人工确认或重生成
- [ ] **Unity / 团结引擎**:登录,确认许可证 + Android 模块;团结到 unity.cn 自行装
- [ ] **Brave Search**:控制台确认 key 有效(值在 secrets.env → 由 §2 还原)
- [ ] **npm**:`npm login`(优先,不复用旧 token)

## 1. clone 仓库 + Hermes(改从 Gitee)

> 方案变更:旧机把**全量改版 Hermes** 推到 Gitee → 玉兔6 直接 clone,**不再需要 official+patch**。

- [ ] 先放置 Gitee 钥匙(见 §0 的 Gitee 项)并 `ssh -T git@gitee.com` 通过
- [ ] 把 Gitee 上改版 Hermes 仓库地址填进 `shared/config/ssh-and-remotes.md` 与 `clone-repos.sh`,然后:
      `git clone git@gitee.com:<你>/<仓库>.git ~/.hermes/hermes-agent`
- [ ] `bash projects/_迁移/scripts/clone-repos.sh`(YuanXiao → `~/Projects/YuanXiao` · Simulaid → `~/TuanjieProjects/Simulaid`)
- [ ] 恢复 `~/.hermes/{plugins,voice-wake}`(若 Gitee 仓库没带,从旧机取)
- [ ] (备选)若仍走 official+patch:旧机取 5 文件 patch(feishu.py / run.py / plugins.py / transcription_tools.py / 测试)

## 2. 还原密钥 .env + SSH key

- [ ] `bash projects/_迁移/scripts/restore-secrets.sh`
  - 按 `# Source` 精确分发:`~/.hermes/.env`(11 项:FEISHU_*/BRAVE_/MINIMAX_*/LLM_MODEL/API_SERVER_*)+ `~/Projects/YuanXiao/ops/config/yuanxiao.env`(全部 YUANXIAO_*)
  - 自动 `chmod 600`,不打印任何值
- [ ] **SSH 私钥**(不在 secrets.env):使用 `~/.ssh/id_ed25519_github_yutu6` 并 `chmod 600`;迁机时从保险库恢复或重新生成后加 GitHub
- [ ] 其他文件类凭证:`~/.android/debug.keystore`(可拷;正式签名另配)

## 3. 拷旧机自定义能力(新机 .codex 是全新装,无自定义)

- [ ] 从**旧机 / 备份**拷 `~/.codex/skills`(41 个)+ `~/.codex/modules` → 新机 `~/.codex/` 同位置
- [ ] 拷完把 `shared/capability_registry/registry.json` 与 `skills-manifest.md` 里相应项
      状态 `pending_from_old_mac` → `present`
- [ ] 读 `~/.codex/skills/SKILL_ARCHITECTURE.md` 做一致性检查

## 4. 装环境 / 工具

- [ ] Xcode CLT · Homebrew · Python3 · Node · Git(部分由 `安装.command` 已装)
- [ ] Android Studio / JDK / Gradle(YuanXiao 构建 APK 用)
- [ ] Unity / 团结引擎(Simulaid 用)
- [ ] **Ollama**(知识库 embedding 用):`ollama serve` + `ollama pull nomic-embed-text`

## 5. 建本地知识库 kb.sqlite

- [ ] `bash knowledge/build.sh`(需 §4 的 Ollama 在跑)→ 建向量+全文索引
- [ ] 冒烟:`python3 knowledge/query.py "玉兔6 的四层组织是什么"`

## 6. 验证 smoke test(迁移记录 08 §10.4)

- [ ] Hermes 启动 · 飞书收发 · 播报后能再唤醒 · 忙碌时新消息进队列不打断
- [ ] Codex handoff 建任务回传 · Gmail 发送走确认卡片
- [ ] YuanXiao 构建 debug/release APK · Simulaid 开 Unity/团结 跑基础检查
- [ ] 自动研究能出报告,且来源、许可证与采纳状态可追踪
