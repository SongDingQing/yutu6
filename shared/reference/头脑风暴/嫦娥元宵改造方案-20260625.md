# 嫦娥部署 + 元宵 UI 改造 + 玉兔6 办公室服务器化(文件级方案)

> 2026-06-25 · 维修主管(4 路并行调研 workflow 综合)· 代码在 /Users/yutu6/Projects/YuanXiao-repo

## 架构
嫦娥(`server/yuanxiao-server/`)= Ubuntu HTTPS relay → 部署到 49.235.187.125;hermes/传奇/codex 走 `bridge/`(Mac mini 侧);元宵(`android/YuanXiao/`)= 安卓 App(本地构建)。

## A) 自包含嫦娥部署(去 hermes/传奇)— 代码侧现在可做,部署卡 SSH key
- `yuanxiao_server.py`:加 `BRIDGE_ENABLED=env(YUANXIAO_BRIDGE_ENABLED,"0")`;转发端点(GET :987/:1006/:1025/:1072/:1092、POST :1154、chat :1186)入口短路 `if not BRIDGE_ENABLED: 503 bridge_disabled`。`forward_*`(:694-760)保留不删(可逆)。`/health`(:864) mode→`self-contained-relay`,bridge 能力位按开关置 False。legend 历史过滤(:324)保留(纯本地)。
- `deploy_server.sh:31`:required_vars 删 `YUANXIAO_HERMES_BRIDGE_URL`。env 设 `YUANXIAO_SYNC_LOCAL_BRIDGE=0`(:398 自动跳过 bridge 部署)。不跑 render_launchagents.sh / deploy_local_bridge.sh。systemd 单元不用改。
- env 新增 `YUANXIAO_BRIDGE_ENABLED=0`、`YUANXIAO_HANDOFF_QUEUE_WORKER_ENABLED=0`、`YUANXIAO_SYNC_LOCAL_BRIDGE=0`;删 Mac mini/隧道/Hermes/Codex 段。
- 验收:`curl -k https://host:port/health`→mode=self-contained-relay;/api/chat、/api/codex/* 返回 503(预期)。
- 需老板补:**SSH key(change.pem 未在本机)**、TLS 域名(SAN)、是否还要"聊天/识图"(当前方案 chat 直接 503)。

## B) 元宵 UI 精简(MainActivity.java ~16k 行,全程序化无 XML)
- 干净可删:传奇/Legend 整块(tab :1669、buildLegendView :1507、showLegend :2050、TARGET_LEGEND :172 等)、视觉工作台 YuanXiaoWorkbenchActivity.kt(孤儿屏,但若 C 用 Compose Pager 可留作模板)、codex-desktop 镜像(:6677/:6870/CodexSessionSyncWindow.java)。
- 谨慎:**Hermes 是日常聊天默认后端**(TAB_HERMES :169,玉兔 tab :1666)——不要纯删,除非老板确认放弃聊天。
- 需老板补:是否放弃 hermes 聊天、是否保留 codex 任务/计划视图。

## C) 元宵"办公室视图 + 上下滑动切换"
- 推荐:新建 `ui/OfficeActivity.kt` 用 Compose `VerticalPager`(Foundation 已在依赖),一页一办公室上下滑;入口在 buildTopTabBar :1666 加"办公室"chip;Manifest 注册;独立 Activity 不动 16k 行主体。
- 备选:MainActivity 内加 officeView + ViewPager2 ORIENTATION_VERTICAL(需加 viewpager2 依赖)。
- 美化:Compose 走 YuanXiaoTheme.kt;程序化 UI 改 YuanXiaoPalette.java(:11-52);XML themes.xml/colors.xml(yuanxiao_primary #5F92FF)。
- 需老板补:办公室展示什么、几个、静态还是接服务端、是否与玉兔6 办公室同源。

## D) 玉兔6 办公室"智能体+服务器"(workspace.html)— 现在就做
- 关键约束:服务器条目**必须绕开 AGENT_META**(否则污染 updateOverview 工位计数 :1033、renderDesks :944、flow 图节点)。单独分区。
- D1:`.office-top` 加 zone `#office-servers`(仿维修区 :296);`SERVER_META=[{id,label:'嫦娥',ip,accent,kind:'server'}]`;renderOffice 加渲染;新写 `serverCardHtml(s)`(不复用 officeActorHtml,后者依赖 ensureAgent/sprite)。
- D2:机箱图标、IP 用等宽、在线绿/离线红降饱和、冷色 accent 区别五彩 agent。
- D3 探活:server.js 加 `GET /api/servers/status`,net.connect 测端口(避开自签 TLS),前端轮询合并 online 重渲。清单可进 config.json `servers`。
- 需老板补:服务器清单/探活端口。

## 建议顺序
A(嫦娥上线/health 可用)→ D 探活打 A 的 /health → B 裁 UI → C 加办公室视图。
**最关键老板输入**:SSH key + TLS 域名(A、D 卡这);是否保留 hermes 聊天(决定 A chat + B2)。
