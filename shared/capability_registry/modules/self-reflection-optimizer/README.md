# 自省优化共享能力

自省优化把“针对某模块挑刺并优化”标准化为一个可审计流程:

1. 解析目标模块。
2. 读取模块文档、注册表、学习案例、相关测试和 artifact。
3. 建立 critique ledger。
4. 对每条缺点给出改法并分类。
5. 低风险高收益项自动执行并验证。
6. 不确定、争议、影响现有功能的项交给主人拍板。
7. 把优秀案例写回学习案例库。
8. 新优秀案例可触发下一轮自省优化。

它不是“大改授权”。自动执行只适用于局部、可回滚、证据充分、低风险的改动。

Starlaid/星桥排除;密钥值不读取、不回显、不提交。
